# Backend API (FastAPI, Step 8)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase project (Settings -> API). Same project as backend/.env -
# these are the public anon key, safe to expose to the browser.
NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
