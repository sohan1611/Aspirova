# Aspirova — Architecture & Strategy Canon

**Tagline:** Every opportunity. One place.
**What it is:** An AI-powered Career Intelligence Platform that *discovers* student opportunities automatically from public sources. It is **not** a job board — companies never post here.

> **Authority model.** This `docs/` folder is the source of truth. The Chief Architect (Claude Opus) owns it. The implementation agent (currently Claude Sonnet; possibly OpenAI Codex later) MUST conform to these documents. If reality forces a deviation, the engineer raises it; the architect amends the doc. Code never silently diverges from the canon.

---

## How to read this

| # | Document | Owner question it answers |
|---|----------|---------------------------|
| 01 | [Product Strategy](01-product-strategy.md) | Why will this win, and who pays? |
| 02 | [System Architecture](02-system-architecture.md) | What are the moving parts and why? |
| 03 | [Data Architecture](03-data-architecture.md) | How is data modeled, indexed, grown? |
| 04 | [Crawler System](04-crawler-system.md) | How do we discover opportunities cheaply & legally? |
| 05 | [AI Systems](05-ai-systems.md) | How do AI features work without bankrupting us? |
| 06 | [Pricing & Cost Analysis](06-pricing-and-cost-analysis.md) | Do the unit economics actually work? |
| 07 | [Implementation Roadmap](07-implementation-roadmap.md) | What gets built, in what order? |
| 08 | [Engineering Governance](08-engineering-governance.md) | What rules must every coding agent follow? |

Each document ends with a **HANDOFF TO ENGINEERING** block — the only part the coding agent is required to act on directly.

---

## The five things you must internalize before reading further

These are the architect's brutally honest top-of-mind conclusions. The rest of the canon defends them.

1. **Stop thinking "scrape the aggregators." Think "go to the source."**
   Internshala, Unstop, Wellfound, and LinkedIn all forbid scraping in their ToS and actively block bots. Building the company on scraping *other aggregators* is legally fragile and technically a treadmill. The durable moat is crawling **company ATS endpoints directly** — Greenhouse, Lever, Ashby, and Workday expose structured, semi-public JSON board endpoints per company. That is where 60–70% of real, fresh, deduplicated opportunity data should come from, at near-zero cost and near-zero legal risk. Aggregators become a *secondary, best-effort* tier, not the foundation. **This single decision reshapes the entire crawler architecture (Doc 04).**

2. **Pricing: Pro Lite ₹39 / Pro ₹49, with annual plans ₹399 / ₹499 (~15% off).**
   The ₹10 (~25%) monthly gap means a rational student takes Pro for the AI features — sound decoy psychology, with Pro Lite as the anchor. **Annual plans are the default offer** (₹399 Pro Lite saves ₹69; ₹499 Pro saves ₹89 vs 12× monthly — both ~15%, "2 months effectively free"): they cut transaction count ~12×, slash involuntary churn, and front-load cash. The higher prices lift blended ARPPU to ₹47.5/mo and move break-even down to **~1,000 users**. Doc 06 models this.

3. **"Crawl once, store centrally, serve many" is correct — and it must extend to AI.**
   The expensive trap is *per-user* AI. The architecture enriches each **canonical** opportunity exactly once (summary, tags, embedding) *after* deduplication, then every user reads the shared result for free. Resume Match is **embedding cosine similarity (no LLM call per match)**, not an LLM grading each resume against each job. Only the Career Copilot is intrinsically per-user — so it is gated to Pro, rate-limited, model-cheap, and cached. Doc 05 enforces this.

4. **Postgres is your search engine, your vector store, and your queue. Add nothing else yet.**
   `tsvector` (full-text) + `pg_trgm` (fuzzy) + `pgvector` (semantic/resume match) + `SELECT … FOR UPDATE SKIP LOCKED` (job queue) means one managed Postgres (Supabase free tier) does the work people reach for Elasticsearch, Pinecone, and RabbitMQ to do. At Aspirova's scale, those would be cost and operational liabilities, not assets. Doc 02/03 defend this.

5. **Be honest about the money: at ₹39/₹49/month and ₹95/$, you break even around ~1,000 users.**
   This is currently a **project, not a live product** — profit isn't the immediate goal, a correct/sustainable build is. But the model is sound: running lean, out-of-pocket stays under ~₹950/mo at every stage (well inside a ₹2,000/mo budget), break-even is ~1,000 users at a sober 3% conversion, and it's clearly profitable by ~1,500. Accelerators stack on top: (a) better Pro conversion, (b) **B2B campus licenses** (one ₹30k/yr deal ≈ 53 Pro subs), (c) annual uptake, (d) ethical sponsored listings + affiliates. Doc 06 quantifies all of it.

---

## Canonical technology decisions (the short version)

The full rationale and rejected alternatives live in the relevant docs. This is the binding summary.

| Layer | Decision | Why (one line) | Doc |
|-------|----------|----------------|-----|
| Frontend | **Next.js on Vercel (free tier)** | Best free hosting + SSR/SEO for opportunity pages | 02 |
| Backend API | **FastAPI (Python) on Render** | AI/crawl/data work is cleaner in Python; clean REST boundary | 02 |
| Database | **Postgres via Supabase** | Relational + FTS + vector + queue in one managed box | 02/03 |
| Search | **Postgres `tsvector` + `pg_trgm`** | Free, sufficient to ~1M opps; no Elasticsearch | 03 |
| Vector / Resume Match | **`pgvector` (HNSW)** | Shared embeddings; match = cosine, no per-user LLM | 03/05 |
| Crawlers | **Python workers on GitHub Actions cron** | Free scheduled compute; keeps Render server light | 04 |
| Primary data source | **ATS JSON endpoints (Greenhouse/Lever/Ashby)** | Structured, low-risk, near-free; the real moat | 04 |
| Browser automation | **Playwright — only for JS-heavy sites, as jobs** | RAM-hungry; never always-on, never the default | 04 |
| Queue / scheduling | **Postgres `SKIP LOCKED` + GH Actions matrix** | No Kafka/RabbitMQ; dead simple | 02/04 |
| Cache / rate limit | **Upstash Redis (free tier)** | Serverless, pay-per-use, rate limiting for AI/API | 02 |
| Email | **Resend (free → Pro), AWS SES at scale** | Cheapest path; SES is ~$0.10 / 1k emails | 02/06 |
| AI enrichment | **Cheapest viable model (Haiku-class), once per canonical opp** | Shared output, never per-user | 05 |
| Payments | **Razorpay; annual prepay preferred** | India-native; annual cuts txn cost + churn | 06 |
| Object storage / backups | **Cloudflare R2 or Backblaze B2** | Cheap/no-egress nightly `pg_dump` | 03 |

---

## Decision log (append-only)

| Date | Decision | Status |
|------|----------|--------|
| 2026-06-23 | Adopt ATS-first crawling; aggregators demoted to best-effort tier | **Binding** |
| 2026-06-23 | Postgres-only data plane (search + vector + queue) for Phase 1–3 | **Binding** |
| 2026-06-23 | Resume Match = embedding cosine, not per-user LLM | **Binding** |
| 2026-06-23 | Crawlers run on GitHub Actions, not the Render web dyno | **Binding** |
| 2026-06-24 | 5-phase roadmap with enforced phase gates; Phase-1 MVP is ATS-first, no payments/AI (Doc 07) | **Binding** |
| 2026-06-24 | Engineering governance adopted; §3 HARD RULES are the merge gate; canon is law, agents are interchangeable (Doc 08) | **Binding** |
| 2026-06-24 | `ai_client`, `can(user, feature)`, and `SourceAdapter` are mandated seams (most HARD RULES are satisfied by routing through them) | **Binding** |
| 2026-06-25 | **Pricing (SUPERSEDES earlier ₹25/₹30):** Pro Lite ₹39/mo · ₹399/yr; Pro ₹49/mo · ₹499/yr. Annual = ~15% off (Pro Lite saves ₹69, Pro saves ₹89). Annual is the default checkout option (Doc 06) | **Binding** |
| 2026-06-25 | FX assumption corrected to ₹95 = $1 across the canon; blended ARPPU ₹47.5/mo; break-even ~1,000 users at 3% conversion (Doc 06) | **Binding** |
| 2026-06-25 | Aspirova is currently a **project (build), not a live product**; profit is a reference model, not the immediate goal | **Binding** |
| 2026-06-30 | Deploy read path now as a **minimal, reversible measurement** — Render **Singapore** ($7 Starter) + Vercel Hobby, Supabase stays Mumbai; purpose = real production p95 + portfolio artifact, NOT "Phase 1 shipped" ([Topology & Deploy ADR](handoffs/TOPOLOGY-AND-DEPLOY-DECISION.md)) | **Binding** |
| 2026-06-30 | Crawler **set-based bulk-operation** refactor required **before Phase 2 scales source count** (root cause of slow GH-Actions→Mumbai crawls is round-trip count × link latency, not commit frequency); non-blocking for the deploy | **Binding** |
| 2026-06-30 | Render has **no Mumbai region** (Singapore is closest, ~120ms to Mumbai DB); strategic direction is **consolidate compute + DB in Singapore**, but gated on the measured p95, not assumed | **Proposed — measure first** |
| 2026-06-30 | **Phase 1 PASSES architect review** (crawler + deployment + auth flow all verified live; no blocking defects). Build phases 1-2 done; Phase 2 (build phases 3-4) issued via [PHASE-2-HANDOFF.md](handoffs/PHASE-2-HANDOFF.md) | **Binding** |
| 2026-06-30 | **Lever-2 ruling:** do NOT migrate Supabase to Singapore yet. Production p95 is over target + variable but not isolated to the DB hop. Phase 2 adds server-side `X-DB-Time-Ms` instrumentation + Upstash read-caching first; migrate only if the *isolated* DB-hop p95 is over budget | **Binding** |
| 2026-06-30 | **Lever-1 (crawler set-based bulk ops) is a hard precondition** inside Phase 2 — must land before Lever/Ashby/aggregator adapters, else the ~12m45s crawl blows the 25-min timeout at 3-5× the source count | **Binding** |
| 2026-06-30 | Rate limiting on the public API is **urgent Phase-2 work**, not deferrable — the API is already public and unthrottled over a $7 Render + free pooler | **Binding** |
| 2026-07-05 | **Part 2.1 rulings** (see [PHASE-2-HANDOFF §11](handoffs/PHASE-2-HANDOFF.md)): rate limiter **fails open**; per-IP keys on `X-Forwarded-For` (not the Render proxy IP); cache invalidation = short TTL backstop **+** crawler-driven version-bump (never SCAN/DEL), TTL-only acceptable if crawler→Upstash is deferred; limits/TTLs are **config, not code**; cache check runs before `get_db`; rate-limit/cache middleware sit inside CORS | **Binding** |
| 2026-07-05 | **Phase 2 (build phases 3-4) engineering complete**, merged to `master`, deployed live, CI green. 4 of 8 parts' acceptance criteria partially blocked on manual prerequisites (Upstash/R2-B2/Razorpay/Resend accounts — none created yet); every integration fails open/closed safely without them. Full account: [PHASE-2-REPORT.md](handoffs/PHASE-2-REPORT.md) | **Binding** |
| 2026-07-05 | **Lever-2 isolated DB-hop measurement** (live production, n=30/endpoint): X-DB-Time-Ms p95 127–202ms; X-Total-Time-Ms p95 192–293ms — already under the 300ms target *including* the DB hop, before Upstash caching is even live. Leans toward Lever-2 not being urgent, but the migrate/no-migrate call is the architect's per the standing role model. Full data: [PART-2.3-LEVER-2-MEASUREMENT.md](handoffs/PART-2.3-LEVER-2-MEASUREMENT.md) | **Measured — architect to rule** |
| 2026-07-05 | **Phase 2 closeout, Resend + Upstash manual prerequisites verified live**: Resend sending domain (`aspirova.org`) fully verified, real digest email delivered via the actual pipeline code; Upstash rate limiting fires exactly at the configured 60/min threshold (60×200 then 429s in a live burst test) and read-cache hits confirmed (`X-DB-Time-Ms` 0.00ms). Cached p95 re-measured at 7.9–8.1ms across all three endpoints (down from 192–293ms uncached) — reaffirms the Lever-2 no-migrate reading with an even wider margin. R2/B2 and Razorpay remain open (Razorpay explicitly deferred to last by user decision). Full detail: [PHASE-2-CLOSEOUT.md](handoffs/PHASE-2-CLOSEOUT.md) | **Verified** |
| 2026-07-06 | **Cloudflare R2 manual prerequisite verified live, real backup/restore bugs found and fixed (two of them)**: (1) `postgresql-client-16` in `backup.yml` was a wrong assumption — production Supabase actually runs **Postgres 17.6**, and `pg_dump` cannot dump from a server newer than itself; fixed by pinning `postgresql-client-17`. (2) even after that fix, the real GitHub Actions run still resolved `pg_dump` to 16.14 — `ubuntu-latest`'s runner image ships a pg_dump 16 binary already on `PATH` that shadows the correctly-installed v17 package (which installs to `/usr/lib/postgresql/17/bin/`, not `/usr/bin/`); fixed by prepending the versioned bin dir to `$GITHUB_PATH`. Both fixes verified end-to-end against real production with explicit user authorization at each step: real local Docker restore drill (all 17 tables, exact row-count match) plus a real `workflow_dispatch` run of the actual "Nightly Backup" GitHub Actions workflow succeeding unattended. **All three non-payments manual prerequisites (Resend, Upstash, R2) are now closed** — only Razorpay remains, deferred to last by standing user decision. Full detail: [PART-2.2-RESTORE-DRILL.md](handoffs/PART-2.2-RESTORE-DRILL.md) addenda | **Verified** |
| 2026-07-06 | **Roadmap re-sequenced: Premium UI (new Phase 2.5) → AI (Phase 3) → Razorpay wiring.** Razorpay deferred by founder decision; architect concurs and reorders per Doc 07's own law ("don't monetize/enhance a weak product"). Payments stay **fail-closed and honest** (pricing = waitlist, never a checkout that errors) until wired after AI, when the paywall will gate real Pro value on a premium-feeling product. Does not void any Binding row — inserts a phase and moves the *completion* of Phase 2's payment gate later. The webhook out-of-order/replay fix (flagged in the Phase-2 review) folds in with the eventual Razorpay wiring | **Binding** |
| 2026-07-06 | **Phase 2.5 (Premium UI & Experience) issued** to engineering: design-system-first (Tailwind v4 `@theme` tokens → owned primitives on Radix → redesigned feed/detail/auth/shell + honest pricing-waitlist page), 8 parts. **Direction = Professional/Trust, blue-led** (founder's call), **system-adaptive theme + manual toggle**; architect caveat pinned — execute it *premium* (typographic rhythm, spacing system, micro-interactions) to avoid the generic-corporate trap. SSR/SEO growth surface stays server-rendered; no AI, no functional payments. Full brief: [PHASE-2.5-HANDOFF.md](handoffs/PHASE-2.5-HANDOFF.md) | **Binding** |
| 2026-07-06 | **Phase 2.5 engineering complete**, all 8 parts merged to `master`, full backend suite green (133/133), production build clean. All 5 gate conditions met: design system consumed everywhere, feed/detail/auth/shell/pricing redesigned in both themes, SSR/SEO intact (`/` and `/opportunity/[slug]` still dynamic), **Lighthouse accessibility 100 on both feed and detail** (target ≥95), pricing page is a real waitlist with zero functional-checkout risk. Six real defects found and fixed via live verification (not code review alone): an unapplied font, a token-naming collision, a route-scoped loading-UI leak, a doubled page title, a self-contradicting dialog title, and **two real WCAG AA contrast failures** (muted-foreground on muted at 4.34:1, white-on-destructive at 3.76:1 — both were unmodified shadcn-preset defaults, now fixed and re-verified at 6.9-7.6:1 and 4.83:1). Full report: [PHASE-2.5-REPORT.md](handoffs/PHASE-2.5-REPORT.md) | **Superseded — gate was not actually met, see next row** |
| 2026-07-06 | **Architect review caught the Phase 2.5 gate was NOT met: CI was red.** The report's "production build clean" was verified only locally *with the backend up*. In reality `/pricing` (added in Part 3.7) is a **static** route that fetched `GET /plans` at **build** time; CI's frontend build has no backend, so `next build` failed prerendering `/pricing` with ECONNREFUSED. The Part 3.7 commit's "local-only artifact, Vercel always has the backend" reasoning was wrong twice (CI is the gate and has no backend; even Vercel would couple frontend deploys to a cold-starting $7 Render dyno). **Fixed** with a canonical in-bundle fallback mirroring `seed_plans.py`, so `/pricing` renders even when `/plans` is unreachable at build or request time; stale `ci.yml` comment corrected. **CI now green — Phase 2.5 gate genuinely met. Phase 3 (AI) can now be issued.** New verification rule: statically-prerendered pages must be built the way CI runs them — **backend down** | **Binding** |
| 2026-07-06 | **Claude + Codex combo adopted** (founder decision, proven live). Operating model: **Opus = Chief Architect** (writes handoffs/reviews/rulings/canon — unchanged); **Claude Code (Sonnet) OR Codex = interchangeable engineer/hands**; the **founder picks which agent per task**. Canon-compatible by the pre-existing role model ("Codex could replace Sonnet later; the canon, not the agent, is authoritative"). Guardrails bind regardless of agent: (1) canon + Doc 08 HARD RULES are law; (2) the merge gate is **CI-green, not local-green**; (3) every commit authored **solely as `sohan1611`** — no `Co-Authored-By` (Claude or Codex), no Dependabot; (4) the non-implementing agent reviews before commit. Mechanism: the `delegate-to-codex` skill (Codex CLI, `workspace-write` sandbox); Claude↔Codex exchanges logged to `.agents/collab-log.md`. First proving task (the `/pricing` CI fix above) landed clean under review | **Binding** |

| 2026-07-06 | **Phase 3 (AI) issued.** Implements Doc 05 under Doc 06 cost discipline, 7 parts, **cost-last sequencing** (build the `ai_client` seam + `ai_usage`/spend-alarm/pgvector guardrails against a stub first — zero spend; wire the real provider key only at Parts 6–7). Locked rulings: `ai_client` is the single seam for generation AND embeddings; generation = Haiku-class (`claude-haiku-4-5`, confirm live); embeddings via a dedicated model (Voyage or OpenAI `text-embedding-3-small` — engineer confirms in Part 1, sets pgvector dim to match, stores model per vector); **Resume Match = pgvector cosine, ZERO per-match LLM** (the viable-vs-bankrupt call); no "will you get in" prediction (reframed to statistical reopen prediction); daily spend cap + graceful degradation, config-driven, blank keys fail safe. Razorpay wiring (+ webhook fix) folds in after Phase 3. Full brief: [PHASE-3-HANDOFF.md](handoffs/PHASE-3-HANDOFF.md) | **Binding** |

| 2026-07-06 | **Phase 3 (AI) build complete** (Parts 1-6, all merged + CI-green): enrichment, Resume Match (zero per-match LLM), Hidden flag + Reopen prediction, Weekly Report, Career Copilot (Pro-gated/capped/cached/grounded + prompt-injection-defended). All key-guarded → dormant at **$0 AI spend** until Part 7 go-live (founder adds Anthropic + OpenAI keys). Workers operationalized in GH Actions (compute_hidden wired into the crawl; enrich dispatch-only pending a cost dry-run; weekly-report cron gated+key-guarded). Built via the Claude→Codex delegation loop (11 work orders, each architect-reviewed, verified on a throwaway pgvector container + production, `sohan1611`-only). Finding: all 4 sources are Tier-1, so the Hidden-gem feature has no qualifying data until Tier-2/3 niche sources are added | **Binding** |
| 2026-07-06 | **Phase 4 (growth loops) STARTED EARLY, partial** — architect ruling: use the pre-go-live window for the Phase-4 parts that need **neither AI keys nor payments** (Part 1 SEO content engine; Part 2 referral loop with a comp-Pro-`subscription` reward, no Razorpay), holding the conversion/payment-dependent parts (campus-ambassador, B2B licenses, sponsored/cost-opt) for the real Doc-07 gate (Phase 3 done + payments live + conversion moving). A deliberate limited early start, not gate-jumping. Founder chose Phase 4 as the post-go-live direction. Full brief: [PHASE-4-HANDOFF.md](handoffs/PHASE-4-HANDOFF.md) | **Binding** |
| 2026-07-07 | **Phase 4 Part 1 (SEO content engine) complete** (Parts 1/1b/1c, all merged + CI-green): `JobPosting` JSON-LD + dynamic `sitemap.xml`/`robots.txt` + cached backend slug/company endpoints; long-tail SSR landing pages (`/internships`, `/remote`, `/jobs`, `/companies/[slug]`); dynamic `next/og` OG/social share cards (branded fallback so a backend-less build never breaks). Verified live against production data. Key-free, payment-free | **Binding** |
| 2026-07-07 | **Phase 4 Part 2 rulings** (see [PHASE-4-PART-2-SPEC.md](handoffs/PHASE-4-PART-2-SPEC.md)): (A) the referral reward is a **bounded 30-day comp `pro_lite_monthly` subscription** (`razorpay_sub_id` NULL), **not** full Pro — it unlocks the non-AI Pro features but leaves `copilot`/`resume_match`/`prediction` OFF, so the reward is **zero-marginal-AI-cost** at go-live (Doc 06). (B) **`can()`/`get_features` refined** (architect-approved amendment of the single gating seam, Doc 08 §1 — still the *only* seam, now expiry-aware): comp subs (`razorpay_sub_id IS NULL`) grant features only while `current_period_end IS NULL OR > now()`; **Razorpay subs are byte-for-byte unchanged** (first `OR` branch short-circuits), so no paying user can ever be affected and bounded comp terms expire without a cron. Part 2a (backend) built + container-verified (migration up/down, 20 new tests, zero row-leak); landing awaits founder authorization of the additive `users.invite_code` prod migration | **Binding** |
| 2026-07-07 | **Phase 4 Part 2 (referral / invite loop) LANDED** (Parts 2a+2b merged to `master`, CI green, deployed): `/referral/me` + `/referral/claim`, unique `users.invite_code`, guarded `record_referral` (self-referral + set-once + row-lock), comp `pro_lite_monthly` reward, the comp-path gating refinement, and the frontend `/referral` invite page + silent first-touch `?ref=` capture + `Invite` nav. Founder authorized the additive `users.invite_code` migration; applied to production Supabase (now head `f2a9c7d4e1b0`); full backend suite green vs prod (173), **zero test-row leak** (0 comp subs / 0 invite codes / 0 users created in the run window). Completes the buildable-now half of Phase 4 (Parts 1+2); Parts 3-5 stay held for the payments/conversion gate | **Binding** |
| 2026-07-07 | **Source expansion: SmartRecruiters (4th ATS adapter) LANDED + activated.** New `SmartRecruitersAdapter` via the `SourceAdapter` seam (pipeline unchanged; `run_tier(1)` auto-discovers it), seeded Wise/Western Digital/Universal Music Group; **+598 real opportunities** ingested (catalog 3,050 → 3,669). Incidents found + fixed: capitalized board-token slugs (→ `board_token.lower()` + a 3-row prod UPDATE), a concurrency flake in `test_runner_isolation` (unique per-run company name), and a per-posting-detail crawl cost (24m44s → `crawl-tier1.yml` timeout 25→40 as a stopgap; lazy-detail optimization is a tracked follow-up). Founder-authorized the prod seed + crawl | **Binding** |
| 2026-07-07 | **Companies directory LANDED.** `GET /companies` (grouped active-opp counts, cached + rate-limited) + a browsable SSR `/companies` index (138 companies) + sitemap/footer entries. Full synthesis of the Phase-4-so-far work (Parts 1+2, source expansion, directory, all incidents/fixes, current live state, gate status): [PHASE-4-REPORT.md](handoffs/PHASE-4-REPORT.md) | **Binding** |
| 2026-07-08 | **AI go-live (partial): Resume Match is LIVE; AI spend cap made real; expensive AI deferred.** Founder loaded $5 Anthropic + $5 OpenAI; real budget **₹950–1,500/mo ≈ $0.45/day**. Findings + fixes: (1) the Phase-3 daily cap was a **no-op** (`ai_client` recorded `est_cost=0` everywhere; `ai_budget` summed it) — now **real**, computing cost from env-driven per-MTok rates on keyed paths, default cap tightened **$2 → $0.45/day**; (2) the `openai`+`anthropic` SDKs were never installed (dormant keyed paths hid it) — added. Ran an **embeddings-only backfill** of all **3,749 opportunities** (total **$0.083**), skipping the ~**$16** Haiku summary/tags backfill (>1 month's budget). **Resume Match verified live end-to-end through Render** (OpenAI key added to Render; a backend résumé returns backend roles by pgvector cosine, zero-LLM at match time, Pro-gated). **Cost ruling (Binding):** high per-use AI (Haiku enrichment/summaries, Copilot, weekly-report intro, nightly enrich schedule) stays **OFF until paying customers** — cost follows subscription revenue (Doc 06). Total session AI spend ≈ **$0.084**. Razorpay is the remaining evening track (needs a founder Razorpay account) | **Binding** |
| 2026-07-08 | **Razorpay wiring COMPLETE (code), dormant behind a flag until go-live.** Founder added **test-mode** Razorpay keys. Done: `setup_razorpay_plans` created the 4 test-mode plans + linked `razorpay_plan_id` onto local rows; checkout plumbing verified (created + cancelled a real test subscription); **webhook out-of-order/replay hardening** (`_TERMINAL_STATUSES` guard so a replayed `charged` can't resurrect a canceled sub + monotonic `current_period_end`) — the fix flagged in the Phase-2 review; **flag-gated `/pricing` checkout UI** (`SubscribeButton` → Razorpay Checkout) behind `NEXT_PUBLIC_PAYMENTS_ENABLED`. **The live public `/pricing` stays the honest waitlist** (flag off = default; verified live-preview both ways) — no test-mode checkout shown to real visitors, consistent with the 2026-07-06 "pricing = waitlist, fail-closed until go-live" ruling. Founder go-live steps remaining: configure the Razorpay webhook (→ `…/payments/webhook`, `subscription.*` events, copy secret) → add `RAZORPAY_KEY_ID/SECRET/WEBHOOK_SECRET` to Render → complete **KYC** → set `NEXT_PUBLIC_PAYMENTS_ENABLED=true` on Vercel → test-card end-to-end | **Binding** |

| 2026-07-28 | **Amendment: Supabase Storage admitted to the data plane (bounded).** Doc 08 §3 requires "Postgres-only data plane (no new infra without amendment)"; the persistent-resume feature (PR #10) introduced **Supabase Storage** without one, so this row is the retroactive architect amendment. Scope is deliberately narrow: **binary user uploads only** (one resume PDF per user at `resumes/{user_id}/resume.pdf`, private bucket, PDF-only, 5 MB cap, owner-only RLS). All *structured* data stays in Postgres — `users.resume` holds only metadata + the ATS result. Justified because it adds **no new vendor** (Supabase is already the data plane), uses capacity already paid for on the Pro plan (100 GB, previously 0 used), avoids the R2/B2 dependency Phase 2 had planned, and keeps resume PII off the API/Render path (uploads go browser→Storage directly). Bucket + RLS are reproducible from `backend/scripts/setup_resume_storage.sql`. **Bound:** any further Storage use beyond user-uploaded binaries needs a fresh amendment | **Binding** |
| 2026-07-31 | **Programmes Registry architected** (not yet built): a new first-class entity for RECURRING ANNUAL PROGRAMMES — IIT/IISc/TIFR research internships, IASc/INSA fellowships, ISRO/DRDO/MeitY government internships, GSoC/Outreachy/LFX open source, Mitacs/CERN/DAAD international, and recurring competitions (~200 entities from the founder's master list). These cannot be represented as `opportunities`: they are one stable organisation with a new edition each year, a fixed window, and long dormant periods, so forcing them into the crawled-listing model would either show closed programmes as open or hide them 11 months a year. **Rulings:** (1) **curated JSON registry committed to the repo + seeded to Postgres, NOT 200 website scrapers** — programme sites are heterogeneous and change annually, so scraping would be fragile, costly and re-derive facts yearly; this follows the existing taxonomy.json / skillsLexicon.json / colleges pattern. (2) **Two tables** `programmes` (stable entity) + `programme_editions` (per-year, verified separately) — the split is what makes honest status possible. (3) **Default status is `expected`, never `open`**; only a VERIFIED `open` edition may surface as actionable, so a stale registry degrades to "informative", never "wrong" (founder's explicit instruction). (4) **Human-in-the-loop verification**, weekly not daily, reusing pipeline/liveness.py — the job flags, a person confirms; never auto-promote to open. (5) Learning resources (Codeforces/CLRS/NeetCode/MDN) are **out of scope** — they are resources, not opportunities, and admitting them changes what the product is. Adds **no new infrastructure, no new vendor, no AI, ~$0 cost**. Full brief: [PROGRAMMES-REGISTRY-HANDOFF.md](handoffs/PROGRAMMES-REGISTRY-HANDOFF.md) | **Binding** |
| 2026-08-03 | **Programmes Registry Parts 4-partial/5a LANDED, plus a live honesty violation found and removed** (PRs #35/#36/#37, all merged + CI-green + seeded to production). (1) **`/research` was a second, contradictory registry.** `frontend/lib/researchPrograms.ts` hardcoded 7 programmes and decided openness from the **browser clock** — `isApplyWindowLikelyOpen()` returned true when the current month fell inside a hardcoded window **or when the window field was missing**, flipping the card CTA to "Apply on the official site". In August that was live on 2 of 7 cards, on a route in the primary nav and the sitemap. This directly violated the standing "no date-based inference, server or client" rule. `/research` is now a view over the registry; the file and `ResearchProgramCard` are deleted and a regression test fails if `isApplyWindowLikelyOpen`/`applyWindow` reappear. 3 of its 7 entries were duplicates of registry programmes (verified by identical official URLs, `iitk-surge` an outright slug collision); the other 4 were migrated. Registry 48 → 52. (2) **Ruling: the "current edition" is the highest-`year` edition that is not `discontinued`**, replacing `year == current_year`, which broke whenever an application window precedes its cohort year (DAAD RISE publishes 15 Oct – 30 Nov 2026 for the **2027** cohort — authored honestly, it was invisible). The selection is **date-free**: it picks which row to display and never derives, changes or overrides a stored status. (3) **Authored edition data** (`backend/data/programme_editions.json`) with source URL + `verified_at` + a note of what each page literally published: RISE 2027 `announced`, Charpak 2026 `closed`, Mitacs 2026 `closed`. Verified-but-left-`expected` because no dates are published: CERN, Outreachy (its two official pages contradict each other), GSoC, Season of KDE, Summer of Bitcoin, SIH; KAUST VSRP states only "all year round" and stays `expected` by founder decision. Several of these sites are JS-rendered and return nothing to a plain fetch — independent support for the §3 no-scrapers ruling. (4) **The seed REJECTS `status: "open"`** from the authored file and writes nothing, validating before its single commit: only a deliberate database action may mark an edition open, never a code path — **including the one that seeds the architect's own content**. Production: 52 programmes, 1 announced / 2 closed / 50 expected, **0 open**. (5) **Part 5a personalisation**: `backend/data/programme_tag_map.json` maps all 49 taxonomy divisions to registry tags (excluding `research` at 39/52, `international` as a location signal and `government` as an organiser signal — broad tags destroy ranking value; 9 divisions map to nothing, and the band then renders nothing rather than faking a match). `GET /programmes?divisions=` **ranks, never filters**, with tests locking both that `total` is unchanged and that a top-ranked programme is still reported `expected` | **Binding** |
| 2026-08-03 | **Vercel free tier is compute-bound, and Next 16 silently ignores `revalidate` on dynamic segments.** The free team hit **4h12m / 4h Fluid Active CPU** (over the limit; projects auto-pause past it, and upgrading is not an option). The quota is team-wide across 7 projects but **aspirova is 99.2%** of it, so pruning other projects saves nothing — unlike the account-wide Render bandwidth case. Root cause: every `ƒ` route invokes a function per request, and the sitemap is **20,793 URLs** (19,195 `/opportunity/[slug]`, 1,536 `/companies/[slug]`, 48 `/programme/[slug]`), so crawling burned CPU continuously. **Ruling / gotcha for all future work:** `export const revalidate` **alone does nothing** for a dynamic segment — verified against `.next/prerender-manifest.json`, where `dynamicRoutes` stayed **empty**. The route must ALSO export `generateStaticParams`; returning `[]` registers it for on-demand ISR while prerendering nothing at build time. Two fixes were tried, measured and **disproved** before this one: removing `export const dynamic = "force-dynamic"` (a no-op — `await searchParams` already forces per-request rendering) and route-level `revalidate` on its own. Production after the fix: `/opportunity/[slug]` and `/programme/[slug]` return `X-Vercel-Cache: HIT` + `Cache-Control: public`, previously `MISS` + `no-store` (PR #38). **Verification note:** Vercel *preview* deployments sit behind deployment protection and return `no-store` regardless, so they cannot be used to test caching — measure production headers or read the prerender manifest. **Still uncacheable:** every route that awaits `searchParams`. Trade-off accepted knowingly: opportunity detail pages may now be up to 1h stale | **Binding** |
| 2026-08-04 | **Programmes Registry Part 7 first tranche LANDED (52 -> 82), and the build order's own premise corrected** (PR #46, merged + CI-green + seeded to production). All nine categories are populated for the first time: **scholarship 0 -> 10** and **conference 0 -> 1**, plus growth in fellowship (4->9), government_internship (8->11), international_research (8->13), open_source (7->9), recurring_competition (5->7) and corporate_research (4->6). **Rulings:** (1) **§9 was wrong that Part 7 unblocks Parts 4 and 5b** — registry expansion adds *programmes*, while those parts need *editions* carrying a verified status and `closes_at`. Production still has **0 `open` editions and exactly 1 with a real `closes_at`**, unchanged by adding 30 programmes. The true unblocker is **edition verification** (the §8 weekly loop), not content volume — so Parts 4/5b stay correctly blocked and are NOT waiting on more registry rows. (2) **Registry content is authored by the architect, never delegated to the implementation agent** — §3 required authored content, this makes the process binding; handing an agent programme descriptions, eligibility and URLs invites invented facts into the one dataset whose whole premise is honesty. The engineering half (validation, seeding, tests) is delegated as normal. (3) **Every `url` is live-checked before commit** — all 30 additions returned HTTP 200 and **9 candidates were dropped rather than guessed** (502/404/unreachable), because a plausible dead URL is worse than an absent programme. (4) **The tag vocabulary is closed**, test-enforced against `programme_tag_map.json` plus five registry-only signals (`research`, `international`, `government`, `competition`, `open-source`); on its first run it caught a real defect — `blockchain` was reachable from no division, so two programmes could never match a CSE student despite the taxonomy having a `blockchain` interest under `cse`. (5) **`typical_window` may contain no 4-digit year**, test-enforced: year-specific facts belong in `programme_editions.json`, and a year baked into the programme row goes stale silently. (6) **`eligibility` may be null, `description`/`typical_window` may not** — 20 entries have no verified eligibility text and inventing it would be the fabrication §6 forbids, whereas the other two are what the directory card renders. Also fixed: 11 programmes from the original 48 had an empty `typical_window`, caught by the new checks. Registry is 82 with the honesty invariant intact (**0 `open`**) | **Binding** |
| 2026-08-04 | **The weekly programme review report could not fire, and is fixed by splitting the human claim from the machine schedule** (PR #49, merged + CI-green). The Monday needs-review job exists to say WHEN to go re-check a programme's official page — the mechanism the 2026-08-04 Part 7 row identified as the true unblocker for Parts 4/5b. Both its scheduling triggers were broken in opposite directions. (1) The **seasonal trigger was nearly blind**: it parsed month names out of the free-text `typical_window`, and only **7 of 82** entries contain a parseable month, because §6's honesty rule means that prose is deliberately hedged rather than asserting unverified months. **Two individually correct decisions combined into a defect** — hedging the prose was right, depending on it for scheduling was not. (2) The **staleness trigger was pure noise**: it flagged every edition with a null `verified_at`, which in production is **78 of 84**, so the report said "78 items need review" and therefore said nothing. **Rulings:** (a) registry entries carry an optional **`review_months`** (ints 1–12) as *internal scheduling metadata* — never rendered, never returned by the API. The asymmetry is the entire justification: a wrong `typical_window` misleads a student, a wrong `review_months` only means we look a month early, so it can carry a schedule the prose honestly cannot. Authored on **67 of 82**. (b) It is read from the registry file at import, following `api/programmes.py`'s tag-map precedent — **no DB column, no migration**; the seasonal trigger prefers it and falls back to the existing prose parsing. (c) **Absent is a valid authoring choice** — 15 entries have none (ad-hoc or rolling Indian government schemes, plus a few with no stable month), because a guessed month fires the report at random and trains the reader to ignore it. (d) **A null `verified_at` is the seeded default, not decayed verification, so it is NOT stale**; staleness now requires a non-null value older than the threshold, and the seasonal trigger brings never-verified rows up at the right time. Measured on production: stale **78 → 0**; seasonal coverage per month **0–5 → 4–44**, concentrated in the real application seasons with April correctly quiet | **Binding** |
| 2026-08-09 | **Vercel free tier exceeded on THREE meters at once; Doc 02 §3.1's "zero infra cost" claim amended.** ISR Writes **434K/200K**, Fluid Active CPU **6h15m/4h**, Fast Origin Transfer **9.82GB/10GB**. **Rulings:** (1) **`renders/day ≈ cacheable paths ÷ revalidate window`.** Every ISR regeneration is a *full server render*, so ONE regeneration bills three meters simultaneously — CPU (the render), an ISR write (storing it), origin transfer (shipping it to the edge). They are not three problems; they are one event counted three ways. (2) **ISR does not reduce render count, it only moves renders into the background** — which is why registering routes for ISR (#38/#39/#42) left the meters untouched. Only widening the window and cutting the path count moved them. (3) **The health metric is the ISR reads:writes ratio.** A working cache is read far more than written; at failure this project was inverted at ~**3:1 writes to reads** (434K vs 145K), which is numerical proof the cache was discarded before it could pay for itself. (4) **Deploy frequency is itself a cost driver** — every production deploy invalidates the whole ISR cache, so each costs ~one full regeneration wave. Three deploys inside 56 minutes produced the worst day on record (43m CPU, 130K writes) *while shipping cost fixes*. Batch frontend releases; and **a day containing a deploy cannot measure a caching change**. (5) **Crawl surface is the root variable**, dialled by `SITEMAP_OPPORTUNITY_LIMIT`, but trimming the sitemap only changes what a crawler DISCOVERS — indexed URLs are re-crawled for weeks, so it is a slow lever, never an emergency one. (6) **The durable fix is on-demand revalidation** (#54): pages invalidate when `content_hash` actually changes in `ingest_one`, never on a routine `last_seen_at` touch, with time-based revalidation kept only as a backstop. Without it, freshness can only be bought with renders. **Shipped:** per-item OG image routes deleted (paths ~44K→~22K; Next does NOT merge `openGraph` from an ancestor when a page returns its own object, so `og:image` had to be set explicitly — a regression caught only in live production), sitemap bounded to 5,000, `revalidate` 86400→604800, on-demand revalidation, and a CI guard (#45) that fails the build if a route loses `generateStaticParams`. **Process lesson:** the diagnosis was wrong twice from reasoning over aggregate charts; the answer was sitting in the ISR observability panel (writes/12h) the whole time. Measure the mechanism, not the symptom | **Binding** |
| 2026-08-14 | **ROOT CAUSE of the Vercel quota incident found: a route's ISR window is the MINIMUM of its `export const revalidate` and the `next.revalidate` of every fetch it renders** (PR #56, merged; Doc 02 §3.1 amended). `/opportunity/[slug]` declared `604800` (7 days) while `lib/api.ts` set `revalidate: 60` on `getOpportunity`, so the real window was **60 seconds across 20,427 pages** — near-total regeneration every crawl pass, and each regeneration bills Fluid CPU + an ISR write + Fast Origin Transfer. **Every earlier fix (#38/#39/#42/#51/#52/#53/#55) was real but marginal, because this dominant term was never touched.** **Rulings:** (1) a fetch `revalidate` is NOT a private data-cache setting — on an ISR route it silently caps the page's cache lifetime, and the four such values now carry comments saying so; (2) **the only trustworthy observable is `Age` vs `X-Vercel-Cache` on a live request** — `STALE` at `Age: 2425` cannot be a 7-day window; before/after was `STALE` within a minute → `HIT` still climbing at `Age: 411`. Dashboards aggregate away the signal; measure the mechanism, not the symptom. (3) **Revalidate only on user-VISIBLE change** — `content_hash` sha256s the whole upstream payload and ATS rows carry volatile fields (`publishedAt`, `secondaryLocations`, `isListed`), so comparing the nine rendered fields instead cut false revalidations from **31% → 12%** of listings per crawl (PR #55). **Process failure recorded deliberately:** every work order in this incident said *"do not change `next: { revalidate: N }` in lib/api.ts"*, fencing off — on an unverified assumption — the exact line defeating the fix. **Do not protect a value you have not proven inert; a guardrail written from a guess is indistinguishable from a bug** | **Binding** |
> Engineers: do not contradict a **Binding** row without an architect-approved amendment.
