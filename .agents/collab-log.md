# Claude <-> Codex collaboration log

Chronological, newest at the bottom. Gists only, no transcripts, no secrets.

## 2026-07-06 — Work order 1 (Claude → Codex)
- Task: fix red CI — /pricing statically prerenders and fetches /plans at build time, ECONNREFUSED with no backend; add a canonical fallback mirroring seed_plans.py + fix the stale ci.yml comment.
- Codex: added typed FALLBACK_PLANS (5 plans, exact seed values) + try/catch around getPlans() in app/pricing/page.tsx; corrected the ci.yml Build comment. Hit the known Windows sandbox CreateProcessAsUserW=5 limit, so it edited but could not self-verify.
- Review: verified pass — Claude ran `pnpm build` with no backend up (succeeds; /pricing stays ○ static, / and /opportunity/[slug] stay ƒ), plus `pnpm lint` and `tsc --noEmit` clean. Committed as sohan1611.

## 2026-07-06 — Work order 2 (Claude → Codex): Phase 3 Part 1 — AI infrastructure (zero-cost foundation)
- Task: build the `ai_client` seam (generate + embed, stub when unkeyed, usage logging), `ai_usage` + `resume_profiles` + `opportunities.embedding` schema (pgvector migration), `ai_budget` daily-cap helper, config, stub-only tests — no key, no spend. Locked: generation `claude-haiku-4-5`, embeddings `text-embedding-3-small` (1536-dim).
- Codex: delivered all of it — `ai_client.py` (deterministic normalized hash stub vectors, lazy key-guarded provider paths, usage logged via a separate session so it can't corrupt the caller's txn), `ai_budget.py`, models, migration `d4b9a2e7c6f1` (down `7c1e5a9f3d6b`), config, `test_ai_client.py` + `test_ai_budget.py`. Self-flagged that `uv.lock` needed regenerating (sandbox can't run `uv lock`).
- Review: verified pass — Claude regenerated `uv.lock` (pgvector 0.4.2); ruff + black clean; full migration chain applies on a `pgvector/pgvector:pg17` container (extension enables, vector round-trips); all 3 new tests pass; confirmed the separate-session usage logging leaks zero rows past the test rollback. Applied the additive migration to production Supabase (user-authorized), full suite green against prod (136), committed as sohan1611, CI green.

## 2026-07-06 — Work order 3 (Claude → Codex): Phase 3 Part 2 — enrichment pipeline
- Task: build `enrich_opportunity` (summary + tags + embedding via ai_client, idempotent) + `enrich_pending` worker (key-guarded so it no-ops in prod until Part 7, budget-aware), clear enrichment on content change in ingest_one, an enrich_worker.py CLI, and stub tests. Not wired into any workflow.
- Codex: delivered all four — enrich.py (defensive tag parsing, idempotency guard, key guard, per-opp budget stop + commit), ingest.py content-change invalidation, enrich_worker.py, test_enrich.py.
- Review: verified pass — ruff clean; black reformatted 2 files (Codex couldn't run it); on a fresh pgvector container all 5 enrich tests pass with ZERO leaked opportunities/tags/ai_usage (per-opp commits are savepoint-contained by the rollback fixture); full suite green against production (141), zero test-row leak in prod. Committed as sohan1611.

## 2026-07-06 — Work order 4 (Claude → Codex): Phase 3 Part 3 (backend) — Resume Match
- Task: HNSW cosine index migration; resume_match.py (save_resume = embed-once-per-version; match_for_user = pure pgvector cosine top-K, ZERO LLM); Pro-gated /resume + /resume/matches endpoints; schemas; tests incl. a zero-generation-usage-on-match assertion. Backend only; frontend + caching + public semantic search deferred.
- Codex: delivered all six files — migration e8c4f1a6b2d9 (HNSW, down d4b9a2e7c6f1), resume_match.py (cosine_distance ordering, NULL-embedding exclusion, score = 1-distance), api/resume.py (can()-gated, 403 for non-Pro), schemas, main.py router, test_resume_match.py (ranking, NULL exclusion, zero-generation proof, versioning/truncation, free-403 + pro-success via dependency_overrides).
- Review: verified pass — ruff clean; black reformatted 2 files; on a pgvector container the HNSW migration applies + all 4 tests pass, ZERO leaked rows (users/resume_profiles/opps/subs/ai_usage) despite the endpoints' db.commit(); applied the HNSW index to production while the embedding column is still empty (instant, zero-risk — prod now at head e8c4f1a6b2d9); full suite green against production (145), zero test-row leak. Committed as sohan1611. Frontend (resume upload + matches surface) is the next Codex work order.

## 2026-07-06 — Work order 5 (Claude → Codex): Phase 3 Part 3 (frontend) — Resume Match UI
- Task: Pro-gated /resume page on the 2.5 design system — paste resume -> POST /resume + GET /resume/matches, ranked matches via OpportunityCard + fit-score badge; signed-out / non-Pro-upsell / empty / error states; header "Matches" link; typed 403 handling.
- Codex: delivered — lib/types.ts (MatchItem), lib/api.ts (uploadResume/getResumeMatches + a typed ProFeatureRequiredError for the 403 branch), app/resume/page.tsx (route + metadata), ResumeMatchPage.tsx (full state machine), a new ui/textarea primitive, AppHeader "Matches" link (+ responsive wordmark/nav), an AuthWidget mobile-overflow fix.
- Review: verified pass — pnpm lint + tsc + build clean; /resume builds as ○ static (client component, no build-time fetch — doesn't repeat the /pricing bug), feed/detail stay ƒ. Live preview: signed-out state renders premium + consistent, header link works, dark-mode foreground token correct, no console errors, no overflow at 375px. Pro/upsell/matches states covered by state-machine review + the backend gating tests (no real Pro Supabase session mintable in preview). Committed as sohan1611. (Note: the frontend push tripped a pre-existing flaky rate-limit test in CI — test_per_ip_keying, a fixed-window timing flake unrelated to the change; green on re-run.)

## 2026-07-06 — Work order 6 (Claude → Codex): Phase 3 Part 4 (backend) — Hidden flag + Reopen prediction
- Task: recompute_hidden (set-based, single-niche-source classification), compute_hidden CLI, expose is_hidden on OpportunityListItem, reopen.py (statistical + curated seed, labeled estimate, no LLM), reopen_estimate on OpportunityDetail, tests. No migration (is_hidden already exists from Phase 1).
- Codex: FIRST run correctly STOPPED and flagged that opportunities.is_hidden already exists (Phase-1 schema + prod) — refused to create a duplicate-column migration (good worker judgment). Re-dispatched without the migration; delivered hidden.py, reopen.py, compute_hidden.py, schema/endpoint edits, test_hidden.py + test_reopen.py.
- Review: verified pass — ruff + black clean (no reformat needed this time); on a pgvector container all 4 tests pass, ZERO leaked rows; full suite green against production (149). No migration/prod-write needed. reopen makes zero ai_usage; recompute_hidden is set-based + idempotent. Committed as sohan1611. compute_hidden is a worker not yet wired into a workflow (populates is_hidden when run — an ops step); the Hidden-gem frontend badge is a small follow-up (folded into Part 6 frontend).

## 2026-07-06 — Work order 7 (Claude → Codex): Phase 3 Part 5 — Weekly Career Report
- Task: templated weekly report (dream matches + closing-soon + new hidden opps) from precomputed data; at most one cohort-cached, key-guarded LLM intro; gated via can(weekly_report); deduped per ISO week via Notification; delivered via the Resend seam; worker --weekly flag; tests (must not send real email).
- Codex: delivered weekly_report.py (full HTML+text templated body, source-linked; _cohort_intro caches the fallback first so a provider error can't retry per-user; budget-aware; grounded prompt "do not invent opportunities"), notification_worker --weekly, test_weekly_report.py (monkeypatches email_client.send_email to capture — no real send).
- Review: verified pass — ruff clean; black reformatted 1 file; model fields (Opportunity.status/first_seen_at, Notification.type/status/sent_at/meta) all confirmed to exist; on a pgvector container all 3 tests pass (gating+content, ISO-week dedup, at-most-one-shared-intro across a multi-user keyed run), ZERO leaked rows despite per-delivery commits; full suite green against production (152). No new scheduled workflow wired (ops step, no Pro users yet). Committed as sohan1611.

## 2026-07-06 — Work order 8 (Claude → Codex): Phase 3 Part 6 (backend) — Career Copilot
- Task: the one Shape-C feature — Pro-gated + hard daily per-user rate limit + key-guard + budget degradation + RAG-over-pgvector (public opps only, no user-private data so answers are cacheable) + shared-answer cache + grounded/no-fabrication. Stub-safe. Tests for all controls.
- Codex: delivered pipeline/copilot.py (retrieve_context cosine top-K, grounded answer_copilot with cache-first -> key-guard -> budget -> generate; the system prompt even adds prompt-injection defense treating opportunity text as untrusted data + defensive cache-payload validation), api/copilot.py (daily rate-limit dependency + can(copilot) 403), schemas (message min/max length), config (per-day limit + cache TTL), main.py router, test_copilot.py.
- Review: verified pass — ruff clean; black reformatted 2 files; on a pgvector container all 7 tests pass (free-403/pro-allowed, key-guard zero-usage, grounded-in-cosine-top-K, cache one-generation-for-repeat, daily 429, over-budget degradation, blank/oversized 422), ZERO leaked rows; full suite green against production (159). Committed as sohan1611. Copilot chat UI + the Part-4 Hidden-gem badge/reopen line are the next (frontend) work order.

## 2026-07-06 — Work order 9 (Claude → Codex): Phase 3 Part 6 (frontend) — Copilot chat UI + Part-4 signals
- Task: Pro-gated /copilot chat on the 2.5 design system (signed-out/upsell/pro states, transcript, sources, 429 rate-limit + degraded handling); surface is_hidden as a "Hidden gem" badge on OpportunityCard + reopen_estimate as a labeled line on the detail page; header Copilot link.
- Codex: delivered CopilotChat.tsx (accessible chat — aria-live transcript, Enter-to-send + Shift+Enter + IME guard, suggested-prompt empty state, thinking loader, degraded turns muted, sources as opportunity links, 403->upsell, 429->rate-limited state), app/copilot route, lib/api askCopilot + typed RateLimitedError, types (CopilotResponse/Source, is_hidden, reopen_estimate), OpportunityCard "Hidden gem" badge, detail-page reopen line, header Copilot link.
- Review: verified pass — pnpm lint + tsc + build clean; /copilot builds ○ static, feed/detail stay ƒ; live preview: signed-out Copilot state premium + consistent, nav link works, dark-mode token correct, no console errors, no 375px overflow, feed still renders. Minor copy nit flagged (the Copilot intro says "and your profile" but the backend deliberately excludes user-private data — offered to tidy). Committed as sohan1611. Phase 3 build (Parts 1-6) complete; only Part 7 (real keys / go-live) remains.

## 2026-07-06 — Work order 10 (Claude → Codex): two small fixes
- Task: (1) kill the flaky rate-limit test (test_per_ip_keying, intermittent "200 == 429" in CI) — a FixedWindow boundary race; (2) remove the overstated Copilot "and your profile" copy (backend uses no user-private data).
- Codex: (1) added an autouse fixture in test_rate_limit_and_cache.py freezing upstash_ratelimit.limiter.now_ms to a constant so every request shares one window bucket (test-only; production limiter untouched — the exact now_ms()//window seam Claude identified); (2) reworded both "and your profile" lines in CopilotChat.tsx to "grounded in Aspirova's opportunities."
- Review: verified pass — ruff + black clean; rate-limit test run 10x = 10/10 (determinism confirmed, flake dead); frontend lint + tsc clean; "and your profile" no longer present. Committed as sohan1611.

## 2026-07-06 — Work order 11 (Claude → Codex): operationalize the Phase-3 workers in GitHub Actions
- Task: wire the built-but-unwired workers into GH Actions (no keys needed to land; AI ones activate at go-live). compute_hidden into the crawl; a dispatch-only enrich workflow (dry-run-first); a weekly-report cron (gated + key-guarded).
- Codex: added a "Recompute hidden opportunities" step to crawl-tier1.yml (DATABASE_URL only); enrich.yml (workflow_dispatch only, limit input, commented deferred schedule with the dry-run note, ANTHROPIC/OPENAI secrets, concurrency); weekly-report.yml (Mon 08:00 UTC + dispatch, RESEND/ANTHROPIC secrets, concurrency).
- Review: verified pass — all three YAML files parse; ran compute_hidden against production (pure SQL, no cost) = 0 hidden (correct: all four sources are Tier-1, so nothing qualifies until niche tier>1 sources exist). Pushing doesn't trigger any of these (schedule/dispatch, not on push). Committed as sohan1611.
- Product finding surfaced to founder: the Hidden-gem feature has no qualifying data until the source mix grows into Tier-2/3 long-tail. Post-go-live direction chosen by founder: Phase 4 (growth loops).

## 2026-07-06 — Work order 12 (Claude → Codex): Phase 4 Part 1 — SEO content engine
- Task: JobPosting JSON-LD on the detail page (backed fields only; validThrough on explicit deadlines only), dynamic sitemap.xml (all active slugs + static routes, resilient fallback), robots.txt, canonical URLs, a cached backend /sitemap-opportunities endpoint + test. Key-free, payment-free, no AI.
- Codex: delivered api/sitemap.py (lean slug+last_seen_at query, active only) + test_sitemap.py; middleware/main wiring (cacheable + rate-limited); sitemap.ts (try/catch -> static-routes fallback, revalidate 300); robots.ts; and JobPosting JSON-LD in opportunity/[slug]/page.tsx (buildJobPostingJsonLd emits only present fields, validThrough gated on explicit deadline per Doc 01 R5, escapes < to < against script injection from crawled text, canonical alternates).
- Review: verified pass — ruff clean; black reformatted 1 file; frontend lint+tsc+build clean WITH NO BACKEND (sitemap.xml + robots.txt now ○ static; /opportunity/[slug] + / stay ƒ — no /pricing-style break); live preview: robots.txt correct, sitemap.xml = 3,050 URLs (4 static + 3,046 active opps), detail page carries a valid JobPosting with validThrough correctly omitted when no explicit deadline; full suite green against production (160). Committed as sohan1611.

## 2026-07-06 — Work order 13 (Claude → Codex): Phase 4 Part 1b — long-tail SEO landing pages
- Task: SSR /internships, /remote, /jobs (reuse feed API) + /companies/[slug] (new backend /company/{slug} + /sitemap-companies) + sitemap entries + footer links. Key-free, indexable, ƒ dynamic.
- Codex: delivered api/company.py (window-function total, active opps, 404) + /sitemap-companies (only companies with active opps) + middleware/main wiring + extended test_sitemap; a shared OpportunityLandingPage renderer reused by /internships /remote /jobs; app/companies/[slug]/page.tsx with generateMetadata + notFound; getCompanyPage + CompanyPage type; pagination.ts basePath support; sitemap.ts adds the new URLs (guarded); AppFooter internal links.
- Review: verified pass — ruff + black clean; frontend lint+tsc+build clean WITH NO BACKEND (all four new pages ƒ dynamic, existing routes unchanged); live preview: /internships (H1 "Internships") + /companies/discord (H1 "Opportunities at Discord") render real lists, a nonexistent company returns a proper 404, sitemap.xml now includes /internships + /remote + 126 company URLs; full suite green vs production (163). Committed as sohan1611.

## 2026-07-07 — Work order 14 (Claude → Codex): Phase 4 Part 1c — dynamic OG / social images
- Task: branded 1200x630 next/og ImageResponse cards so shared links render rich previews — root brand card (app/opengraph-image.tsx), opportunity card (title + company/location/remote), company card ("Opportunities at {co}" + open-role count); each dynamic route falls back to the brand card on any error/missing data so a backend-less build never breaks; layout.tsx twitter card summary -> summary_large_image. Key-free, payment-free, no AI.
- Codex: delivered all three routes + the layout tweak. First pass failed eslint react-hooks/error-boundaries ("Avoid constructing JSX within try/catch") because the ImageResponse JSX was built inside the try. Corrective work order 14b: restructured so the try/catch computes only nullable card DATA and the JSX/ImageResponse is built once afterward (brand fallback when data is null). On this pass Codex hit sandbox write-denials on the bracketed [slug] route files and used a guarded rename to apply the edits, leaving two undeletable backups it parked under ignored .next/codex-backups (Claude removed them; nothing under app/).
- Review: verified pass — pnpm lint + tsc + build all clean (exit 0); all three OG routes return 200 image/png with valid PNG magic bytes; with no backend every route correctly falls back to the identical brand card. Pointed the dev server at production and confirmed the real-data paths render: /opportunity/…/opengraph-image → "Agency Core 1 / Caterpillar Inc. - Peterlee, - Remote / View on Aspirova", /companies/airbnb/opengraph-image → "Opportunities at Airbnb / 277 open roles, auto-discovered", root → brand card ("Every opportunity. One place."). Reverted .env.local. Committed as sohan1611.
- Minor data nit surfaced (not a code bug): some crawled location strings carry a trailing comma (e.g. "Peterlee,"), so the OG secondary line can read "… - Peterlee, - Remote". Cosmetic, source-data side — flagged for a future cleanup, not blocking.

## 2026-07-07 — Work order 15 (Claude → Codex): Phase 4 Part 2a (backend) — referral / invite loop
- Task: build the referral loop backend to the architect spec (docs/handoffs/PHASE-4-PART-2-SPEC.md). Additive users.invite_code migration; core/referral.py (code gen/resolve/record_referral/grant_comp_pro); GET /referral/me + POST /referral/claim; a surgical comp-sub expiry guard in the single gating seam (Ruling B); tests incl. reward + idempotency + zero row-leak + three gating-expiry cases. Reward = bounded 30-day comp pro_lite_monthly (non-AI Pro only, cost-safe at go-live).
- Codex: delivered all of it — migration f2a9c7d4e1b0 (nullable-unique invite_code, down_revision e8c4f1a6b2d9, reversible), core/referral.py (8-char unambiguous base32 with IntegrityError-retry, case-insensitive resolve, record_referral guarded by self-referral + set-once + a with_for_update row lock, atomic set referred_by + grant + commit), api/referral.py + schemas + main.py wiring (/referral/me returns invite_code only — no hardcoded web origin, per §4 — plus referral_count + reward_active_until; /referral/claim returns {referred, reason}), the Ruling B or_() guard in core/gating.py leaving the Razorpay branch untouched, test_referral.py (7) + test_gating.py additions (3). Hit the usual Windows sandbox process-spawn block, so edited-but-couldn't-self-verify.
- Review: verified pass — ruff clean; black reformatted 3 files (Codex can't run it); on a throwaway pgvector/pgvector:pg17 container the full chain applies AND the new migration reverts down and re-applies cleanly; the 20 referral+gating tests all pass, including the three Ruling B cases (expired comp → no grant, future comp → grants, Razorpay-with-past-period → still grants) and the savepoint leak-containment test (zero leaked users/subscriptions). Full-suite container run = 168 passed / 5 failed, where the 5 are exclusively test_api.py's real-data tests (they need the 2,256+ crawled opps that only exist in prod — they pass in CI against production, unrelated to this change). Committed on branch phase-4/part-2a-referral-backend as sohan1611. NOT pushed/merged — landing awaits founder authorization of the additive users.invite_code prod migration (CI runs backend tests against prod, so the column must exist there first).

## 2026-07-07 — Work order 16 (Claude → Codex): Phase 4 Part 2b (frontend) — referral / invite UI
- Task: build the referral frontend on the 2.5 design system to the architect spec §3 — a silent global ?ref= capture component (first-touch localStorage, auto-claim once when a session appears), a client-static /referral invite page (link + copy + count + reward status), lib/api.ts + lib/types.ts wiring, and an Invite header link. Frontend only, backend contract fixed by Part 2a.
- Codex: delivered ReferralCapture.tsx (stash-once + claimStartedRef guard + clear-in-finally, all errors swallowed), ReferralPage.tsx (ResumeMatchPage-style state machine: signed-out/checking/error-retry/content, copy with clipboard→execCommand fallback, singular/plural count, formatted reward date), app/referral/page.tsx (+metadata), AppHeader Invite link, layout.tsx mounting ReferralCapture in a Suspense boundary, api/types additions. Deferred the optional opportunity-detail share control (allowed by the order). Couldn't self-verify (sandbox process block).
- Corrective round (16b): first pass failed eslint react-hooks/set-state-in-effect twice in ReferralPage — setState synchronously in the fetch effect body, and a setOrigin useEffect. Fix spec: mirror ResumeMatchPage (no synchronous setState in the effect; move the retry reset into the onRetry event handler) and read window.location.origin via the repo's existing useHydrated() hook (useSyncExternalStore) instead of a state+effect. Codex applied both cleanly. (Note: the dispatch was briefly blocked by a transient Opus-classifier outage that gates PowerShell; retried and it went through — no direct-edit needed, delegation held.)
- Review: verified pass — pnpm lint + tsc + build all clean (exit 0); /referral builds as ○ client-static (no build-time fetch — no /pricing-style break), every existing route unchanged. Live preview: signed-out /referral renders the sign-in card, the Invite header link is present, ?ref=TESTCODE9 is captured to localStorage AND a second visit with a different ref does NOT overwrite it (correct first-touch attribution), zero console errors. Signed-in reward/copy states aren't previewable (no real Pro Supabase session) — covered by review + the Part 2a backend tests. Committed on branch phase-4/part-2b-referral-frontend as sohan1611. Held (with 2a) for the founder's users.invite_code prod-migration authorization before the whole loop merges + deploys.

## 2026-07-07 — Work order 17 (Claude → Codex): Phase 4 Part 2c — opportunity Share button
- Task: the deferred optional piece of Part 2 — a client ShareButton beside BookmarkButton on the opportunity detail page that copies the opportunity URL with the signed-in user's ?ref= appended (plain URL signed out / on referral-fetch failure), so shares carry the referrer's code and land on the Part 1c OG preview card. Frontend only, no backend/migration; reuse the existing getReferralMe.
- Codex: delivered components/ShareButton.tsx (origin via useHydrated, per-access-token cached invite code, clipboard→execCommand→hidden-input select fallback ladder with toasts, plain-URL fallback when signed out or the referral fetch fails) + wired <ShareButton slug=.../> into the detail action row right after BookmarkButton. Detail page stays a server component; ShareButton is the client island. Couldn't self-verify (sandbox process block).
- Review: verified pass — pnpm lint + tsc + build all clean; /opportunity/[slug] stays ƒ (SSR intact — the client island didn't regress it). Live preview against production data: the Share button renders beside Bookmark on a real detail page; clicking it while signed out copies exactly the plain opportunity URL with NO ?ref (correct), degrading to the select-fallback + a "Link selected" toast in the automated preview (a real user gesture in a secure context gets navigator.clipboard + "Link copied"); zero console errors. Signed-in ?ref-append path covered by review (no real Pro session mintable in preview). Committed on branch phase-4/part-2c-share-button as sohan1611.

## 2026-07-07 — Work order 18 (Claude → Codex): source expansion — SmartRecruiters ATS adapter
- Task: add a 4th ATS source via the SourceAdapter seam (canonical "adding a source = adding an adapter", pipeline unchanged). Architect verified the SmartRecruiters public API live, captured a real 3-posting fixture (Wise/WesternDigital/Visa), and confirmed the one structural quirk: the description lives only on the per-posting DETAIL endpoint (the list has none). Deliverables: crawlers/smartrecruiters.py (paginated list + per-posting detail fetch → complete RawListing so parse stays pure; health mapping; location comma-cleanup; section concat + HTML strip; tz-aware releasedDate), runner ATS_ADAPTERS registry entry, seed_companies (Wise 377 / WesternDigital 277 / UMG / Visa; Bosch 4667 + DeliveryHero 1110 held to bound crawl time), tests against the fixture. No migration, no workflow change (run_tier(1) auto-discovers).
- Codex: delivered all of it defensively (paginate by offset to totalFound; a failed detail skips that posting + marks degraded, never raises; 404 board → broken, 429/non-200/request-error → degraded). Corrective round 18b: architect's live container crawl found Visa's SR postings are thin syndication shells (one EEO line, one EMPTY description) while UMG returned 4149 chars — so Codex dropped Visa from the seed (Doc 01 R5), keeping the three full-description boards. Both rounds edited-but-couldn't-self-verify (sandbox process block; the 18b patch applied directly after the block).
- Review: verified pass — ruff + black clean; 10 new adapter parse-tests pass (identity, health, field mapping, multi-section HTML-stripped description, remote True/False, category, location comma-cleanup, tz-aware date); full suite on a pgvector container = 178 passed / only the 5 known real-data test_api tests fail on the empty DB. END-TO-END proof: seeded only SmartRecruiters into the container and ran a REAL crawl — it fetched live and ingested real opportunities (Visa 2, UMG 1) with clean locations ("Austin, TX, United States"), real jobs.smartrecruiters.com apply URLs, stripped HTML, and parsed posted_at. Committed on branch phase-4/source-expansion-smartrecruiters as sohan1611. Code lands dormant; activation = running scripts.seed_companies against prod (adds the SR source + 3 boards) so the scheduled Tier-1 crawl ingests ~650 new opportunities — an authorized prod step.

## 2026-07-07 — Work order 19 (Claude → Codex): ops — raise Tier-1 crawl timeout
- Task: after activating SmartRecruiters in prod, the first real Tier-1 crawl ran 24m44s against the 25-min timeout (16s margin). Root cause: the SR adapter fetches a per-posting DETAIL endpoint for every listing inside fetch(), and the board fingerprint is computed after fetch() — so ~654 detail calls (Wise 377 + WD 277) run EVERY crawl (~18 min), regardless of change. That thin margin would eventually kill the whole Tier-1 crawl (all sources). Stopgap: raise .github/workflows/crawl-tier1.yml timeout-minutes 25 -> 40 with an explanatory comment.
- Codex: applied the one-line change + rationale comment; nothing else touched.
- Review: verified pass — YAML structure intact, exactly one timeout-minutes (40), diff limited to that block. Committed + merged + pushed as sohan1611.
- Follow-up flagged (background task): make the SR adapter fetch detail lazily — list-only fetch() drives the fingerprint, detail fetched only for new/changed postings — so unchanged boards cost ~0 detail calls, the timeout can drop back to 25, the crawler is politer to SR's API, and the big held-out boards (BoschGroup 4667, DeliveryHero 1110) become addable.

RESULT (source expansion, live): SmartRecruiters seeded to production and the Tier-1 crawl ingested 598 new opportunities (Wise 375, Western Digital 222, UMG 1; 0 errors). Production feed total 3050 -> 3669; /company/Wise = 375, /company/WesternDigital = 222, all with clean locations, real apply URLs, and stripped-HTML descriptions.

## 2026-07-07 — Work order 20 (Claude → Codex): fix red master (SmartRecruiters slug casing) + test hardening
- Trigger: after seeding SmartRecruiters to prod, master CI went red on two backend tests. Diagnosis: (1) REAL bug — seed_companies sets company_slug = board_token, and SR identifiers are capitalized ("Wise"/"WesternDigital"/"UniversalMusicGroup"), so those 3 companies got capitalized slugs (every other company is lowercase), breaking test_sitemap's "slugs are sorted" invariant; (2) FLAKE — test_runner_isolation derives opportunity slugs from a FIXED company name, so two overlapping CI jobs (caused by two rapid pushes: the timeout bump + the docs commit) generated identical deterministic opp slugs and collided on opportunities_slug_key. Scoping query confirmed ZERO leaked residue in prod (0 "Fake Job" opps) — purely a concurrency race, not my SR code.
- Codex: applied both edits (seed company_slug = board_token.lower(); test company name unique per run) but its apply_patch mis-indented BOTH files (it couldn't read them — Select-String hit the sandbox block), leaving seed_companies at an 8-space IndentationError. Claude fixed the indentation directly (the sanctioned "Codex can't get there" case) in both files.
- Review: verified pass — ruff+black clean, both files AST-parse; on a pgvector container the seed is idempotent (re-run = 0 created, no dupes) and produces lowercase SR slugs while ats_board_id keeps the exact provider identifier ("Wise" etc.); test_runner_isolation + test_sitemap both pass. Prod corrective UPDATE (founder-authorized): lowercased the 3 existing SR company slugs (ats_board_id + the 598 opps untouched; opps link by id and their own slugs were already lowercase) — 0 capitalized slugs remain anywhere. Committed as sohan1611; single push to avoid re-triggering the concurrency race.

## 2026-07-07 — Work order 21 (Claude → Codex): companies directory (/companies index + endpoint)
- Task: the catalog is now ~138 companies / 3,669 active opps but there was no browsable index — only per-company detail pages. Add a directory. Backend: GET /companies -> [{slug, name, active_count}] (one GROUP BY over companies with active opps, ordered by count desc then name), cached + IP-rate-limited via the existing middleware seams (/companies is a distinct prefix from /company/). Frontend: an SSR indexable /companies page (resilient empty fallback), sitemap + footer link. Explicitly scoped OFF the files two in-flight background sessions are touching (crawler/SR, location display).
- Codex: delivered all of it — the endpoint (+ CompanyListItem schema + middleware prefixes), test_company_list, and the frontend page/api/types/sitemap/footer. Stayed within the listed files. Couldn't self-verify (sandbox process block).
- Review: verified pass — backend ruff clean, black reformatted 1 file; the new test + the sitemap tests pass on a pgvector container; frontend lint+tsc+build clean with NO backend (/companies is ƒ SSR; existing routes unchanged). Real-data proof: ran the backend locally against the production DB and pointed the frontend at it — GET /companies returns 138 real companies ordered by open-role count (Stripe 543, Wise 376, Brex 280, Airbnb 279, ...), and the rendered directory shows all 138 with correct lowercase-slug links (Wise -> /companies/wise), the footer Companies link, and zero console errors. Committed on branch phase-4/companies-directory as sohan1611.

## 2026-07-07 — Work order 22 (Claude → Codex): AI cost-safety + embeddings-only backfill (go-live prep)
- Context: founder loaded $5 Anthropic + $5 OpenAI; real budget ~$0.45/day (₹950–1500/mo). Two problems found while prepping AI go-live: (1) the Phase-3 $2/day cap was a NO-OP — ai_client recorded est_cost=0.0 everywhere and the budget guard sums est_cost, so it never fired; (2) a full Haiku summary/tags backfill of 3,672 opps ≈ $16 (avg ~6,000-char descriptions sent to Haiku twice), 3× a month's budget.
- Task: make the cap real + tighten it (est_cost from env-driven per-MTok rates, cap 2.0→0.45), and add an embeddings-ONLY backfill (skip Haiku) to unlock Resume Match cheaply.
- Codex: delivered config rates + cap, ai_client _generation_cost/_embedding_cost on the keyed success paths (stub/error paths stay 0.0), pipeline.backfill_embeddings (OpenAI-key-guarded, budget-aware, 28k-char truncation, per-row commit), scripts/embed_backfill.py CLI, and tests. One test bug (numpy array `or []`) Claude fixed directly.
- Review + run: 12 AI tests pass on a pgvector container (incl. the cap now firing at threshold). Found + fixed a real go-live gap: the openai + anthropic SDKs were never installed (ai_client imports them only inside the keyed paths, which were dormant) — added via `uv add` (openai 2.44.0, anthropic 0.116.0). Calibration against production (real OpenAI key, 20 opps): $0.00057, per-opp $0.000028 → full backfill ≈ $0.10. The full-catalog --limit 4000 select failed (e3q8, too many heavy rows at once over the Mumbai link) → ran it in resumable --limit 500 chunks. Committed as sohan1611.
- Cost principle (founder): high per-use AI (Haiku enrichment/summaries, Copilot) stays OFF until paying users — then funded by subscription revenue. Embeddings (Resume Match) are near-free so they're fine to run now.

## 2026-07-08 — Work order 23 (Claude → Codex): Razorpay webhook hardening (go-live prep)
- Context: founder added Razorpay TEST-mode keys to .env. Claude ran scripts.setup_razorpay_plans → created the 4 test-mode Razorpay plans (Pro-Lite/Pro × monthly/annual) and linked razorpay_plan_id onto the local plan rows; verified checkout plumbing by creating + cancelling a real test subscription (sub created, status 'created', cancelled cleanly). Then the Phase-2-flagged webhook out-of-order/replay hardening.
- Task: api/payments.py — guard against Razorpay redelivery/reordering: a terminal-status guard (once 'canceled', a replayed/late 'charged'/'activated' must NOT resurrect it) + monotonic current_period_end (never rewind). Plus tests.
- Codex: added _TERMINAL_STATUSES = {'canceled'}; the webhook returns 'ignored_terminal' if the local sub is already terminal (before applying any status), and only advances current_period_end when the incoming current_end is newer (null treated as older). Tests for terminal-replay + monotonic period end + kept the existing coverage.
- Review: verified pass — ruff + black clean; on a pgvector container all 8 payments-webhook tests pass (incl. the 2 new hardening cases). Committed on branch razorpay/webhook-hardening as sohan1611. Backend payment→Pro flow now fully proven (checkout creates a real test sub; webhook sets active with replay/reorder safety; can() unlocks Pro). Next: frontend /pricing checkout UI, gated behind a payments-enabled flag so the live site stays a waitlist until go-live; then founder configures the webhook + adds Razorpay keys to Render + completes KYC.

## 2026-07-08 — Work order 24 (Claude → Codex): Razorpay checkout UI on /pricing (flag-gated)
- Task: wire real Razorpay Checkout into /pricing, gated behind NEXT_PUBLIC_PAYMENTS_ENABLED so the LIVE public site stays a waitlist until the founder flips it at go-live (don't show a test-mode checkout to real visitors). Frontend only; the backend POST /payments/checkout is already built + verified.
- Codex: delivered lib/api.ts createCheckout(), types/razorpay.d.ts (typed window.Razorpay, no any), components/SubscribeButton.tsx (signed-out → HeaderAuth "Sign in to subscribe"; signed-in → "Upgrade to {plan}" → createCheckout → Razorpay Checkout with the subscription_id + a success toast; 503 → "Payments aren't available yet"), HeaderAuth optional trigger props (backward-compatible defaults), PricingPlans paymentsEnabled prop (Free → Included; enabled → SubscribeButton; else → WaitlistForm), and app/pricing/page.tsx gating the next/script Razorpay load + passing the flag.
- Review: verified pass — pnpm lint + tsc + build clean; /pricing stays ○ static, no build-time backend fetch introduced. Live preview both ways: flag OFF (production default) → waitlist only, NO subscribe button, NO Razorpay script (live site untouched); flag ON → Razorpay Checkout.js loads + signed-out visitors see "Sign in to subscribe", waitlist replaced. Committed on branch razorpay/pricing-checkout as sohan1611. The actual test-card payment through the Razorpay modal is the founder's manual test in test mode.
- Remaining founder go-live steps: configure the Razorpay webhook (Dashboard → Webhooks → https://aspirova-api.onrender.com/payments/webhook, subscription.* events, copy secret) → add RAZORPAY_KEY_ID/SECRET/WEBHOOK_SECRET to Render → complete Razorpay KYC → set NEXT_PUBLIC_PAYMENTS_ENABLED=true on Vercel → test-card end-to-end.

## 2026-07-08 — Work order 25 (Claude → Codex): fix broken email sign-up confirmation
- Bug (founder-reported): after sign-up, the Supabase confirmation email link errored / did nothing, so users couldn't confirm + sign in. Root cause: lib/supabase/client.ts used @supabase/ssr createBrowserClient, which defaults to the PKCE flow — that returns a ?code= needing a server/callback exchange, but this app is client-only auth with no /auth/callback route, so confirmation never completes (and PKCE also breaks cross-device confirm). Fix: implicit flow (token in URL hash, auto-detected client-side by detectSessionInUrl) — no callback route needed.
- Codex: switched createClient() to `createClient` from @supabase/supabase-js (implicit-flow default) after finding @supabase/ssr@0.12.0 force-overrides auth.flowType back to pkce; kept the same wrapper signature so no other file changed. AuthWidget signUp now passes options.emailRedirectTo = window.location.origin (browser-guarded) so the confirmation returns to whichever origin the user signed up on.
- Review: verified pass — pnpm lint + tsc + build clean; @supabase/supabase-js is already a direct dep (^2.108.2). Verified the actual mechanism live: minted a real session for a throwaway confirmed user, navigated the preview to /#access_token=…&refresh_token=…&type=signup (exactly what the email link produces) — on the full page load the new client detected + stored the session (sb-…-auth-token in localStorage, correct email, hash cleared) and the signed-out "Sign in" CTA disappeared. Deleted the throwaway user. Committed on branch fix/auth-email-confirmation as sohan1611.
- FOUNDER dashboard step still required (not code): Supabase → Authentication → URL Configuration → set Site URL to the live site and allow-list the redirect origins (https://aspirova.vercel.app, https://www.aspirova.org, http://localhost:3000). The localhost Site URL is what made the link "show an error." Also ensure "Confirm email" is on/off per preference.

## 2026-07-08 — Work order 26 (Claude → Codex): social login (Google / Microsoft / LinkedIn / GitHub)
- Founder request: add OAuth social login. Bonus: OAuth sidesteps the flaky Supabase built-in confirmation email entirely (no email round-trip).
- Codex (frontend only): added a social-login section to AuthWidget above the email form — 4 full-width outline buttons (Continue with Google/Microsoft/LinkedIn/GitHub) with inline brand SVG glyphs (aria-hidden), a typed `OAuthProvider` union ("google" | "azure" | "linkedin_oidc" | "github" — exact Supabase slugs), per-button loading spinner, buttons disabled during any in-flight auth, errors surfaced via the existing formError, and an "or continue with email" hairline divider. handleOAuthSignIn calls `supabase.auth.signInWithOAuth({ provider, options: { redirectTo: window.location.origin } })`; the implicit-flow client picks up the returned session (no callback route). Email/password flow, signin/signup toggle, "Check your email" + signed-in states unchanged.
- Review: verified pass — pnpm lint (0) + tsc (0 diagnostics) + next build all clean; reviewed the JSX (grid of 4 buttons + divider + email form, correct slugs, a11y). Provider round-trip itself can't be tested locally — needs each provider enabled in the Supabase dashboard. Committed on branch feat/oauth-social-login as sohan1611.
- FOUNDER dashboard steps still required (not code): for each provider, Supabase → Authentication → Providers → enable + paste Client ID/Secret from that provider's OAuth app; each provider's app must allow-list Supabase's callback https://kvtuxbvuynjlehktutmc.supabase.co/auth/v1/callback. Google=Google Cloud Console, GitHub=Settings→Developer settings→OAuth Apps, Microsoft=Azure App registrations, LinkedIn=LinkedIn Developers ("Sign In with LinkedIn using OpenID Connect"). Redirect allowlist from WO 25 also applies.

## 2026-07-08 — Work order 27 (Claude → Codex): clean malformed location strings (schema-level)
- Bug (found in the comprehensive sweep): ~42% of feed items had messy `location` values from the crawler — trailing/leading commas ("Montego Bay, ", "Victoria,"), empty segments, and duplicated segments ("Gurgaon, Gurgaon, Haryana, India"). Chose a pure read-layer fix (no DB mutation, fully reversible).
- Codex (backend only): added one `field_validator("location")` on `OpportunityListItem` (inherited by `OpportunityDetail`, so both list + detail + every response built from them — feed/search/company/OG — are cleaned automatically, no from_model change). Logic: split on comma, strip, drop empties, order-preserving case-insensitive de-dup, rejoin ", ", empty -> None. Added tests/test_schemas_location.py (reflection-based model builder + parametrized dirty-input cases, incl. detail inheritance).
- Review: verified pass — ruff + black clean; the 8 new location tests pass; validator matches spec ("Gurgaon, Gurgaon, Haryana, India" -> "Gurgaon, Haryana, India", "Montego Bay, " -> "Montego Bay"). Full suite: 187 passed; the 10 failing tests are pre-existing empty-container/seed-data failures (confirmed identical on stashed master — they pass in CI against the real prod DB), unrelated to this change. Committed on branch fix/location-cleanup as sohan1611.

## 2026-07-08 — Fix (Claude, direct): /pricing memory-leak / PC hang — Supabase client singleton
- Founder-reported: after login, opening /pricing hung the PC with memory spiking to ~90% (back to normal on closing the tab). Production-severity regression.
- Root cause: work order 25's email fix swapped lib/supabase/client.ts from @supabase/ssr createBrowserClient (which memoises/singletons the browser client) to raw @supabase/supabase-js createClient, which builds a NEW client on every call. /pricing with NEXT_PUBLIC_PAYMENTS_ENABLED=true renders a SubscribeButton per Pro tier, each calling useSession() → each constructing its own client (+ the nav's). Logged-in, every instance runs its own autoRefreshToken timer + storage listeners on the SAME localStorage auth key → multi-GoTrueClient storage-event storm → runaway CPU/memory. Latent until now because payments were flag-gated off (waitlist form, no useSession) and logged-out clients have nothing to refresh.
- Fix: made createClient() a lazy module-level singleton (one browser client per tab), restoring the single-instance behaviour @supabase/ssr provided while keeping the implicit-flow email fix. Frontend only, one file.
- Done directly by Claude (not Codex) given severity + the fix being a 6-line singleton. Verified: pnpm lint + tsc + build clean; reproduced locally with the flag temporarily on — /pricing renders stable (no "Multiple GoTrueClient instances detected" warning, Razorpay checkout.js loads once, only a couple of auth token refreshes total, no request loop, DOM stable). Reverted the temp local flag. Committed on branch fix/supabase-client-singleton as sohan1611.

## 2026-07-08 — Work order 28 (Claude → Codex): premium rebrand + logo + trim OAuth buttons
- Founder request: use the new app logo as the header brand, make the whole UI feel premium and match the logo, remove the not-yet-working Microsoft + LinkedIn login buttons, (checkout "couldn't start" handled separately — it was CORS on Render, not code).
- Architect derived the palette straight from the logo image (sampled frontend/app/icon.png with PIL): plum ink #60294e + sage #dfdfd2 → a heritage/editorial system: warm cream paper, plum primary, sage surfaces, Fraunces serif display. Gave Codex exact :root/.dark hex token blocks + a plum brand-* scale so the change was mechanical.
- Codex (frontend only): globals.css — swapped blue brand scale + all :root/.dark semantic tokens to the plum/cream/sage set (both themes), added --font-serif + an h1,h2 serif base rule. layout.tsx — wired Fraunces via next/font. Wordmark.tsx — replaced the geometric SVG with <img src="/logo.png"> (rounded, ring-border) + serif wordmark; staged public/logo.png (Claude copied the binary — Codex can't). AuthWidget.tsx — SOCIAL_PROVIDERS + OAuthProvider narrowed to google/github only, removed MicrosoftIcon/LinkedInIcon.
- Review: verified pass — pnpm lint (0 errors; the one <img> warning silenced with an eslint-disable + rationale), tsc clean, build clean. Visually verified in a preview (had to clear .next — Turbopack served stale CSS twice; after clearing, computed --primary=#60294e light / #d290b3 dark): feed + pricing render premium in BOTH light (cream+plum) and dark (aubergine+mauve), logo + Fraunces headings correct, auth shows only Google+GitHub. Committed on branch feat/premium-rebrand as sohan1611. NOT yet pushed — awaiting founder sign-off on the look.

## 2026-07-08 — Work order 29 (Claude → Codex): spacious desktop grid + beige-cream palette
- Founder: the site "feels like a mobile site" (narrow max-w-3xl centered stack) and the dark theme read as "maroonish"; wants a spacious desktop grid where cards sit side by side, with a user control for columns AND rows-per-page, and a beige-cream UI.
- Codex (frontend only): (1) widened the feed <main> to max-w-7xl + header/footer to match; (2) feed page now reads cols (1-3, default 2) + rows (5-20, default 10) from searchParams, LIMIT = cols*rows, renders a responsive grid (mobile always 1 col, chosen cols at md+ via a static class map so Tailwind keeps the variants) and repaginates on the new LIMIT; (3) new client component FeedViewControls.tsx — Columns + Per-page selects that push cols/rows to the URL (reset page=1) and persist to localStorage; (4) OpportunityCard given h-full for equal-height tiles; (5) palette retuned to warm beige-cream light (bg #f4efe2) + warm neutral charcoal dark (bg #1b1917, killed the aubergine/purple tint), plum #60294e kept as the accent; (6) layout.tsx ThemeProvider defaultTheme="light" enableSystem={false} so everyone lands on cream.
- Review: verified pass — pnpm lint + tsc + build clean. Live preview at 1440px: main is 1280px centered, default 2-col grid (600px cards, 20 items = 2x10); driving ?cols=3&rows=5 → 3-col grid (394px cards, 15 items, "Page 1 of 253") — the control drives columns + per-page + pagination correctly; beige-cream is the default theme now. Committed on branch feat/desktop-grid as sohan1611. Open follow-up if founder wants: swap the plum accent to bronze/sage (they flagged "not maroonish"; plum kept for now as the logo colour).

- Follow-up (Claude, direct): founder found plum too "maroonish" → swapped the accent to warm bronze/caramel (light --primary/--ring #8f6234 on #fdf9f0 text; dark #c99f63 on #241a0e). Only --primary/--primary-foreground/--ring changed (no brand-* utilities are used anywhere). Verified in preview: buttons rgb(143,98,52) on beige-cream, AA-safe, light default.

## 2026-07-08 — Work order 30 (Claude → Codex): Account Area PART 1 (backend)
- Architect wrote docs/handoffs/ACCOUNT-AREA-HANDOFF.md (full premium /account IA + data model + endpoints + phasing). This WO = §4 backend foundation the frontend will consume.
- Codex (backend): migration c6d8e3f1a4b2 (down f2a9c7d4e1b0) adds users.notification_prefs jsonb null; new api/account.py with GET /account/me + PATCH /account/me (partial via model_fields_set, notification_prefs merged not clobbered) + POST /subscription/cancel (Razorpay cancel_at_cycle_end=1, never locally flips status — webhook stays source of truth per handoff §7 Binding); PlanState/AccountMe/AccountUpdate schemas with validators (name<=80, college<=120, graduation_year 2000-2100 else 422); registered router + added PATCH to CORS allow-methods; wired notification opt-out (wants() helper) into pipeline/notifications.py (daily_digest + instant_alerts) + weekly_report.py without weakening the existing can() gates; tests/test_account.py.
- Review: verified pass — ruff + black clean; migration up→down→up reversible on the pgvector container; 6/6 account tests pass; full suite 193 passed (was 187 + exactly these 6), the 10 failures are the known empty-container seed-data ones (green in CI vs the real DB); confirmed razorpay.errors.{BadRequestError,GatewayError,ServerError} all exist (the 400/502 cancel paths). Committed on branch feat/account-backend as sohan1611. NEXT: prod migration (founder-authorized) must land BEFORE the push (CI runs against the real prod DB + the model references the new column), then deploy; then WO-2 = the /account frontend.

## 2026-07-09 — Incident + fix: prod DB connection-pool exhaustion (Claude arch + Codex)
- Incident: during the account-area preview verification, a burst of prod-connecting diagnostic scripts + Render's pool exhausted Supabase's SESSION-mode pooler (hard cap 15 clients) → every DB endpoint (/plans, /feed, /account/me) 500'd with EMAXCONNSESSION; not self-healing (Render held dead connections). Root cause in core/db.py::make_engine: SQLAlchemy default pool (5+10=15) with no pool_pre_ping / pool_recycle on the 15-client session pooler → one instance can eat the whole budget + never recover stale connections. The account-area CODE was proven fine (container tests pass; the 500 traceback was a pure connection error, identical across all endpoints).
- Fix (Work order via Codex): core/db.py make_engine now defaults pool_pre_ping=True, pool_size=5, max_overflow=5 (<=10/instance, leaves headroom under the 15 cap), pool_recycle=300; caller kwargs still override (poolclass=NullPool handled). connect_args/keepalives/statement_timeout event untouched. New tests/test_db_pool.py.
- Review: ruff + black clean; test_db_pool 3/3 pass (repeatedly). Full local container suite was unreliable — Docker Desktop kept idling mid-run (5h/96-err then 4min/11-err runs, all e3q8 connection drops, zero code failures), so CI against the real prod DB is the authoritative full-suite gate on push. Committed on branch fix/db-pool-hardening as sohan1611.
- FOUNDER action for immediate recovery: restart the Render aspirova-api service (drops the stale pool). Deferred lever (architect): move DATABASE_URL to the TRANSACTION-mode pooler (port 6543) for real many-client scaling — bigger change (prepared-statement + statement_timeout implications), separate decision.

## 2026-07-09 — Work order (Claude → Codex): wide/spacious desktop layout + move theme toggle to Settings
- Founder: on a 1080p+ monitor the 1280px (max-w-7xl) layout looked shrunk/mobile with big empty side margins vs a spacious premium site (compared to apple.com); also wanted the header light/dark toggle removed and only in Settings.
- Codex (frontend only): feed <main> + AppHeader + AppFooter containers widened max-w-7xl -> max-w-[1680px] with px-4 sm:px-6 lg:px-10 xl:px-12; feed grid now 1 col (mobile) / 2 (sm) / user-chosen at lg+, cols clamp 1..4 DEFAULT 3, gap-4->gap-5; FeedViewControls Columns options 1-4 (was 1-3), control shows at lg+. Removed <ThemeToggle/> from AppHeader and DELETED components/ThemeToggle.tsx (no remaining consumers); theme control remains only in /account -> Appearance (AppearanceSection). defaultTheme stays "light" so signed-out visitors still land on beige-cream.
- Review: verified pass — pnpm lint + tsc + build clean. Live preview at 1920px: main is 1680px centered (was 1280), default 3-column grid (30 cards = 3x10), header has NO theme toggle (aria check false), beige-cream default. Committed on branch fix/wide-layout as sohan1611. NOTE: this is frontend-only so Vercel deploys it independently of the (currently flaky, pooler-contention) backend CI job.

## 2026-07-09 — Premium UI v2 PHASE 1 (Claude arch + Codex): "The Almanac" foundation + core surfaces
- Architect wrote docs/handoffs/PREMIUM-UI-V2.md (heritage-editorial "Almanac" POV derived from the logo; type scale, warm palette, layered shadows, paper grain, eyebrow labels, signature story/trust moments; 3-phase plan). Researched 2026 premium references (editorial/curatorial/trust-through-polish, Mercury/Stripe/Notion school).
- Codex (frontend, Phase 1 = foundation + most-seen surfaces): globals.css — modular type-scale vars, .eyebrow/.tnum utilities, refined warm tokens (paper #faf7f0 / card #fffdf8 / bronze #8a5e2f primary / #6b5f4e muted / #e6ddcb border), a rare --heritage plum token (light #5e2b47 / dark #cf92b4) exposed via @theme inline, a 3-step warm layered shadow system (--shadow-sm/md/lg) + .shadow-soft utilities, a barely-visible feTurbulence paper grain on body::before (2.5% light / 4% dark, pointer-events none, z-index -1), bronze ::selection, slim custom scrollbar, refined :focus-visible, a rise keyframe, h1/h2 tracking -0.02em + text-wrap balance. OpportunityCard — favicon "stamp", 2-line title, tracked JOB/REMOTE/heritage pills, deadline chip w/ .tnum, hover lift + bronze border. AppHeader — hairline rule + backdrop blur, animated nav underline-draw. button/badge/card primitives refined + a new `heritage` badge variant.
- Review: verified pass — pnpm lint + tsc + build clean. Live preview 1440px: card computed style confirmed the layered warm shadow + #fffdf8 raised card + rounded-xl + hover ease-premium + tracked pills + 3-col grid (389px cards); light + dark both read crafted/warm (dark = #1a1712 charcoal + bronze, subtle grain). Committed on branch feat/premium-ui-p1 as sohan1611. Phase 2 (story/hero/footer colophon/empty states) + Phase 3 (account member-card/detail/pricing) queued pending founder sign-off on the direction.

## 2026-07-09 — Premium UI v2 PHASE 2 (Claude arch + Codex): story + trust
- Codex (frontend): components/SignedOutHero.tsx (session-aware editorial masthead, returns null when signed in) wired atop the feed — eyebrow "EST. 2026 · THE OPPORTUNITY ALMANAC", Fraunces "Every opportunity. One place.", mission sub-line, a bordered TRUST line naming the sources (Greenhouse/Lever/Ashby/SmartRecruiters, source-first promise), "Create a free account" (HeaderAuth) + "Browse below" CTAs, hairline separator. AppFooter reworked into an editorial colophon (wordmark + mission, two eyebrow-headed link columns Browse/Aspirova, source-first colophon line, bottom bar with copyright + EST. 2026). Branded no-results empty state (stamped icon + Fraunces line + clear-filters).
- Review: verified pass — pnpm lint + tsc + build clean. Live preview: signed-out masthead + colophon render premium in the Almanac system (dark shown). Committed on branch feat/premium-ui-p2 as sohan1611.

## 2026-07-09 — Premium UI v2 PHASE 3 (Claude arch + Codex): account member-card + editorial detail + pricing
- Codex (frontend): app/account/page.tsx — full-width member card (larger avatar, Fraunces display name, email, heritage plan chip, "Member since" .tnum) + "YOUR ALMANAC" eyebrow; ProfileSection eyebrow labels + right-aligned save; AccountSidebar bronze active + SETTINGS kicker. app/opportunity/[slug]/page.tsx — editorial layout: back-link, favicon stamp + company, Fraunces role title, eyebrow meta pills, prominent source-first Apply CTA ("Opens the company own listing — Aspirova never mirrors applications"), reading column, reopen/summary callout. PricingPlans.tsx — eyebrow plan names, Fraunces price + .tnum, heritage "Most popular" + border-heritage accent, refined check rows + segmented billing toggle.
- Review: verified pass — pnpm lint + tsc + build clean. Live preview: pricing (eyebrow + Fraunces price + refined toggle) and opportunity detail (Fraunces title + source-first Apply CTA + trust line + eyebrow pills) both read editorial/premium. Committed on branch feat/premium-ui-p3 as sohan1611. (Noted separately: location mojibake e.g. "SÃ£o Paulo" is a pre-existing ingest-encoding bug, not design.)

## 2026-07-09 — Rich feed cards + quiet sign-in (Claude arch + Codex)
- Founder: after 3 premium phases the feed still looked generic — every company avatar was a flat beige initial (data has NO company domain/logo_url, so CompanyFavicon always fell back). Also (astute observation, comparing Apple/Samsung/Amazon) the header filled bronze "Sign in" button is an amateur/AI tell; premium sites keep utility controls quiet.
- Codex: CompanyFavicon now renders a deterministic COLORED MONOGRAM (10-tone warm heritage palette, char-hash → color, cream text, 44px, optional size) instead of the flat initial; real logos (when domain exists) render as bordered tiles. OpportunityCard rebuilt richer — 44px colored avatar, 2-line title, company line, icon meta (MapPin location / Globe Remote), NEW/relative-time freshness chip, category + heritage-hidden badges, deadline chip, hover lift + View→. Feed grid gap-6.
- Claude (direct, tiny): AppHeader now renders <HeaderAuth triggerVariant="ghost" triggerClassName="font-normal text-muted-foreground hover:bg-transparent hover:text-foreground"/> so the header Sign-in is a quiet muted text link (the hero "Create a free account" stays a deliberate content CTA). (Theme toggle was already removed from the header in a prior change.)
- Review: verified pass — pnpm lint + tsc + build clean. Live preview 1440px: avatars render 5 distinct warm tones across 8 cards (plum/brick/rust/indigo/mauve); sign-in bg transparent + muted text; richer card hierarchy visible. Committed on branch feat/rich-cards as sohan1611.
- OPEN data follow-ups (backend, flagged to founder): populate company.domain (unlocks REAL logos — the biggest premium jump) + fix ingest text mojibake ("SÃ£o Paulo", tofu titles).

## 2026-07-09 — Encoding fix (ftfy) + Company/Location feed filters (Claude arch + Codex)
- Codex (backend, gpt-5.5 xhigh): core/textclean.py fix_text (ftfy + strip + whitespace-collapse, idempotent); applied at ingest (opportunity title/location/description_raw + company_resolution cleans name before normalize/match) so new data stays clean; scripts/fix_encoding.py idempotent keyset-batched backfill with --limit/--dry-run; api/feed.py company filter (or of slug-eq and name ILIKE) + location ILIKE, AND-composed with existing filters. Tests added.
- Review: textclean 4/4 pass; "SA£o Paulo" -> "Sao Paulo" verified; CJK mojibake only partially reversible (lossy) — acceptable. ruff+black clean; uv lock added ftfy 6.3.1. DB tests could not run (Docker Desktop kept dying, e3q8) — code-reviewed sound, CI against the real prod DB is the gate. PROD --dry-run: WOULD FIX 3617 rows. Committed on branch feat/encoding-filters as sohan1611. NEXT (founder-authorized): run the real backfill after CI green; then real-logos + top-N Forbes + filter UI.

## 2026-07-09 — Real logos: company domain backfill (Claude arch + Codex)
- Since apply_url is an aggregator (no company domain in data), logos need a name->domain lookup. Codex wrote scripts/backfill_domains.py: for each domain-null company, queries Clearbit's free autocomplete (name->domain), and sets companies.domain ONLY when the top suggestion's normalized name matches (equal, or shared-first-token prefix) — a confidence guard so we never attach a WRONG logo. Keyset-batched, --limit/--dry-run/--sleep(0.4s), per-batch commit, idempotent (domain-null only). CompanyFavicon (already shipped) renders a real Google-favicon logo once a domain exists; unmatched companies keep their colored monogram.
- Review: ruff+black clean. PROD dry-run (limit 40): scanned 40, matched 25 (62%), skipped 15 — all example matches correct (Slingshot->slingshotedu.com, Tamworth Regional Council->tamworth.nsw.gov.au, Jabil->jabil.com, Ajax Systems->ajax.systems, City of Campbell River->campbellriver.ca). Confidence guard verified (no wrong matches). Ran the FULL prod backfill (founder asked for real logos). Committed script on branch feat/logo-domains as sohan1611.

## 2026-07-09 — Top-N Forbes filter (Claude arch + Codex)
- Claude bundled backend/data/forbes_global2000.json (top 1000 from the prasertcbs Forbes Global 2000 CSV: rank, name, domain; 988 w/ domain). Codex: Company.global_rank + index, migration b7e3c9d2a1f4 (down c6d8e3f1a4b2), scripts/match_forbes.py (exact domain-first then normalized-name match, --dry-run/--reset, idempotent), api/feed.py top=N (>0) filter (global_rank IS NOT NULL AND <= N), tests.
- Review: ruff+black clean; logic sound (domain precedence). Applied migration to PROD (additive, reversible). PROD dry-run: scanned 179, would rank 5 (Delta Air Lines 715, Capgemini 484, Lockheed Martin 134, Caterpillar 132, Western Digital 905) — accurate but low yield (startup-heavy catalog, as flagged). Ran the real match: 5 ranked. Committed on branch feat/logo-domains as sohan1611. NEXT: frontend filter UI (Company + Location + Top-N tiers).

## 2026-07-09 — Filter UI: Location + Company + Top-N (Claude arch + Codex)
- Codex (frontend): SearchFilters.tsx split into primary row (search/category/remote) + a REFINE row (Location Input, Company Input, Top-companies segmented Any/10/50/100/500/1000, Clear all); page.tsx reads location/company/top and passes to getFeed; lib/api.ts FeedParams extended. Claude fixed a react-hooks/set-state-in-effect lint error by removing the URL->state sync effect and instead keying <SearchFilters> on the filter params in page.tsx (remounts to reflect the URL / reset on Clear-all).
- Review: verified pass — pnpm lint (0) + tsc + build clean. Live preview: ?top=1000 shows 252 opps, Top control shows 1000 active, Location/Company inputs + Clear-all present; and the feed now renders REAL logos (Western Digital) for Forbes-ranked companies. Committed on branch feat/filter-ui as sohan1611.

## 2026-07-09 — DB pool relax for Supabase Pro (Claude arch + Codex gpt-5.6-terra @ ultra)
- Founder upgraded to Supabase Pro (much higher pooler client limit); the API runs on a PERSISTENT Render server so session-mode pooler (port 5432) is correct (transaction mode is for serverless). The earlier 5+5=10 cap was only to survive free's ~15-client limit. Relaxed to POOL_SIZE=10/MAX_OVERFLOW=10 (up to 20/instance, comfortably under Pro) for concurrency + CI headroom; kept pool_pre_ping + pool_recycle 300 + connect_args/keepalives/statement_timeout event untouched; DATABASE_URL/port unchanged (still session mode).
- Review: ruff+black clean; test_db_pool 3/3 pass (inspect engine.pool, no DB). Committed on branch fix/pool-relax as sohan1611. Should end the CI backend-job pooler-contention flakiness now that Pro + more headroom are in place.

## 2026-07-10 — Premium pass on remaining pages (Claude arch + Codex gpt-5.6-sol @ ultra)
- Codex (frontend): CopilotChat (editorial header eyebrow+Fraunces, PRO-INTELLIGENCE heritage badge, premium member-access/upsell card, chat bubbles, composer, suggestion prompts, source chips), ResumeMatchPage (Fraunces header, tidy form Card, OpportunityCard match grid + fit badges, skeleton/gated/empty/error states), app/companies/page.tsx (THE ROSTER editorial directory, refined company Cards w/ CompanyFavicon stamp + open-role .tnum counts + hover, branded empty state), companies/[slug] (masthead + domain link + OpportunityCard grid), lib/types.ts (CompanyListItem now exposes domain/logo_url).
- Review: verified pass — pnpm lint(0)+tsc+build clean; live preview: /companies (THE ROSTER, Fraunces "Companies hiring now", Stripe/Airbnb/Western Digital cards, 202 companies) + /copilot signed-out (editorial header + member-access card) both premium. Committed on branch feat/premium-remaining as sohan1611 (also carries the test_feed_filters black-fix commit).
- FOLLOW-UP (small backend gap): /companies API CompanyListItem schema does NOT return domain yet, so the companies page shows monograms not real logos — add domain (+logo_url) to the companies list response to light up real logos there (fold into logo-coverage task #37).

---

## #37 — Widen real-logo coverage (companies directory) — gpt-5.6-sol @ ultra
- **Claude → Codex:** /companies list endpoint didn't return `domain`, so the directory fell back to monograms while the feed showed real logos. Order: expose `domain`+`logo_url` on `CompanyListItem` (schema + `list_companies` SELECT/GROUP BY, ordering unchanged); add a comparison-only legal-suffix strip to the backfill matcher (still first-token-guarded, exact after strip — no fuzzy); tests.
- **Codex delivered:** `CompanyListItem.domain/logo_url`; `list_companies` returns them; `_strip_legal_suffixes` + suffix-equality branch in `_is_confident_match`; new `test_backfill_domains.py`; logo-field assertions in `test_company_list.py`.
- **Claude verified:** ruff clean, black clean (reformatted 1 file), matcher tests 3/3. Prod dry-run (60) → 0 new matches: remaining unmatched companies are genuinely obscure (Clearbit unknown), so coverage stays ~45%; the real win is the schema fix lighting up real logos for already-matched companies. Committed as `sohan1611` (f5950a5) on `feat/logo-coverage`. **Awaiting merge authorization (master push → Render deploy). No prod DB write required.**

## #38 — Consolidated Filter dropdown + Sort dropdown (feed) — gpt-5.6-sol @ ultra
- **Claude → Codex:** Consolidate the scattered "REFINE" filter row into ONE Filter dropdown (Popover) + a Sort dropdown side by side. Backend `sort=recent|deadline` and `getFeed({sort})` already exist; only the UI + feed-page wiring is missing. New `ui/popover.tsx` (radix Popover, Almanac-styled); rewrite `SearchFilters.tsx` (search box + Filter popover holding Category/Work mode/Location/Company/Top-N + active-count badge + Clear-all-preserving-sort + Sort Select Newest/Closing-soon); feed page reads+passes `sort` and persists it across pagination. Frontend only; preserve exact URL-param semantics.
- **Status:** Codex running (branch `feat/feed-filter-sort`); Claude to verify lint+tsc+build then preview.

### #38b — keep the Filter popover open across selections (follow-up) — gpt-5.6-sol @ ultra
- **Claude found (preview):** the popover closed after every filter click because page.tsx put the filter params in `<SearchFilters key=…>`, forcing a remount that reset the Radix open state.
- **Order:** remove the remount key; resync the q/location/company inputs to searchParams so Clear all / Back still reset them.
- **Codex applied** the key removal + a `useEffect` resync. **ESLint (react-hooks/set-state-in-effect) rejected setState-in-effect.** Claude replaced it with React's canonical "adjust state during render" (guarded prev-signature) pattern — no effect — and fixed a 2-space indent in page.tsx.
- **Claude verified (preview + gates):** eslint + tsc + `pnpm build` all clean; stacked category+remote+top in ONE open popover session (badge counts 1→3, popover stays open); Sort composes (`?…&sort=deadline`, reorders); Clear all clears filters + empties Location/Search inputs (render-pattern reset, no remount) + preserves sort; no 375px overflow; no console errors. Ready to commit on `feat/feed-filter-sort`.

## #39 — Active-filter chips on the feed — gpt-5.6-sol @ ultra
- **Claude → Codex:** Extend components/SearchFilters.tsx to render a removable-chip row beneath the Filter/Sort controls — one pill per active param (q as `Search: "…"`, category/remote via option labels, location/company raw, top as `Top N` with .tnum), each with an X that calls updateParam(key,null); a "Clear all" that reuses clearFilters() (preserves sort); renders nothing when no filters active. Frontend only, one file, reuse existing param logic + Almanac primitives (Badge secondary), responsive, a11y, no setState-in-effect. Supabase-safe (zero DB impact).
- **Status:** Codex running (branch feat/filter-chips); Claude to verify lint+tsc+build + preview.

- **Codex delivered:** extended SearchFilters.tsx with an `activeFilters` descriptor list + a chips row (Badge secondary pills, keyboard-focusable X per chip calling updateParam(key,null), Top N in .tnum, chips-row "Clear all" reusing clearFilters). Renders nothing when no filters active.
- **Claude verified (gates + preview):** eslint + tsc + `pnpm build` clean; all 5 chip types render (Search:"designer" / Internships / Remote / London / Top 50); removing a chip drops only that param; chips-row Clear all clears filters + preserves sort (?sort=deadline); 6 chips at 375px with no overflow; no console errors. Committed on feat/filter-chips.

## #40 — Search composes with filters — gpt-5.6-sol @ ultra
- **Claude found (verifying #39):** when a search `q` is active, page.tsx routes to /search which ignores the feed filters (category/remote/location/company/top) — so search + filters don't compose and the chips misrepresent what's applied.
- **Claude → Codex:** extract feed.py's filter predicates into a shared backend/api/filters.py `opportunity_filters(...)`; refactor feed.py to use it (behavior identical); add the 5 filter params to /search and apply them to all 3 builders (fts_query, fts_total count, fallback) while preserving relevance order + count/fallback semantics (no `sort` on search); frontend searchOpportunities forwards filters + page.tsx passes them; extend test_search.py. Cross-stack, Supabase-safe (same query count).
- **Status:** Codex running (branch feat/search-filters); Claude to verify ruff/black/pytest + eslint/tsc/build + preview.

- **Codex delivered:** backend/api/filters.py `opportunity_filters()` (verbatim extraction); feed.py refactored to use it (identical behavior); /search gains the 5 filter params applied to all 3 builders (fts_query, fts_total count, fallback) with relevance/count/fallback intact; searchOpportunities(q, filters, page, limit) + page.tsx passes a shared filters obj to both branches; new test_search.py (1 no-filter + 5 parametrized filter cases).
- **Claude verified:** ruff+black clean; **test_search.py 6/6 passed against the real DB**; frontend eslint+tsc+build clean; direct API — search "engineer" 2664 → +remote 752 → +internship 52; frontend end-to-end /?q=engineer&remote=true shows 752 (filtered), no console errors. Committed on feat/search-filters.

## Phase 5 — Opportunity Expansion (cream layer + competitions) — architecture by Claude Opus
- **Diagnosis (prod, 2026-07-11):** 203 companies, only 5 Forbes-ranked (min 132) → Top-10/50/100 = 0; MAANG count = 0. Root cause: crawler seed = ~21 hardcoded ATS companies + RemoteOK; MAANG don't use those ATSes. Architecture written to docs/handoffs/PHASE-5-HANDOFF.md.
- **Founder decisions:** cream-layer FIRST; MAANG via staged custom adapters (public JSON only); ask before each prod write.

### #41 — Phase 5.1 cream layer — gpt-5.6-sol @ ultra
- **Claude → Codex:** prestige_rank migration + Company field; scripts/match_prestige.py (mirror match_forbes, reads Claude-curated data/prestige_companies.json = 130 cos incl MAANG + top unicorns + Indian names); api/filters.py top = (prestige_rank OR global_rank) <= N; seed_companies.py += 30 Claude-verified-live famous companies (OpenAI/Anthropic/Databricks/Cohere/Perplexity/Ramp/Spotify/…); tests (match_prestige, feed top-filter, search). Backend only, no prod writes.
- **Status:** Codex running (branch feat/cream-layer); Claude to verify ruff/black/pytest, then founder-authorized prod migration+seed+crawl+rank.

- **Codex delivered:** migration a5d1f8c3e7b2 (prestige_rank col+index, chains from b7e3c9d2a1f4 head); Company.prestige_rank + index; scripts/match_prestige.py (reads data/prestige_companies.json, writes prestige_rank); api/filters.py top = or_(prestige_rank<=N, global_rank<=N); seed_companies.py +30 verified famous cos; tests/test_match_prestige.py + extended test_feed_filters.
- **Claude verified:** ruff+black clean; migration chains from single head; models/filters/matcher logic correct. Feed/search tests ERROR only with "column prestige_rank does not exist" — model is ahead of the un-migrated prod DB (expected); they pass once the migration is applied. Committed on feat/cream-layer. NEXT (founder-authorized): apply migration → seed → crawl → match_forbes + match_prestige → verify Top-N live.

- **Claude PROD ROLLOUT (founder-authorized, within Supabase limits):** applied migration a5d1f8c3e7b2; merged 480edab (CI green, Render redeployed); seeded 30 famous cos (30 created); ran match_forbes (5) + match_prestige (46 ranked). **Top-N fixed LIVE: /feed?top=50 = 0→2696, top=100→3752.** Spotify ingested (109). Remaining 29 famous cos ingesting via the running scheduled crawl (already prestige-ranked → hit Top-N on ingest). OP NOTE: two concurrent crawls (my triggered + scheduled) caused pooler contention (~10x slowdown, ~10 companies/20min); cancelled the triggered run so one crawl finishes alone. top<=10 will fill once OpenAI(r7)/Anthropic(r10) crawl.

- **5.1 close-out (crawl finished):** Top-N LIVE — top<=10 0→400 (Anthropic r10), top<=50 0→4624, top<=100→5818; total opps 4026→6207. Ingested famous Greenhouse cos (Anthropic/Databricks/Instacart/Lyft/MongoDB/…) + Spotify. OPEN: Ashby cos (OpenAI/Cohere/Perplexity/Ramp/Vanta/Replit/…) not yet ingested — scheduled crawl hit GitHub Actions 40-min job timeout before the Ashby source (contention wasted ~13min). Self-heals next scheduled crawl (Greenhouse now change-detection-skips → time reaches Ashby). ARCH NOTE: crawl at the 40-min ceiling — before adding MAANG/competition sources, raise the Actions timeout OR split ATS vs aggregator jobs OR activate the crawl_jobs parallel queue. PAUSED per founder (asleep).

## #42 — Fix crawl duration / Actions-minute burn — gpt-5.6-sol @ ultra + Claude cost edits
- **Trigger:** GitHub emailed "90% of Actions minutes used" (1840/2000). Cause: Crawl Tier 1 ran every 2h (12x/day) and hit its 40-min timeout every run since the +30 companies — never reaching the Ashby boards or the aggregator (aggregator ran 0x/12h).
- **Codex:** added --group ats|aggregator|all to crawlers/runner.py (run_tier); split crawl-tier1.yml into ATS-first step + best-effort aggregator step (continue-on-error); test_runner group test.
- **Claude cost edits:** cron 2h→12h (2x/day, matches founder's original plan); job timeout 90→40; aggregator step hard-capped timeout-minutes:12 (biggest minute hog). Verified ruff+black clean, test_runner 6/6, YAML valid.
- **Also (separate, local):** ran an ATS-only crawl LOCALLY (0 Actions minutes) to immediately ingest the Ashby cos (OpenAI/Cohere/ElevenLabs/etc.) that the timed-out Actions crawl kept missing.

## #43 — Phase 5.1c: MAANG adapters (Amazon) — gpt-5.6-sol @ ultra
- **Claude probe of MAANG public job APIs (2026-07-11):** Amazon amazon.jobs/en/search.json = WORKS (clean JSON, 165 intern hits); Google careers v3 = 404 (locked); Microsoft gcsservices = SSL/hostname mismatch (not public); Apple jobs api = 404/CSRF; Netflix = 0 Lever postings. Per founder rule (public JSON only, stop+flag if blocked, never scrape around) → build Amazon; FLAG Google/Microsoft/Apple as not publicly crawlable (would need browser scraping = out of scope/ToS).
- **Claude → Codex:** new crawlers/amazon.py (adapter contract; bounded student-role queries [intern/new grad/graduate], cap ~900, dedupe; company_name fixed 'Amazon'; apply_url = amazon.jobs listing); register in ATS_ADAPTERS; seed Amazon source+company (domain amazon.com, prestige 4); test via captured fixture tests/fixtures/amazon_search_sample.json.
- **Status:** Codex running (branch feat/amazon-adapter); Claude to verify ruff/black/pytest, then LOCAL ingest (0 Actions min) + match_prestige.

- **Codex delivered:** crawlers/amazon.py (AmazonAdapter, terms intern/new grad/graduate, per-query cap 300, dedup, polite delay, apply_url=amazon.jobs listing, company_name fixed 'Amazon'); registered "amazon" in ATS_ADAPTERS; seeded Amazon source+company (amazon.com); tests/test_amazon_adapter.py + fixture.
- **Claude verified:** ruff+black clean, test_amazon 5/5; LIVE fetch = 420 student roles (156 intern + 264 new-grad), health ok. Seeded Amazon to prod + LOCAL crawl (0 Actions min) → 332 Amazon opps ingested; match_prestige (47 ranked, Amazon r4). Prod: /feed?company=amazon=332, top<=10 1127→1473 (Amazon joins OpenAI/Anthropic). MAANG G/MS/Apple flagged (no public API; would need ToS-violating headless scraping) — offered a licensed-aggregator (Adzuna) route instead.

## Phase 5.2 — Competitions / Hackathons — architecture by Claude Opus
- **Claude probe (2026-07-11):** Devpost api/hackathons = WORKS (13,578 hackathons; prize_amount, submission dates, org, url). Unstop api/public/opportunity/search-result = WORKS (Flipkart-GRiD-class competitions; regn_open, organisation, seo_url, type/subtype). Both public JSON → crawlable.

### #44 — 5.2a schema + ingest foundation — gpt-5.6-sol @ ultra
- **Claude → Codex:** migration add opportunities.meta JSONB; Opportunity.meta + NormalizedListing.meta; ingest passes meta through; broaden category validators to internship|job|hackathon|competition (feed/search); tests. Backward-compatible plumbing only (adapters come in 5.2b).
- **Status:** Codex running (branch feat/competitions-schema); Claude to apply migration + verify + merge.

- **Codex delivered:** migration c9f4e2a6b1d8 (opportunities.meta JSONB, chains from a5d1f8c3e7b2); Opportunity.meta + NormalizedListing.meta; ingest passes meta through (create+update); category validators broadened to internship|job|hackathon|competition (feed+search); tests.
- **Claude verified:** ruff clean; black (Codex missed 2 files → Claude reformatted); applied migration to prod (a5d1f8c3e7b2→c9f4e2a6b1d8); test_ingest+test_feed_filters 15/15 (meta round-trips, hackathon/competition accepted, backward compatible). Committed on feat/competitions-schema.

### #45 — 5.2b Devpost + Unstop adapters — gpt-5.6-sol @ ultra
- **Claude probe/fixtures:** Devpost /api/hackathons?status[]=open (72 live; submission_period_dates range, prize_amount HTML, organization_name); Unstop /api/public/opportunity/search-result?opportunity=competitions|hackathons (end_date ISO w/ tz = registration deadline, organisation.name, region=mode, type). Saved tests/fixtures/devpost_sample.json + unstop_sample.json.
- **Claude → Codex:** crawlers/devpost.py + crawlers/unstop.py (aggregator-type, no-arg ctor; category hackathon/competition + meta + deadline); register in AGGREGATOR_ADAPTERS; order remoteok LAST (competitions not starved by its 12-min cap); seed devpost+unstop sources; adapter tests via fixtures.
- **Status:** Codex running (branch feat/competition-adapters); Claude to verify + LOCAL ingest (0 Actions min) + confirm Flipkart-type competitions.

- **Codex delivered:** crawlers/devpost.py + crawlers/unstop.py (aggregator adapters); registered in AGGREGATOR_ADAPTERS; run_tier orders remoteok LAST (aggregator_jobs.sort remoteok->last); seeded devpost+unstop sources; adapter tests + fixtures.
- **Claude verified:** ruff clean; black (fixed 1 Codex missed); test_devpost+test_unstop+test_runner 21/21; LIVE fetch = Devpost 72 hackathons (deadlines from date-range end) + Unstop 1669 (669 competition + 1000 hackathon, ISO deadlines), organizers resolve (XPRIZE/Salesforce/IIT/etc.). Committed on feat/competition-adapters.

### #48 — 5.2d competitions UI + 14-day lifecycle — gpt-5.6-sol @ ultra
- **Codex delivered:** meta on OpportunityListItem + /feed kind param; Hackathons/Competitions filter; competition cards (prize ₹/mode/deadline); /competitions (THE ARENA) + nav; 14-day lifecycle (open→"Registers by", closed≤14d→"Registrations closed" ranked below open, closed>14d excluded); Unstop skips >14d-closed; scripts/cleanup_expired_competitions (FK-safe) + crawl-workflow prune step; tests.
- **Claude verified + hardened:** 31 backend tests pass (incl cleanup), eslint/tsc/build clean, ruff/black clean. Hardened cleanup (batch 200, per-batch commit, 120s maint timeout) after a statement-timeout from crashed-pytest lock contention. Ran cleanup → drained 1180 stale competitions (0 stale >14d remaining; 520 live competitions; 9703 active opps). Committed on feat/competitions-ui.

## Rescue & Polish Roadmap (docs/handoffs/RESCUE-POLISH-ROADMAP.md) — Claude Opus
- Founder-reported live issues. RP-0 DONE (competition dates: lifecycle + drained 1180 stale). RP-1 DD/MM dates; RP-2 perceived perf (loading skeletons + useTransition; feed API 0.3-1.2s); RP-3 roles/competitions split + PPI/PPO bridge; RP-4 internship tracks (company/college/research/learning, subsumes research vertical); RP-5 Google/MS/Apple cards + flagship programs.
### #49 — RP-1+RP-2 — gpt-5.6-sol @ ultra (branch feat/polish-dates-perf, running bztoxn05o)
- **Codex delivered:** lib/date.ts (en-GB formatDate long/numeric); day-first dates on cards ("5 Mar")/detail ("05/03/2026 DD/MM/YYYY")/account/referral; OpportunityCardSkeleton + loading.tsx for (feed)/competitions/companies; useTransition + isPending pending-affordance in SearchFilters + FeedViewControls.
- **Claude verified:** eslint+tsc+build clean; preview — cards "Registers by 11 Jul", detail "Deadline: 11/07/2026" (day-first ✓); no console errors. Committed on feat/polish-dates-perf.

### #50 — RP-3 roles/competitions split + PPI/PPO bridge — gpt-5.6-sol @ ultra
- **Codex delivered:** unstop.py meta.offers_ppi/offers_ppo from prizes' pre_placement flags; feed.py kind=roles = category(internship,job) OR meta offers_ppi/ppo (JSONB as_boolean); home feed defaults kind=roles (competitions no longer clutter internships; explicit category/kind overrides); OpportunityCard "Offers internship/PPO" heritage badge; tests.
- **Claude verified:** ruff+black clean, 17 tests pass, eslint+tsc+build clean. Committed on feat/roles-competitions-split. Re-crawling Unstop locally to backfill meta.offers_ppi on existing rows.

### #51 — India internships + deadline correctness — gpt-5.6-sol @ ultra + Claude fixes
- **Codex delivered:** Unstop adapter adds opportunity=internships; extend 14-day lifecycle to internships (filters/cleanup); card "Apply by"/"Applications closed".
- **Claude found + fixed 2 real bugs during review:** (1) DEADLINE was Unstop end_date (event's final-round date) not the registration close — added _registration_deadline() using regnRequirements.end_regn_dt (fallback end_date). This is why closed contests showed future deadlines. (2) CATEGORY: Unstop returns internships with type="jobs", so all 1000 internships were miscategorized as "competition" — now derive category from the SEARCH opportunity type (injected as _aspirova_opportunity in fetch). Live re-verify: 967 internships (was 0), Indian orgs, registration deadlines. All gates + 19 tests green.

### #52 — A1 feed open-first + roles-first ordering — gpt-5.6-sol @ ultra
- Codex: unified feed ordering — is_closed asc (open/undated first), roles-first when kind=roles, then sort (open-deadline asc / last_seen), id tiebreak.
- Claude verified: ruff/black + 8 tests; live — internships sort=deadline leads with OPEN (was closed); kind=roles leads with internships. Merged. NOTE found A6: meta.skills payload bloat (perf).

### #53 — C1 closing-soon deadline alerts (never-miss) — gpt-5.6-sol @ ultra
- Codex: send_closing_soon_alerts (bookmarked opps closing within 3d, open, per-(user,opp) dedup type=closing_soon, wants() pref, Resend path) + _render_closing_soon html/text; --closing-soon worker arg; tests (14 pass).
- Claude: added the missing daily workflow step (Codex omitted it); smoke test runs clean {users_notified:0}. Verified ruff/black + 14 tests. Merged. THE anti-Flipkart never-miss feature, unblocked by the deadline fix.

### A6 — trim Unstop meta payload (perf) — Claude direct (small)
- meta.skills -> deduped list of name strings (was full nested objects w/ pivot/ai_generated); meta.prizes -> {rank,cash,currency} (dropped pivot/entity internals). offers_ppi/ppo computed before trim. Card unaffected (reads prize.cash). Tests updated, 11 pass, frontend build clean. Re-crawl backfills existing rows.

### #46 — RP-5 straight-to-source cards for Google/Microsoft/Apple — gpt-5.6-sol @ ultra
- **Architect (Claude) authored the data:** lib/externalCompanies.ts — 3 companies, verified careers URLs, and flagship annual programs (Google: GSoC, GDG Solution Challenge, STEP, Girl Hackathon; Microsoft: Imagine Cup, Engage, Explore; Apple: Swift Student Challenge). Every URL fetch-verified live (HTTP 200) before shipping. Trust guardrail: no scraping, honest "we always link to source" framing, tentative-timeline caveats.
- **Codex delivered (frontend):** SourceCompanyCard (branded card) + SourceCompanyDetail (honest note + verified careers CTA + flagship-programs grid w/ 🌍/🇮🇳 scope badges + official links); search q=google/microsoft/apple surfaces the card; /companies/<slug> short-circuits the API for the 3 slugs; /companies "Marquee employers" row.
- **Claude verified:** eslint+tsc+build clean; live browser — /companies/google+apple render honest detail, /?q=google surfaces the card above 1019 results, /companies shows the 3-up marquee, normal slug (/companies/databricks) still hits the API. Polished a duplicate heading (eyebrow → "The annual calendar"). Merged to master as sohan1611.
- **Ops note:** stopped the A6 meta-trim backfill (failing on lock contention from a leaked idle-in-transaction session — a killed re-crawl); meta-trim is perf-only and applies on fresh crawls. Leaked session is harmless to reads; flagged to founder to clear via Supabase SQL editor if desired.

### #47 — RP-4 research track (IISc/IIT/NIT/TIFR) — gpt-5.6-sol @ ultra
- **Architect (Claude) authored + verified the data:** lib/researchPrograms.ts — 7 flagship Indian research programs (Science Academies' SRFP national, IISc Bengaluru, IIT Bombay IRCC, IIT Madras SFP, IIT Kanpur SURGE, NIT Rourkela, TIFR VSRP). Every applyUrl fetch-verified live (HTTP 200) via WebFetch+curl. DROPPED IIT Gandhinagar SRIP — unreachable (000) from both resolvers, unverifiable = not shipped (trust rule). Plus matchesResearchIntent() token-based matcher.
- **Codex delivered (frontend):** ResearchProgramCard + /research static page + "Research" nav link + /research in sitemap + feed search callout when matchesResearchIntent(q).
- **Claude verified:** eslint+tsc+build green; live — /research renders all 7 with verified apply links, /?q=iit surfaces callout, /?q=united does NOT false-trigger (token guard). Merged to master as sohan1611 (e332fd6).

### #54 — source-diversity interleave + Source filter — gpt-5.6-sol @ ultra [STAGED, pending prod migration]
- **Diagnosis (Claude, read-only prod query):** default feed sorts last_seen_at DESC → the most-recently-crawled source (Unstop) forms a solid wall; first 30 cards 100% Unstop even though Unstop is ~10% of active roles (greenhouse 5884/58%, ashby 1869, unstop 975, smartrecruiters 694, amazon 372, remoteok 227, lever 178). Root cause = ordering clusters by crawl batch, not volume.
- **Codex delivered:** opportunities.primary_source column (migration e3b7a1c9d5f2 + backfill_primary_source.py) set at ingest from adapter slug; feed recent-sort round-robins via row_number() OVER (PARTITION BY primary_source ORDER BY last_seen_at DESC, id DESC); grouped Source filter (direct/unstop/remoteok/devpost) on /feed + search; source exposed on OpportunityListItem; card 'via X' chip; SearchFilters UI + chip; tests (filter + interleave/stable-pagination).
- **Claude verified so far:** frontend eslint+tsc+build green; backend ruff+black clean; interleave logic + tests reviewed (existing feed tests unaffected — their NULL-source rows share one '' partition, so source_rank reproduces the old last_seen/id order = no-op). Committed on branch feat/source-mix-filter (e105de3), NOT merged.
- **PENDING (needs founder OK — classifier gates prod migration):** (1) alembic upgrade head, (2) python -m scripts.backfill_primary_source, then Claude runs full pytest + live browser check, then merges to master. Must migrate+backfill BEFORE merge or the live API queries a missing column.
- **Regression audit (Claude, read-only):** all past bugs still fixed — deadlines correct (0 Unstop roles with >120d deadline), categories clean (1170 internship / 8991 job / 289 competition / 236 hackathon), 595 open India roles, oldest active internship only 12 days old (no months-stale), closed comps excluded (11 past-grace rows invisible, cleanup due). 

### Audit & polish (3 batches) — gpt-5.6-sol @ ultra
- **Context:** founder asked for a comprehensive checkup — verify every reported bug fixed, find + fix new minor bugs, add premium improvements ("site still feels blunt").
- **Batch 1 (5a88574) — Saved workspace + SEO domain fix:** BookmarksProvider (shared, optimistic) + SaveButton on every card (overlay sibling of the card Link, no nav hijack) + /saved page + "Saved" nav link + detail BookmarkButton reuse; fixed SITE_URL from dead aspirova.vercel.app → www.aspirova.org (canonical/OG/sitemap/robots). Verified: lint/tsc/build, live save-on-card + /saved signed-out.
- **Batch 2 (0cf822f) — homepage trust + discoverability:** GET /stats (cached 300s via ReadCacheMiddleware, single aggregate query) + homepage stats bar (10,678 opps · 1,220 cos · 8 sources · updated daily, graceful null fallback) + SignedInWelcome strip + footer Competitions/Research links + detail company→/companies link + broadened hero copy. Verified: ruff/black + test_stats (2) + lint/tsc/build + live stats bar.
- **Batch 3 (a697170) — mobile nav + pricing:** MobileNav (Sheet drawer) replaces horizontal-scroll nav on phones (desktop unchanged); pricing masthead → Almanac eyebrow+font-serif. Verified: lint/tsc/build + live 375px hamburger opens Sheet with all 7 links.
- **Claude also:** compiled the full bug ledger (10 reported bugs, all fixed/1 partial), ran a fresh scan (8 new issues, 6 fixed / 2 roadmap), delivered a written field report artifact, and flagged the tight 15-slot session-mode DB pool as a scaling/reliability item. Ran verification egress-lean per founder's Supabase-usage note (targeted tests only, no persistent local backend).

### Crawler reliability — gpt-5.6-terra @ ultra (Phase 1) [5579314]
- **Diagnosis (Claude, prod + Actions logs):** Tier-1 crawl fetched ~100 ATS companies sequentially (~17 min), then the aggregator step hit its 12-min cap and was SIGKILL'd mid-query -> a QueryCanceled Postgres ERROR every run + some 40-min job cancellations. Main ongoing error source.
- **Codex delivered:** parallel ATS fetch (ThreadPoolExecutor, 10 workers, HTTP-ONLY — workers never get a Session, so the 15-slot session-mode pool sees no extra connections; ingest stays one-session-sequential); aggregator internal soft deadline (DEFAULT_AGGREGATOR_MAX_SECONDS=600, env-tunable) so it commits+exits cleanly before the 12-min hard cap; SIGTERM/SIGINT handler sets a stop flag checked at safe commit boundaries; prefetched param keeps back-compat.
- **Claude verified:** ruff+black, 26 crawler tests. Merged to master as sohan1611. Next 12:00 UTC crawl will confirm clean completion.

### Country + location scope — gpt-5.6-terra @ ultra (Phase 2) [43834e6]
- **Founder spec:** pick a home country (searchable flag dropdown, alphabetical) + an Abroad/<country>/Both toggle beside Filter; Remote shows in all scopes; country in localStorage (no account column).
- **Codex delivered:** pipeline/location_country.derive_country() (country names/aliases + major global & Indian cities; remote/online -> None; ambiguous short ISO codes avoided); opportunities.country column (migration f4d2c8b7a1e5 + backfill_country.py; set at ingest on create+content-change); filters.location_scope_filters + scope/country params on /feed + /search (domestic = country==X OR remote; abroad = country!=X OR remote); frontend lib/countries.ts + CountryPicker + LocationScope wired into SearchFilters.
- **Claude verified + fixed:** applied migration + backfill to prod (8538 rows, 79%; US 4886/GB 661/CA 608/IN 366/... + 4438 remote); 63 backend tests pass; fixed a LocationScope setState-in-effect lint error myself (-> useSyncExternalStore/useHydrated); frontend lint+tsc+build green; scope magnitudes sane on real data. Merged as sohan1611. Model note: dispatched on gpt-5.6-terra @ ultra per founder.

### Discovery + polish — gpt-5.6-terra @ ultra [2c0e48c]
- **Scan (Claude, live prod + code):** site clean (no console errors, location scope filters correctly). Found /competitions missing from sitemap; mobile control row crowding; scope-toggle-only-on-home (noted).
- **Codex delivered:** GET /opportunity/<slug>/similar (tiered heuristic: same company > category+country > category > country; active+non-closed, excludes self; single indexed query) + "Related opportunities" grid on the detail page; country on OpportunityListItem + country-flag on cards; SearchFilters control row wraps on mobile.
- **Claude:** added /competitions to sitemap (direct 1-line fix); verified ruff/black + 4 similar tests + real-data relevance (Amazon slug -> 6 Amazon roles); frontend lint/tsc/build green. Merged as sohan1611.

### Application tracker — gpt-5.6-terra @ ultra [e9715be]
- **Codex delivered:** bookmarks.status (saved|applied|interviewing|offer|archived, default 'saved'; additive migration b9e3f7c2a6d4, existing rows auto-default) + PATCH /bookmarks/<slug> (rate-limited, 404/422 guards) + /bookmarks returns bookmark_status (SavedOpportunityItem); /saved rebuilt into a pipeline — stage tabs with live counts + per-card stage dropdown, optimistic (revert+toast on error).
- **Claude verified:** ruff/black; applied migration to prod (no backfill needed — server_default; 0 bookmarks currently); 4 bookmark tests pass; frontend lint/tsc/build green; reviewed the tracker UI (optimistic, async-callback setState, not the flagged pattern). Merged as sohan1611.

### "For You" personalized feed + onboarding — gpt-5.6-terra @ ultra [840cf2c]
- **Codex delivered:** GET /for-you ranks the feed by interest fields via the existing FTS (search_tsv + ts_rank; FIELD_KEYWORDS dict), recent-feed fallback when no keywords, respects category + country/scope; frontend OnboardingDialog (first-run: pick fields + country -> localStorage), a For You/Latest switch that pushes interests into the URL for the server feed to rank; interests.ts uses the useSyncExternalStore/useHydrated store pattern.
- **Claude verified + fixed:** ruff/black; frontend lint/tsc/build green; real-data ranking excellent (fields=data_ai -> ML/AI roles top the list). Codex's 2 no-keyword tests asserted absolute total/order which can't hold against the shared prod DB (domestic scope ORs is_remote -> real remote rows leak); Claude rewrote them to deterministic invariants (pagination consistency; freshest-open-row surfaces). 4 tests pass. Merged as sohan1611.
- **Roadmap decisions (Claude):** (1) feed perf index — EXPLAIN shows a 5.4MB disk sort but the query runs in 141ms; deferred as premature optimization (uncertain planner use + write overhead vs marginal gain; Doc 02 measured-not-anticipated principle). (2) location scope on /internships etc. — skipped (low value; would clutter the simple SEO landing pages). (3) transaction-mode pooler — founder/infra hand-off (deliberate session-mode decision #35; risky to flip without connection-config changes + testing).

### Transaction-pooler support — Claude direct (high-stakes infra, validated live) [fcfffac + 4433f1d]
- **Founder asked to do the pooler part** (not just hand it off). Claude coded core/db.py directly (small, delicate, needs pgbouncer-semantics precision + live validation) rather than delegating.
- **What:** make_engine adapts when config db_pool_mode == "transaction" (env DB_POOL_MODE): NullPool (external pooler owns pooling), psycopg prepare_threshold=None (no server-side prepared statements across rotating backends), and statement_timeout via a per-transaction `SET LOCAL` in an engine "begin" event. Default stays "session" => zero change to current prod; session path byte-for-byte unchanged.
- **Validated live against the real Supabase :6543 transaction pooler** (probe, both modes): connect OK, multiple checkouts / multi-query OK (prepared-statement path safe), statement_timeout=20s enforced in BOTH modes. KEY FINDING: neither a connect-time `SET` nor the `options` startup param is honored by the :6543 pooler (it reported the 2min default) — SET LOCAL per-transaction is what works. Caught only because Claude tested before shipping.
- **Founder flip (Render, when scaling):** set DATABASE_URL to the ...pooler.supabase.com:6543 transaction URL + DB_POOL_MODE=transaction. Removes the 15-slot session-pool ceiling. Not urgent (prod is within budget on session mode). Auth note: OAuth/auth is DONE + live as of 2026-07-13 (founder finished Supabase config).

### Browse-nav lag + transaction-pool leak fix — gpt-5.6-terra @ ultra [dd82f98]
- **Lag (friend-reported):** tapping a footer browse link "hangs". Diagnosed: /internships, /jobs, /remote are force-dynamic (block on API fetch) with NO loading.tsx, so nav showed nothing until the round-trip. Codex added loading.tsx skeletons (match OpportunityLandingPage max-w-3xl) -> instant tap feedback. (/research is static, already instant; companies/competitions already had them.)
- **Pool leak (root cause of the rollback):** Claude reproduced it — under CONCURRENT load the transaction-mode NullPool config left connections idle-in-transaction; prod showed 12 stuck 7-21 min. Fix (core/db.py, terra): transaction mode now uses a bounded QueuePool (pool_size 5 + max_overflow 10) with pool_reset_on_return='rollback' instead of NullPool; and BOTH modes now arm idle_in_transaction_session_timeout=120s (SET session / SET LOCAL transaction) as a hard safety net so Postgres auto-kills any transaction idle >120s (also protects session mode from the leaked-session class of bug hit earlier this session).
- **Claude validated live against the real :6543 pooler:** both timeouts armed (20s stmt / 120s idle) in both modes; concurrent load's transient idle-in-txn fully clears; ruff/black + session-mode regression (9) + frontend build green. Merged as sohan1611. TRANSACTION POOLER IS NOW SAFE TO RETRY: founder can re-flip DATABASE_URL->:6543 + DB_POOL_MODE=transaction in Render; the idle guard prevents accumulation.

### Recently viewed + pooler re-flip verified — gpt-5.6-terra @ ultra [6f8863d]
- **Transaction pooler re-flip verified healthy in prod:** founder re-set DATABASE_URL->:6543 + DB_POOL_MODE=transaction. Claude checked: all endpoints 200, 10 rapid /feed OK, idle-in-transaction=1 (age 0s) vs 12 stuck 7-21min before — leak gone; total backends 17 (multiplexing); /for-you ~1s (2.9s was a cold one-off); no console errors. The QueuePool-rollback + 120s idle guard fix holds.
- **Recently viewed (feature):** Codex delivered lib/recentlyViewed.ts (localStorage external store — useSyncExternalStore on the raw string + memoized parse for referential stability, dedupe/cap 12, SSR-safe), RecordOpportunityView leaf on the detail page, and a self-hiding "Recently viewed" strip on the home feed. Frontend-only, works signed-out.
- **Claude verified:** lint/tsc/build green; store logic reviewed (no infinite-loop / hydration-mismatch risk). Merged as sohan1611.

### Notification center + Most viewed + Polish + Currency — gpt-5.6-terra @ ultra [cdf6580, bdbe9f7, 648e403, 9ac8f41]
- **Founder:** "proceed with all 3 one by one" (notification center, trending/most-viewed, polish) + add currency-by-country on pricing (India->INR, else->USD; amounts + payment deferred). Built + verified + merged one at a time.
- **① Notification center [cdf6580]:** surfaces the alerts already emailed (closing_soon/instant_alert/daily_digest/weekly_report) in-app. Codex: additive migration c4d8e1f6a2b3 (notifications.read_at) + api/notifications.py (GET /notifications with per-type server-rendered copy joined to opportunity+company, /unread-count, POST /read; all auth-gated + user-scoped; also handles the legacy `digest` type) + a header NotificationBell (unread badge, popover, mark-read-on-open, 60s badge poll, race-guarded optimistic clear) + /notifications page. Claude applied the migration to prod; **fixed an ambiguous `ORDER BY created_at`** (opportunities.created_at collided with the coalesce label -> relabeled notification_created_at); 6 tests pass; lint/tsc/build green.
- **② Most viewed [bdbe9f7]:** honest-by-design discovery. Codex: additive migration d8f3a1c6b4e2 (opportunity_view_counts counter table, kept off the wide opportunities row) + api/trending.py (POST /opportunities/{slug}/view — per-IP rate-limited, fail-safe upsert; GET /trending — roles, active, views>=3, desc) + RecordOpportunityView also pings the counter once per session per slug (sessionStorage dedupe, fire-and-forget) + a MostViewed strip that renders ONLY on the clean default feed AND only when >=4 qualifying items exist, so low traffic shows nothing (no fake trending). Claude applied migration; 4 tests pass; lint/tsc/build green.
- **③ Polish [648e403]:** frontend-only, 3 focused items after auditing (a11y baseline already strong — global reduced-motion, icon aria-labels, img alts all present). Skip-to-content link (targets #main-content, since pages own their <main>); CompanyFavicon fallback CHAIN (logo_url -> Google favicon -> monogram, via inner-component remount keyed on company identity — no setState-in-effect) instead of dropping straight to the monogram on a dead logo_url; warmer /internships /jobs /remote empty state + a "Browse all opportunities" action. lint/tsc/build green.
- **④ Currency by country [9ac8f41]:** frontend-only, no invented USD amount. Extracted the inline country store from LocationScope into a shared lib/country.ts (readStoredCountryCode/storeCountryCode/subscribeStoredCountry + useStoredCountryCode + useCurrency); currency = INR for India/unset (SSR-safe home-market default) or once no country chosen, USD once a non-India country is picked. PricingPlans: INR shows ₹ as before; USD shows "USD pricing coming soon" (no number) + a currency note + waitlist CTA (INR checkout only offered to India). Claude verified LIVE in the browser preview: default ₹ state correct; setting aspirova.country=US switched to the USD treatment live (no reload) with zero console/hydration errors. lint/tsc/build green. Merged as sohan1611.

### GitHub Actions minute clamp — gpt-5.6-terra @ ultra [a54adf8]
- **Founder alert:** GitHub emailed "90% of Actions minutes used" (2,717/3,000, 18 days to Aug 1 reset). Asked to fix.
- **Diagnosis (Claude, gh run data):** TWO drains. (1) **Crawl Tier 1 is BOTH expensive AND broken** — 2×/day, and the last 12 runs had ZERO successes: they hit the 40-min job `timeout-minutes` and get **cancelled (full 40 min billed)** or fail at ~30 min. ~2,280 min/mo alone (~76% of budget) on a crawl that never completes. Phase-5 crawl load (Amazon, SmartRecruiters, competitions, research, Unstop) pushed the un-capped ATS step past 40 min. (2) CI runs on every push (incl. docs-only commits like collab-log) with no concurrency guard — 41 runs in the recent sample. My earlier "3,000 is enough" assumed the crawl finished in ~10-15 min; it doesn't.
- **Founder chose "Balanced"** (steady-state fix; accept a possible small overage this cycle vs a $0 Actions budget hard-block).
- **Codex delivered (config-only, 2 YAML files):** crawl-tier1 2×/day→1×/day (`0 1 * * *`, before the 03:30 digest), job cap 40→30, **20-min cap on the ATS step** (a runaway can't eat the whole budget), corrected stale header comment; ci.yml `concurrency: cancel-in-progress` + `paths-ignore` (docs/`.agents`/`**.md` skip full CI — also saves the backend job's prod-DB pytest egress).
- **Claude verified:** both files valid YAML with the exact asserted values (pyyaml parse); merged as sohan1611 before the old 12:00 UTC cron could fire again (schedule reads from default branch → today's 2nd crawl averted, ~40 min saved immediately). Merged branch deleted.
- **STILL OPEN (flagged to founder):** the crawl **does not complete** — the caps only make failures cheaper. A real crawler-perf pass is needed so the ATS crawl finishes inside ~20 min (profile/parallelize ingest, or shard the tier-1 company set across runs). Recorded in memory so "3,000 is enough" isn't re-claimed until it runs green.

### Crawl: three real perf bugs fixed + scope fix + abroad-remote toggle [77d5aae, 821df01, 069b9b3, 6ebef9a, a30de59, 1866e90, 1c62de4]
- **Trigger:** GitHub "90% of Actions minutes" alert + a daily "Run failed: Crawl Tier 1" email. Diagnosed (gh run history): the crawl ran 2×/day and **failed EVERY run** for weeks, burning the full 40-min timeout (~2,280 min/mo ≈ 76% of budget on a crawl that never completed). The daily email IS that failure — there is no notification config; **never suppress it, fix the crawl**.
- **Bug 1 — last_seen_at [77d5aae]:** every already-seen listing did `opportunity.last_seen_at = func.now()` per ORM object → one non-batchable UPDATE per listing @ ~250ms RTT (Actions US → Supabase Mumbai). databricks 788 listings = 229s. Fix: `ingest_one` collects seen ids (optional `seen_opportunity_ids`, falls back to old behaviour when absent so the aggregator/tests are untouched); `crawl_company_board` flushes ONE chunked bulk `UPDATE ... WHERE id = ANY(:ids)` per company. **Measured: databricks 229s→31s, stripe 169s→32s.**
- **Bug 2 — search_tsv trigger [6ebef9a]:** fired on EVERY opportunities UPDATE (4× to_tsvector + per-row company lookup) even for is_hidden/last_seen_at bumps. Guarded to text-relevant columns (title/summary/description_raw/company_id); INSERT always computes. Migration applied to prod.
- **Bug 3 — recompute_hidden [a30de59]:** `UPDATE opportunities SET is_hidden=...` with **no WHERE** → full-table rewrite every crawl → exceeded the 20s statement_timeout. Added `WHERE is_hidden IS DISTINCT FROM <expr>`. **Measured: 20s+ timeout → 4.7s.** Bugs 2+3 were LATENT — invisible until the crawl got fast enough to reach those tail steps.
- **Caps [821df01]:** 2×/day→1×/day (`0 1 * * *`); job 40→38m; ATS step cap 28m. CI got `concurrency: cancel-in-progress` + `paths-ignore` (docs).
- **Prod data cleanup:** 27 fake **"Fake Job" opportunities were LIVE in the feed** under 8 leaked `test-isolation-co-*` companies (0 bookmarks). Deleted transactionally (children→parents; FKs are NO ACTION). Root cause: `test_runner_isolation.py` MUST commit fixture data (the runner uses its own sessions) and a hard-killed run skips its `finally`. **[069b9b3]** adds a session-scoped autouse purge in `tests/conftest.py` so it can never accumulate; also fixed the isolation fake's `ingest_one` signature (regression from 77d5aae — that suite wasn't run before merging; Claude's miss).
- **Scope fix [1866e90]:** founder reported "select India, everything shows". `location_scope_filters`'s remote arm was unconditional (`country==X OR is_remote`), so every "Remote - US" (country='US') matched the India feed. Measured: India returned 4,977 rows, only **390** actually India, **2,918** foreign-remote. Restricted the remote arm to `country IS NULL` in both scopes. **Verified prod: 4,977 → 2,059.**
- **Abroad-remote toggle [1c62de4]:** founder asked for "stay in India, work remotely abroad". Claude flagged that "Remote - US" means remote WITHIN the US (1,949 of 2,918) and usually needs local work authorization — so it ships OPT-IN, default OFF, additive, labelled "Remote roles tied to another country often require work authorization there". No work-from-India promise. The genuinely location-agnostic remote (1,669, country NULL) already shows by default.
- **STILL OPEN — new failure:** run 29420491392 processed only 5 boards then went **silent for 24 min** → 28-min ATS timeout. A HANG, not slowness. Ruled out by measurement: the tsv guard (0.85s/200 rows — Claude's first theory, tested and WRONG), greenhouse HTTP (15s timeout), `health()` (cached attr), `_prefetch_ats_boards` (waits + shutdown(wait=True)), and statement_timeout (proven live — compute_hidden raised QueryCanceled from it). Prime remaining suspect: **session-pooler client-slot queueing** (PgBouncer accepts the TCP conn then queues for a server slot, which `connect_timeout` does NOT bound → indefinite silent wait); Claude was running local scripts against prod at that exact time and may have contended for slots. The two runs before it did NOT hang → possibly transient. **Founder chose to let the free 01:00 UTC scheduled run be the test rather than burn more overage.** Needs live `pg_stat_activity` during a hang to confirm.

### Supabase "23% success rate" Postgres errors — diagnosed, NOT ours, no fix possible
- **Founder screenshot:** 2,698 Postgres ERRORS / 3,144 requests (23.1% success) over 24h, plus Disk 86% on the t4g.micro.
- **Root cause (from the founder's Postgres logs — Claude asked for them rather than theorising a 3rd time, and was right to):** every log line is `schema "pg_pgrst_no_exposed_schemas" does not exist`, repeating every ~30s → ~2,880/day, matching the 2,698 almost exactly. **NOT the crawler** — Claude's precedent-based hypothesis (memory recorded an earlier 2,741/53.4% caused by crawl cancellations) was WRONG.
- **What it is:** Supabase disables the Data API by pointing PostgREST at the sentinel schema `pg_pgrst_no_exposed_schemas`. PostgREST keeps re-introspecting every ~30s and logs an ERROR because the schema does not exist. The founder's setting is CORRECT (Data API already disabled); the spam is Supabase's own implementation side-effect. Cosmetic — nothing in Aspirova is broken.
- **Attempted fix FAILED (by design):** `CREATE SCHEMA pg_pgrst_no_exposed_schemas` → `ReservedName: the prefix "pg_" is reserved for system schemas`. Supabase deliberately chose an uncreatable name so nobody can conjure it and silently re-expose the Data API. Good design; our workaround was impossible.
- **Verified safe posture:** Aspirova does NOT use PostgREST at all (every `.from(` in the frontend is `Array.from()`; supabase-js is used only for `auth.*`; all data goes via FastAPI). RLS is ENABLED on all 21 public tables, 0 disabled. Do NOT "fix" this by setting Exposed Schemas to `public` — that would publish users/subscriptions/bookmarks/resume_profiles to the anon key (which ships in the frontend bundle), leaving only RLS between them and the world.
- **Decision:** accept the noise; filter that string when reading logs; report to Supabase as their bug. DO NOT re-investigate.
- **Disk 86% (separate):** DB is only 487MB (opportunities 371MB, raw_listings 97MB), no replication slots pinning WAL, 43GB cumulative temp spill since 2026-05-22. Claude's "the feed query spills" theory was also WRONG — measured: 833kB quicksort, 32ms, no external merge. The 86% is most likely WAL/PITR retention; the per-listing last_seen_at UPDATE storm + compute_hidden's full-table rewrite (both fixed 2026-07-15/16) were generating enormous WAL for weeks, so growth should slow. Watch it.

### Comprehensive audit: CI was NEVER green + light-only + account polish + wordmark [826e5b7, 577bca7]
- **Founder asked for a very comprehensive bug sweep + account/profile polish (study premium sites) + remove Appearance (light only) + the queued loading wordmark.**
- **BIGGEST FIND — CI has never once passed.** A full local suite run showed **4 failing tests on master**, and `gh run list` showed the last **20 CI runs ALL "cancelled" at ~919s = the backend job's 15-min timeout**. The suite runs against the real Supabase-Mumbai DB from US runners (~250ms/round-trip), so its ~5-7min local runtime exceeds 15min there and is killed every run. CI validated NOTHING — which is exactly how the two ingest_one signature regressions + a db_pool stub break reached master unnoticed. **Fix [826e5b7]:** test_db_pool stub missing db_pool_mode (added by the pooler work) → add it to the shared helper; test_crawler_reliability's fake ingest_one predated seen_opportunity_ids → add the kwarg (swept tests/ — aggregator fakes don't get it, no change); ci.yml backend timeout 15→25 with a comment. Full suite now **349 passed, 0 failed**. Claude did these 3 fixes DIRECTLY (Codex was hitting Windows-sandbox spawn errors AND red CI was a fire) — test-signature fixes, no app logic. First real CI run triggered to confirm.
- **Light-only + account + wordmark [577bca7, via Codex]:** (A) ThemeProvider forcedTheme="light" (keeps useTheme working for sonner while .dark never applies, so dark: utils stay inert without ripping them out), deleted AppearanceSection + sidebar entry, ?section=appearance→Profile. (B) Account identity card now shows REAL stats from getBookmarks (Saved, In-progress=applied/interviewing/offer, Member since; skeleton while loading, hidden on error) + a profile-completeness cue (name/college/grad-year, N of 3) — no invented data. (C) BrandLoading: serif "Aspirova" letters animate in one-by-one (pure CSS, per-letter delay) above the existing card skeletons in every listing loading.tsx — ZERO added latency (only shows during a real load), respects prefers-reduced-motion via a dedicated rule that shows the letters static (never stranded invisible). Claude verified in browser: light forced (no dark class, no toggle), no console errors, keyframes ship; lint/tsc/build green.
- **Honesty note:** founder asked to "study" LinkedIn/Amazon/JPMorgan/Morgan Stanley account pages — those are behind login; only marketing homepages are reachable. Claude did NOT fake a study; applied known premium account-settings patterns grounded in real API data instead.
- **Signed-in account stats verified by code+build only** (no test account) — same limitation as Saved/Notifications.

### Crawl hang + CI EDBHANDLEREXITED: pool-mode drift [c685395]
- **Founder reported both failing again on f12a006.** Claude's earlier "the hang was transient / probably my own local scripts" call was **WRONG** — it recurred at 04:10 UTC with nobody touching prod, identical fingerprint both times: weride→angellist→wealthfront→spotify→**stripe**→ ~23 min SILENCE → 28-min ATS step cap (runs 29420491392, 29554050922). The one green run did 54 boards.
- **The tell:** 23 min of silence with **NO QueryCanceled**. statement_timeout is 20s, so it MUST have fired if applied — its absence proved statement_timeout was never applied. Combined with CI's `EDBHANDLEREXITED` (a Supavisor TRANSACTION-pooler error) => GitHub Actions is on the :6543 transaction pooler.
- **Root cause:** make_engine picked transaction behaviour from `settings.db_pool_mode`, but NEITHER crawl-tier1.yml NOR ci.yml sets DB_POOL_MODE — they pass only DATABASE_URL (the :6543 secret). So Actions ran SESSION-mode settings against a TRANSACTION pooler: connect-time `SET statement_timeout` is silently ignored by :6543 (proven live earlier this session — only SET LOCAL works) => no query timeout => unbounded hang; and server-side prepared statements stayed on => dropped handlers => EDBHANDLEREXITED. The local .env is :5432, which is exactly why it passed locally and only broke in CI/the crawl. **The design flaw was Claude's:** port and DB_POOL_MODE were two sources of truth that drifted.
- **Fix [c685395]:** derive transaction mode from the URL **port** (:6543 => transaction) — the port IS the pooler contract. Explicit DB_POOL_MODE=transaction still forces it on any port; malformed/blank URL falls back to session. Makes the drift class impossible. 8 pool tests incl. ":6543 + DB_POOL_MODE unset => transaction". No workflow/timeout/pool-size changes.
- **NOTE:** the GitHub DATABASE_URL secret is write-only (founder can't read it to confirm the port) — the autodetect makes that moot: correct either way, a no-op if it's :5432.
- **UNVERIFIED / RESUME HERE:** the next scheduled 01:00 UTC crawl + the CI run on c685395 are the proof. If the crawl still hangs after stripe, the :6543 theory is wrong and the next suspect is the airbnb board specifically (it is always board #6).

### Pool-mode fix PROVEN live + fail-fast guard [c685395, 910d988]
- **Verified the pool-mode fix against the REAL :6543 transaction pooler** (not just unit tests): with DB_POOL_MODE unset, a :6543 URL now auto-selects transaction mode and `statement_timeout` reads **20s** inside a transaction (before: silently the 2min default -> the class of unbounded hang). This is the definitive proof the CI/crawl root cause is fixed.
- **Added a fail-fast guard [910d988]:** `verify_connection_guards(engine)` opens one short transaction and asserts statement_timeout == STATEMENT_TIMEOUT_MS (via pg_settings ms, not string matching); raises immediately with an actionable message otherwise. run_tier calls it right after make_engine, BEFORE any board work. So a future pool/pooler mismatch fails the crawl in ~2s with a clear cause instead of hanging to the 28-min cap. Verified live: passes against real :6543 in 2s; raises on a wrong (2min) value. 14 run_tier + 10 pool tests green.
- **RESUME-HERE now RESOLVED at the code level** — the :6543 theory is confirmed correct (statement_timeout proven applied). The next scheduled 01:00 UTC crawl should complete green; if it still hangs after stripe despite this, THEN look at the airbnb board (#6) — but the timeout guard would now catch any such hang in seconds regardless.
- **Process note (Claude, honest):** I twice merged before running ALL affected suites (the isolation + run_tier fakes). CI never caught it because CI itself was timing out. Both are now fixed: CI timeout raised (25m) so it actually runs, and the guard converts the specific silent-hang failure mode into a loud fast one. Standing lesson: run the full/affected suite before merge; don't close an investigation on a comfortable theory without a second data point.

### Crawl time-box + fair ordering [7ad06be] + Codex tooling fix
- **The pool-mode fix ended the hang** (confirmed: run 29629671955 flowed through 28 boards). But two problems remained, both measured live: (1) the ATS step ran to the 28-min GitHub cap and was SIGKILL'd (failed run, tail steps skipped, failure email), and (2) fixed board order meant it always died ~board 28 and the back half was NEVER crawled — 28 of 52 tier-1 boards crawled in 24h, 27 stale/never. Plus a real backlog (4,608 opps touched in 24h) from days of failures (databricks 7min, a one-time catch-up cost).
- **Fix [7ad06be]:** run_tier now (a) soft-time-boxes the ATS loop (DEFAULT_ATS_MAX_SECONDS=1320, env CRAWLER_ATS_MAX_SECONDS) mirroring the aggregator deadline — breaks cleanly before the hard cap so the run finishes GREEN and tail steps run; (b) orders ats_jobs stalest-first (SourceState.last_crawled_at ASC nulls-first, one set-based read) so each time-boxed run covers the boards the last one missed -> all 52 covered over ~2 runs, backlog clears. **Claude implemented this DIRECTLY** (Codex was down) and — the lesson applied — ran the FULL suite before merge: **360 passed, 0 failed**; added 2 tests (clean stop at budget; never->oldest->freshest order). Caught + fixed own mid-edit slip (mangled the aggregator max(0.0,...) clamp).
- **Claude was WRONG earlier** calling the hang "transient" — it recurred with an identical fingerprint. Root cause was the pool-mode drift (fixed), and UNDER it the time-box/coverage problem the hang had masked. Lesson logged: don't declare "done" before a real end-to-end run.
- **Codex tooling fix:** Codex auto-updated its binary into a new bin/<hash>/ dir but did NOT copy its helper exes (codex-windows-sandbox-setup.exe etc.) — they stayed in the old dir, so the new codex.exe hit "helper program not found" and could not read/edit/run anything. Fixed by copying the 3 helper exes into the new binary's dir; verified Codex can edit a repo file again. NOTE: this will likely RECUR on the next Codex update (new dir, helpers not copied) — re-copy or reinstall. The pwsh "Access denied" line is a benign pre-existing quirk (Codex edits via apply_patch, not the shell).
- **UNVERIFIED / RESUME HERE:** the next scheduled 01:00 UTC crawl is the end-to-end proof — it should finish GREEN (time-boxed) and the stale-board count (currently 27) should drop over ~2 runs. CI on 7ad06be should also be green.

### Growth #1: +21 verified brand company boards [dffcd86]
- **Founder chose "growth & product", user "both equally" (India + global).** Key finding from a coverage assessment: the feed was skewed to US full-time tech jobs (greenhouse/ashby ~7,300) while the product is FOR STUDENTS — only 330 India roles, 8,505 jobs vs 1,300 internships. So the highest-leverage growth is student-relevant supply (India brands + internships).
- **Delivered:** 21 brand-name company boards, EACH token verified live (HTTP 200 + non-empty jobs, Claude probed ~80 candidates) before adding — no dead boards. India (6): Razorpay, PhonePe, CRED, Postman, Druva, Zeta. Global student-magnets (15): Datadog, Palantir, Harvey, Roblox, Verkada, Sierra, Decagon, Duolingo, Gusto, Temporal, Supabase, Abridge, Character.AI, Squarespace, Resend. Added to scripts/seed_companies.py (via Codex, terra/ultra) in the correct per-ATS lists; seed run on prod (authorized): 21 created, companies 1920->1941, all never-crawled so the stalest-first crawl picks them up FIRST next run (~2,300 new live roles).
- **Honest constraint logged:** India is hard to grow via the 4 supported ATSs — most Indian firms use Darwinbox/Keka/Workday/own. 6 India brands is near the ceiling of what's cleanly addable now. To make India a real strength needs GROWTH #2: either a Darwinbox/Keka adapter OR an India-internship source (Internshala-style — check ToS/robots first, only if cleanly permitted). Not yet started.
- **Codex tooling:** works again after copying the sandbox helper exes into the new binary dir; will recur on next Codex update.

### Growth #2 India: clean-source ceiling + Darwinbox deferred
- Checked before building: **Internshala robots.txt forbids** /api/, /internship/search/, /job/search/, all query strings -> NOT built (ToS rule). **Unstop jobs** endpoint works (~10k) but is LOW QUALITY (telecaller/pre-sales/field-marketing spam at no-name firms) -> NOT added (would wreck the quality bar; Unstop internships/competitions stay, they're decent). Indian firms mostly aren't on greenhouse/lever/ashby (use Darwinbox/Keka/Workday/own), so the 6 quality India brands added in Growth #1 are near the clean ceiling.
- **FUTURE SPIKE (founder can greenlight):** Darwinbox is the real India unlock (Swiggy/Nykaa-tier use it) — investigate whether it exposes a clean public per-company jobs API; if so, build an adapter. Deferred; founder chose to pivot to discovery first.

### Discovery #1: "Early career" filter [9b2490e]
- **Grounded in a measurement:** 53%+ of active jobs (4,563 of 8,505) are explicitly senior/staff/principal/director/manager — roles a STUDENT can't get; only 4% explicitly early. The product is for students, so the feed was "blunt" by burying student-relevant roles under senior noise.
- **Delivered (via Codex terra/ultra):** an opt-in Experience filter. filters.py SENIOR_TITLE_PATTERN + experience_filters() excludes senior titles (coalesce so null titles are kept, not dropped); feed.py + search.py take experience=early; frontend adds an 'All levels / Early career' Select in SearchFilters wired to the URL param + active-filter chip + hasActiveFilters, composing with every existing filter.
- **Verified END-TO-END** (Claude ran a local backend + browser, not just tests): all jobs 8,505 -> experience=early 2,762 (senior removed, matches prod), invalid value -> 422; the 'Early career' chip + 'Filter 1' badge render, feed shows internships + non-senior roles only, page loads clean. 10 feed-filter tests + lint/tsc/build green. Note: a rare abbreviation like 'RVP' isn't caught by the whole-word regex — negligible, tunable.
- **Natural follow-ups (not yet done):** default-rank internships/early-career higher for signed-in students; saved searches + alerts (retention). Darwinbox India spike still deferred.

### Discovery #2 (part A): student-first default feed [947711b]
- Default feed sort is now "student": internships (0) -> early/ambiguous roles + competitions (1) -> explicitly-senior jobs (2) [reuses SENIOR_TITLE_PATTERN], then source diversity + recency. "Newest"(recent) + "Closing soon"(deadline) are explicit opt-outs with removable chips; the default keeps the URL clean so the trending/most-viewed clean-default gate still fires. Verified end-to-end (local backend + browser): default feed = internships first; sort=recent = pure recency; 422 on bad sort; "For students" default label, no console errors. 13 feed tests + build green.
- DESIGN NOTE: applied to ALL (incl. signed-out) because the feed is SSR and can't cheaply know signed-in state pre-render; also serves signed-out students. Founder can request signed-in-only gating (needs client-fetched feed) later.

### Discovery #2 (part B): saved searches + email alerts [296b426, 107de94]
- **Completes #2 (retention loop).** Part i (296b426, Codex): auth-gated saved-search CRUD — POST/GET/DELETE/PATCH under user_id scoping, `with_for_update()` race guard on a 25-per-user cap (409 on limit), `_validated_params` (422 on invalid). Part ii (107de94, Codex): `pipeline/saved_search_alerts.py` daily worker — for each `alerts_enabled` saved search it emails the newly-seen matches (`first_seen_at` in the half-open window `(last_alerted_at or created_at, now]`, ordered newest-first, capped at 10), advances `last_alerted_at`, and records a `saved_search_alert` Notification (sent/failed). Wired a `--saved-search-alerts` flag into notification_worker + a daily step in notifications-digest.yml. Reuses feed/search semantics via a new `saved_search_base_filters` helper (mirrors base_filters exactly). All dynamic email content `html.escape(quote=True)`; branded email_layout.
- **Bug Claude caught in review — NOT a worker bug, a test-isolation flaw:** Codex's 2 "assert zero emails" tests seeded one saved search with `params={"category":"internship"}` and asserted the user got no mail. They failed against the SHARED prod DB because the worker (correctly) also matches the thousands of REAL internships first-seen in the same window -> an email IS sent. The worker is right; a broad saved search SHOULD alert on new real internships. Fix was in the tests: give each an isolating `q` FTS token that only its own seeded row carries, so real prod rows can't be candidates. Also hardened `html_escapes` (same latent fragility — its seeded row could be pushed out of the top-10 by fresher prod internships as the fixed `now` date recedes).
- **FTS gotcha logged:** the token-in-title trick failed for the `<script>` title — the default text-search parser treats `<script>` as an HTML *tag* and DROPS everything after it (`to_tsvector` -> `'research':1` only), so a trailing token never indexes. Fix: put the token FIRST in the title. Verified by probing `to_tsvector/websearch_to_tsquery` directly against the real DB before trusting it.
- **Verification:** the 5 originally-passing tests were robust (they assert `==1`/`==0` per unique per-test user, or a disabled search); the 2 negatives + html_escapes now isolated -> 7/7 green. Ran the adjacent regression suites too: test_notifications + test_notifications_api + test_saved_searches + test_saved_search_alerts = **39 passed**. ruff + black clean. Author sohan1611, no Co-Authored-By. Did NOT run the full backend suite locally (egress discipline — don't hammer prod); CI on 107de94 is the full-suite gate.
- **Fixture-safety note:** the worker `session.commit()`s per saved search inside a loop, but the test's `db_session` binds a Session to a connection already in a transaction, so SQLAlchemy's default `conditional_savepoint` join mode turns each commit into a SAVEPOINT release inside the outer transaction — the final `transaction.rollback()` still discards everything. No repeat of the "27 Fake Job rows leaked to prod" class here (that was the separate runner-isolation test, which commits via its own engine).
- **#2 DONE.** Next: #3 Darwinbox India spike (founder deferred to "tomorrow"), then #4 Razorpay go-live (founder's KYC/live keys — Claude preps only, never handles live keys).

### #3 Darwinbox India spike: VERDICT = NOT VIABLE, DO NOT BUILD. Keka is the real unlock.
- **CI note first:** the earlier "CI 107de94 couldn't finish" was wrong — run 29772722215 on 107de94 was `completed/success` (backend + frontend both green). What failed (exit 4) was Claude's backgrounded `gh run watch` command, not the CI run. #2 is fully green, merged, pushed; 7/7 alert tests re-confirmed locally on resume.
- **Darwinbox findings (all probed live, not assumed):**
  - Tenant subdomains are NOT guessable: `nykaa/meesho/dream11/lenskart/mahindra` all return the *same* `"Invalid subdomain"` error as a deliberately bogus control tenant. The earlier HTTP-200s were a wildcard catch-all serving an empty SPA shell — **200 is meaningless on this host**, which is exactly the trap the "verify live before adding" rule exists to catch.
  - The candidate portal is **login-gated**: `swiggy.darwinbox.in/ms/candidate/careers` redirects to `/ms/candidate/candidate/login`, renders empty, and its only data call `GET /ms/candidateapi/getLandingPage` returns **500** unauthenticated.
  - Real tenants found via search (`dbox, hetero, mordorintelligence, yesforyou(YES Bank), upgrad`) return **403 "Attention Required! | Cloudflare"** to a plain HTTP client — active bot protection.
  - Tenant `robots.txt` 302s to the vendor marketing homepage (no tenant robots file).
- **Ruling (binding):** ingesting Darwinbox would require either evading Cloudflare bot protection or authenticating as a candidate. Both are out of bounds under the standing no-ToS-violation / no-evasion rule — same class as the Internshala robots block. **Adapter NOT built. Spike closed.**
- **Constructive replacement — Keka, validated end-to-end:** Keka is India's largest new-gen HRMS (**5,400+ Indian businesses**), and unlike Darwinbox it is cleanly open:
  - Tenant `robots.txt` is explicitly permissive *exactly where the jobs are*: `Disallow: /` + `Allow: /careers` + `Allow: /careers/`.
  - Public JSON API, entirely **under `/careers/`** (so inside the robots Allow): `GET /careers/api/embedjobs/default/active/<tenantUuid>` -> full active-jobs array (id, title, description, location, experience, jobType); `GET /careers/api/organization/default/careerportalinfo` -> company name + the tenant UUID; also `/departments`, `/colorcode`.
  - **Plain `curl` gets 200 + real JSON** — no Cloudflare block, no login. This is the exact gate Darwinbox failed.
  - Live tenants confirmed: `zluri`, `qualminds`, `wealthindia`. Apply URLs are clean: `/careers/jobdetails/<id>`.
  - Fits the existing architecture unchanged: same per-company-board pattern as greenhouse/lever/ashby/smartrecruiters, so it reuses the seed-list + stalest-first crawl machinery.
- **Open implementation question for whoever builds it:** the tenant UUID currently has to be read out of `careerportalinfo` (it appears embedded in `careersBackgroundPath`), which may be empty for tenants with no banner image — a robust adapter needs a reliable UUID source (likely the `/careers/api/embedjobs/js/<uuid>` reference in the page). Flagged, not solved.
- **Cost caveat (founder cares):** every new board competes for the SAME time-boxed crawl (`ATS_MAX_SECONDS=1320`, 1x/day, 3,000 Actions min/mo). Keka boards would not blow the budget — the crawl self-limits — but they DO dilute per-run coverage of existing boards. Sizing the seed list is a founder call, not an automatic yes.

### Growth #3: Keka ATS adapter — the India unlock Darwinbox couldn't be [1ee21a5]
- **Built after #3's Darwinbox verdict.** Founder greenlit "build Keka adapter now". Implementation by Codex (terra/ultra); architecture, live research, seed-list verification and ALL verification by Claude.
- **Delivered:** `crawlers/keka.py` (two-step: resolve tenant UUID from `/careers/` HTML, then `GET /careers/api/embedjobs/default/active/<uuid>`), registered in `runner.ATS_ADAPTERS` as `keka`, `KEKA_COMPANIES` seed list, offline fixture + 7 mocked tests. `board_token` = tenant subdomain, so NO schema change was needed.
- **Architect decisions made BEFORE handoff (Codex would have guessed wrong on both):** (1) `jobType` is NOT an internship signal — Keka's own widget JS does literally `jobType == 2 ? "Full Time" : "Part time"` — so category comes from `classify_category(title)` like every other adapter; (2) the tenant UUID is regex-extractable from the careers HTML, which is what let board_token stay a single string.
- **Seed list — 9 boards, every one probed live before it was written down:** clickpost, sjinnovation, zluri, qualminds, talentformula, arisinfra, lumel, auracloud, wealthindia = **93 jobs, 65 India roles, 10 internships** (~+20% on the ~330 India roles). **dpdzero was DROPPED** even though search surfaced it — no extractable UUID, so it would have shipped as a permanently-empty board. Same discipline as the Growth-#1 "verify every token live" rule, which is exactly what caught Darwinbox's meaningless wildcard 200s.
- **Company domains deliberately left NULL, not invented** (no-fake-data rule). `companies.domain` is nullable and its unique index is partial (`WHERE domain IS NOT NULL`), so multiple NULLs don't collide — verified before accepting. Cost: these 9 get no logo until real domains are added. Cheap, optional follow-up.
- **Verification (Claude, not Codex):** Codex could run NOTHING this session — `CreateProcessAsUserW failed: 5`, the sandbox-helper breakage recurring after its auto-update — so it delivered entirely UNVERIFIED code. Claude ran everything: ruff clean, black (reformatted 3 files, as always), 7/7 keka tests, **85 adapter tests** as regression. Then the check unit tests cannot make: **live end-to-end against the real API** — ClickPost 13 fetched (2 internships), SJ Innovation 21 (7 internships), `health=ok`, locations like `Bengaluru, KA, India`, real `posted_at`, correct `/careers/jobdetails/<id>` apply URLs, 800-6700ch descriptions.
- **False alarm logged:** a title rendered as `Intern Network Engineer (Onsite) � Goa`. Checked the codepoints rather than assuming — `0x2013`, a correct EN DASH. Pure Git Bash console artifact, data is clean. (Worth remembering: don't "fix" encoding based on terminal output.)
- **NOT seeded to prod.** The adapter is INERT until companies exist — merging is safe, but seeding is a separate founder-authorized action, and seed sizing trades against the time-boxed daily crawl (`ATS_MAX_SECONDS=1320`, 3,000 Actions min/mo).
- **UPDATE — SEEDED TO PROD (founder authorized "seed all 9").** Checked for slug collisions FIRST: the seed script overwrites `ats_type`/`ats_board_id`/`domain` on any existing company whose slug matches, so a collision would have silently hijacked a real company to Keka and nulled its domain. Zero collisions found, then seeded. Result: `created source: keka`, **9 created / 1941 -> 1950 companies**; source row is `('keka','ats',crawl_tier=1,'keka')`. All 9 are never-crawled, so stalest-first ordering picks them up FIRST on the next crawl.

### Crawl 28-min timeout: ROOT-CAUSED + FIXED + VERIFIED GREEN [7af23ce]
- **The real root cause was NOT "datadog overshot the budget"** (Claude's first read, stated too confidently and then corrected). `_prefetch_ats_boards` fetches every board's HTTP up front at runner.py:717, and `ats_deadline_monotonic` was armed at line 724 — AFTER it. So the budget never covered the prefetch it followed: prefetch took ~3.5min, pushing the effective deadline to ~25.5min instead of the advertised 22. That is exactly why `"ATS time budget reached"` never appeared in any failing log — **the deadline never fired**. datadog finished at 04:39:31 still inside the inflated deadline, so the loop legitimately started another board, which was mid-ingest when the 28min cap SIGKILL'd it (the unexplained 4.6min of silence).
- **Fix:** arm the deadline BEFORE the prefetch so it bounds the phase it claims to bound, and 1320s -> 1080s so the gap to the 28min cap exceeds the slowest single board (datadog alone ingests 8.5min; the deadline only gates whether a board may START).
- **Second, quieter harm:** the SIGKILL skipped every tail step (no `if:`), so `expire_missing` / `compute_hidden` / `cleanup_expired_competitions` never ran — dead listings stayed live in the feed on every failed day. Those three now run on `success() || failure()`, deliberately NOT `always()` (this workflow uses cancel-in-progress; `always()` would fire them on cancelled runs too). The email step was left untouched.
- **VERIFIED on a real run (29805312659): FULL GREEN, all 13 steps, 32min**, inside the 38min job ceiling. `"ATS time budget reached; ending after committed work"` printed — the graceful exit fired by design, not by luck. The aggregator + all three tail steps executed for the first time in weeks.
- **NEW FINDING — throughput is the next real bottleneck:** that green run completed only **7 of 82 ATS boards** before the budget expired (verkada, roblox, gusto, duolingo, temporaltechnologies, squarespace, harvey). At ~2min/board of INGEST (not fetch — fetch is prefetched concurrently), a full rotation takes ~12 days. Stalest-first works, but ~30 boards are tied at never-crawled (`datetime.min`) and Python's stable sort leaves that tie in arbitrary DB order, so a new board can sit unreached for days. Lowering the budget did not cause this; the pre-fix runs managed 8 boards before dying. **Ingest cost per board is the thing to attack next.**

### Keka: ingested to prod, and one quality removal pending founder approval
- Because of the rotation above, the 9 freshly-seeded Keka boards were NOT reached by the green run. Claude ran their ingest directly so the India supply landed the same day: **9/9 boards success, 0 errors, 93 listings -> 91 active opportunities, 10 internships**, and 9 `source_state` rows now exist so the daily crawl rotates them normally.
- Domains: 5 filled from each company's SELF-DECLARED `companyWebsite` in its own Keka portal config, each verified to resolve 200 with a prod collision check against the partial unique index on `companies.domain`; 4 left NULL because they declare none (a guessed domain attaches the WRONG company's logo).
- **QUALITY ISSUE — unresolved, needs founder call:** `wealthindia`'s ONLY listing is literally titled **"Test HR Intern"** (opportunity 38362) — the company's own test posting, now live in the student feed. Claude attempted to delete it + the company and was **blocked by the permission classifier** (prod deletion needs explicit founder authorization, same as the 27 'Fake Job' rows). NOT worked around. Note a seed-list removal alone is NOT sufficient: the company row keeps `ats_type='keka'`, so the daily crawl would re-activate the listing regardless. Removing it needs the company row gone (or its ats_type cleared).

### Board ordering + Keka wave 2 [e75202d]
- **Ordering fix (Codex, reviewed+fixed by Claude):** never-crawled boards all collapsed to the same stalest-first key (`datetime.min`) and tied; Python's stable sort then kept arbitrary DB order. That is why the 9 new Keka boards were never reached by the green run and had to be hand-ingested. Staleness stays primary; `-company_id` added as secondary so the newest-seeded board leads the very next run. A board leaves the tie permanently once crawled, so this cannot starve anything. Sort extracted to `_order_ats_jobs()` so the rule is testable without a DB (6 offline tests incl. determinism-under-shuffle).
- **The regression run is what caught the real problem:** Codex's 6 new tests passed while **5 pre-existing `test_crawler_reliability` tests were broken** by the new required `_AtsJob.company_id` and by company fakes lacking `.id`. Fixed the fixtures; where a test's boards are all never-crawled, ids are numbered DESCENDING so crawl order still equals list order — keeping those tests about prefetch isolation and the time budget instead of silently about ordering. Final: **99 passed**, ruff+black clean.
- **THROUGHPUT CLAIM RETRACTED.** Claude earlier reported "~2min ingest per board, ~12-day rotation" — that was inferred from one run, not measured, and it was wrong. Measured directly with a SQLAlchemy `before_cursor_execute` counter: steady-state ingest is **5 queries per BOARD**, independent of listing count (0.24-0.38 round-trips/listing) — the bulk prefetch + batched commits already work. Cost lives ONLY in NEW listings (datadog had 387). Those were inflated because the crawl had been failing for days and every failure skipped `expire_missing`, so listings kept being retired and re-created as "new". **The backlog was a symptom of the timeout bug, not an independent problem, and should drain now that runs complete.** Local RTT measured at 48.9ms; whether CI pays ~5x that against Supabase-Mumbai is still UNMEASURED and is exactly the `X-DB-Time-Ms` isolation the deferred Lever-2 ruling calls for. Do not act on it before measuring.
- **Keka wave 2:** +4 boards, each probed live for a resolvable UUID, non-empty active jobs, AND screened for test/dummy titles (the WealthIndia lesson): cloudthat (86 jobs/75 India), satsure (26/24), adda247 (8/8), softprodigy (3/2). Seeded + ingested, 0 errors. **Keka now: 13 boards, 210 active opportunities, 24 internships.** Total active internships 1,374 -> **1,388**.
- Domains again only where SELF-DECLARED and resolvable: cloudthat.com, softprodigy.com. `adda247` declares its own Keka careers URL as its "website" — not a company domain, so left NULL rather than becoming a wrong logo.
- **STILL PENDING FOUNDER ACTION:** the `wealthindia` "Test HR Intern" row (opportunity 38362) is still live — prod deletion remains blocked by the permission classifier, correctly. It is the 1 junk title in the Keka set.
- **RESOLVED (founder authorized):** `wealthindia` removed. Seed-list entry deleted FIRST (so no window where a re-seed could recreate it), then the prod rows. First attempt hit `raw_listings_opportunity_id_fkey` — `raw_listings.opportunity_id` was missing from the delete order — but the whole thing ran in ONE transaction so it rolled back with nothing deleted; retried with `raw_listings` correctly placed after `opportunity_sources` (which FKs to it). Deleted exactly: opp_sources 1, raw_listings 1, opportunity 1, source_state 1, company 1 (tags/notifs/views 0). Verified after: company gone, opportunity 38362 gone, **0 junk titles in the Keka set, 0 orphan opportunities**. Keka now **12 boards / 209 active / 23 internships**.

### Bug hunt (founder: "find and fix them") — audit, one shipped regression, four confirmed bugs
- **PROCESS FAILURE, CLAUDE'S — own it:** commit e75202d (board ordering) **broke the crawler and CI FAILED on it**, and Claude pushed without checking CI, having reported it "verified". The verification used `pytest -k "crawler or adapter or ordering"`, which **does not match `tests/test_runner.py`** — a green filtered run hid 4 real failures. The filter was the mistake, not the code. Rule going forward: after changing a shared dataclass/helper in `crawlers/runner.py`, run the FULL crawler test FILES by path, never a `-k` filter, and check CI on the pushed SHA.
- **The regression itself (fixed, 9065c1d):** `_order_ats_jobs()` was called unconditionally while `last_crawled_by_board` was bound only inside `if ats_jobs:`. An **aggregator-only run** (`--group aggregator` — exactly the daily workflow's RemoteOK step) has zero ats_jobs, so it raised `UnboundLocalError` and would have killed that step outright starting with the next scheduled run. Dict now initialised before the branch.
- **Full-suite baseline taken (one run, egress-conscious): 6 failed / 388 passed.** 4 were the regression above. The other 2 (`test_match_forbes`, `test_match_prestige`) **pass in isolation (163s) but fail in a full-suite run — order-dependent pollution**, not a product bug. Detail not captured because the background command piped through `tail -15`; deliberately NOT re-running a 5-minute full suite against prod just to capture it.
- **Audit discipline — 8 candidates examined, 4 DISSOLVED on inspection and were NOT reported as bugs:** (a) 8 "title == company name" rows are Devpost/Unstop hackathons where the event legitimately IS the organiser; (b) the 5 "duplicate" groups are distinct Greenhouse requisitions (MongoDB x3, same day, different external_ids) — merging would HIDE real openings; (c) 831 past-deadline actives are 196 competitions already excluded by `exclude_closed_competitions()` plus roles deliberately bottom-ranked by the A1 open-first design; (d) the Keka `\bremote\b` false-positive risk ("Remote Sensing", and SatSure is a remote-sensing company) currently matches ZERO rows — latent, not live.
- **CONFIRMED BUG 1 — `uq_opportunity_sources` UniqueViolation, live, loses a listing per run.** `_ensure_provenance` decides insert-vs-update purely from `board_state.provenance_by_key`, which is built from ONE company's opportunities — but `raw_by_external_id` is loaded SOURCE-WIDE, so `ingest_one`'s identity fast-path can reach an opportunity of a DIFFERENT company whose links were never cached. It then inserts and collides with an existing row. This is what made run 29805312659's unstop leg `status: partial`.
- **CONFIRMED BUG 2 — Razorpay webhook `payload["payload"]["subscription"]["entity"]`** is bare indexing after signature verification: a known event with an unexpected shape raises KeyError -> 500 -> Razorpay retries the same broken payload forever. Matters before go-live.
- **CONFIRMED BUG 3 — dream-companies writes have NO rate limit** while `bookmarks` (identical shape of operation) uses `enforce_bookmark_write_limit`.
- **CONFIRMED BUG 4 — RemoteOK is STARVED, not deprioritised.** `aggregator_jobs.sort(key=lambda job: job[1] == "remoteok")` pins it last, and unstop (1,414 listings) exhausts the shared 12-min aggregator budget before it is reached. **Last successful RemoteOK crawl: 2026-07-10, eleven days ago.** Its 227 active listings are drifting to dead links (23 already >14d unseen). Being last was intentional ("lowest-value tier"); never running at all was not. Fix direction: give aggregators the same stalest-first fairness the ATS boards already have. NOT yet fixed.

### Bug fixes 1-5 landed [cfd6c78, b8e4fac, 87263d1]
- **BUG 1 provenance collision [cfd6c78]:** on a cache MISS `_ensure_provenance` now looks the `opportunity_sources` row up before inserting and reuses it when present. Cache-HIT path untouched — still zero queries, so no cost on the common path. Lookup uses `coalesce(source_url,'')` so NULL and '' cannot diverge into different keys, matching `load_board_state`'s own convention.
- **BUG 2 webhook [cfd6c78]:** malformed-but-signature-valid payloads now return 200 `{"status":"malformed_payload"}` instead of KeyError->500->infinite Razorpay retries. Signature verification, 503-when-unconfigured and 400-on-bad-signature all unchanged.
- **BUG 3 dream-companies [cfd6c78]:** added `rate_limit_user_dream_company_write_per_minute` (config, not code) + a limiter diffed byte-for-byte against the bookmark one (differs only in name/bucket/setting).
- **BUG 4 RemoteOK starvation [b8e4fac]:** aggregators now ordered stalest-first like ATS boards, with remoteok-last kept only as a TIE-break and adapter_key as a final deterministic key. Self-balancing: whatever runs today is freshest tomorrow. Verified against the real timestamps — starvation case orders remoteok->unstop->devpost; an all-equal tie still orders devpost->unstop->remoteok. Staleness read set-based, MAX per source, no `page_key` assumption, and `last_crawled_by_source` bound OUTSIDE the `if` (the exact UnboundLocalError trap from e75202d).
- **BUG 5 match-test isolation [87263d1]:** `test_match_forbes` and `test_match_prestige` created companies with the SAME hard-coded domains/names (slugs were uuid'd, domains/names were not). The matcher scans every company row, so whichever file's row it resolved first took the rank and the other's assertion failed. The per-test uuid is now threaded through fixture AND rows in both files, with placement chosen so each assertion still pins its behaviour: rank 12 keeps company UPPER vs fixture lower (case-insensitivity), rank 42 keeps " LLC" AFTER the uuid (suffix strip), rank 5-vs-80 still proves domain beats name. **Verified on the actual reproduction — the two files run TOGETHER, which is what used to fail: 2 passed.**
- **Verification discipline after the e75202d miss:** every fix above was checked with FULL test files by path, never a `-k` filter. 50 passed (ingest+payments+dream+runner+crawler+ordering), 39 passed (ordering+runner+reliability+ingest), 2 passed (the match pair together).
- **CI reading correction worth keeping:** `cancel-in-progress` means superseded runs show `completed/cancelled`, which is NOT a pass. Only the newest pushed SHA proves anything — misreading a cancellation as green is how the e75202d regression would have stayed hidden.

### Razorpay go-live: steps 1-3 done, blockers cleared [0a2111a]
- **Step 1 (KYC)** was already complete. **Step 2 (legal.ts)** filled with the founder's real details — sole proprietorship, so the proprietor's name IS the registered entity. Verified in a browser: /contact, /privacy, /terms, /refunds all 200 with no `[To be completed before launch]` notice and no `TODO_` marker. **Step 3 (--relink)** shipped.
- **The trap that would have broken go-live silently:** prod holds Razorpay plan IDs created with TEST keys (`plan_TAyB...`). Test and live are separate object spaces, so those IDs do not exist under live keys and every live checkout would fail with "plan does not exist" — and `setup_razorpay_plans.py` SKIPPED any row that already had an ID, so re-running it with live keys did nothing. Now has `--relink`, `--dry-run`, and key-mode detection (prints MODE: LIVE/TEST from the `rzp_live_`/`rzp_test_` prefix WITHOUT echoing the key). 8 offline tests.
- **support@aspirova.org set up for inbound.** Apex had NO MX at all, and Vercel DNS offers no mailbox/forwarding, so the address on the legal pages would have bounced — including anything Razorpay sent during review. Used ImprovMX forwarding + 3 apex records added via `vercel dns add` (founder ran them; the DNS write was blocked by the permission classifier for Claude, correctly). Verified on public DNS: MX 10 mx1 / 20 mx2, and exactly ONE apex SPF.
- **Claude corrected itself on SPF, on evidence.** Initially advised AGAINST an apex SPF record fearing it would break Resend. Reading the actual zone showed Resend signs DKIM at the apex (`resend._domainkey`) but uses `send.aspirova.org` as the Return-Path — and SPF is evaluated against the ENVELOPE sender, not the From header. So an apex SPF cannot affect Resend, and adding it was correct. Retracted the warning rather than leaving the founder with a permanent ImprovMX error.
- **Windows CMD gotcha logged:** `vercel dns add ... '@' ...` fails with `Invalid 'name' parameter (400)` because CMD does not strip single quotes, so the literal `'@'` is sent. Unquoted `@` works.
- **NEXT (founder-only):** confirm a real test email reaches support@, then Step 4 — generate LIVE keys, set `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` in the Render dashboard (Claude never sees them), then run `setup_razorpay_plans --dry-run --relink` and finally `--relink`, then register the webhook + `RAZORPAY_WEBHOOK_SECRET`.
- **support@aspirova.org VERIFIED end-to-end (2026-07-21).** A genuinely external sender (different Gmail account) reached it and was forwarded through: headers show `to: support@aspirova.org`, `mailed-by: aspirova.org`, `Signed by: gmail.com` (original DKIM intact, as ImprovMX documents for external senders). Landed in SPAM — expected for a brand-new forwarding path, and the test body was literally "Hi", which is textbook spam content. Founder advised to mark Not-spam AND add a Gmail filter on `to:support@aspirova.org` -> "Never send it to Spam", so a Razorpay verification mail can never be buried.
- **Diagnostic worth keeping:** the earlier "failed" self-send test was NOT a misconfiguration. Gmail silently drops a message arriving with the same Message-ID it just sent, so mailing an alias FROM the address it forwards to always looks broken. ImprovMX's own warning mail actually PROVED inbound routing worked — they could only have known about that message by receiving it via the public MX records.

### RAZORPAY IS LIVE — first real payment verified end-to-end (2026-07-21)
- **Steps 4-8 complete.** Live keys generated, plans re-provisioned with `--relink`, webhook registered, and a REAL ₹39 payment taken and verified.
- **The relink trap was real and is now closed.** Live-mode Plans tab was EMPTY before the relink, confirming the four `plan_TAyB...` IDs existed only in test mode. After `--relink`: `pro_lite_monthly=plan_TGDlM2P6fAt4Ku`, `pro_monthly=plan_TGDlMoB9vcNRar`, `pro_lite_annual=plan_TGDlMZAv6gmcZe`, `pro_annual=plan_TGDlNLmCgfY0dT` — verified to match the prod `plans` table exactly, with correct amounts (₹39/₹49/₹399/₹499) and billing cycles, and ZERO stale test IDs.
- **Verified without ever handling a secret:** webhook secret confirmed by probing `POST /payments/webhook` unsigned -> **400 "Missing signature"**, not 503 "not configured" (that check runs first, so 400 proves the secret loaded). The API keys were proven only by the successful live charge, since their 503 sits behind authentication.
- **End-to-end result:** `sguide683@gmail.com` -> `pro_lite_monthly`, `sub_TGE2D75gDKRTDS`, **status=active** (so the webhook fired, signature verified, event mapped, DB updated). Gating correct for the tier: hidden_opps/instant_alerts on, copilot/resume_match OFF, dream limit 5.
- **CLAUDE GOT ONE WRONG — own it:** Claude asserted "the public site stays on the waitlist throughout; we flip `NEXT_PUBLIC_PAYMENTS_ENABLED` last" WITHOUT checking the flag's actual value. It was ALREADY `true` on Vercel, so the site was publicly accepting checkout the whole time. Consequence: between the plan relink and the live keys landing on Render, a real visitor clicking Subscribe would have failed. Low traffic and now closed, but the safe sequencing Claude designed was based on an unverified assumption. **Rule: verify the current value of any flag the plan depends on — do not infer it from the plan's intent.**
- **OPEN:** (a) live keys are still in local `backend/.env` — must be reverted to test keys or blanked, else a local run/test suite can create real subscriptions; (b) the ₹39 is refundable from Razorpay -> Transactions; (c) two stale TEST-mode subs for the founder (8 + 15 July) still read `active`, will never charge or receive webhooks, and inflate any active-subscriber count.

### Post-go-live: customer emails + the cancel-state bug [0c7c340, e85e5d5]
- **Subscription emails [0c7c340].** Going live turned a dormant gap into a real one: the webhook updated `subscriptions.status` and told the customer NOTHING — a payer got a Razorpay receipt and silence from us, and a FAILED card (`subscription.halted` -> `past_due`) silently revoked Pro with no warning. Now sends one email on a genuine transition into `active` or `past_due`, reusing the branded `email_layout` + `send_email` seam and recording a Notification row. Two constraints drove it: (1) transition-only, because `subscription.charged` fires on EVERY renewal so a naive send re-mails the welcome monthly and duplicates on redelivery; (2) the send runs AFTER the status commit inside try/except so an email failure can never surface as a 5xx — which would make Razorpay retry the same payload forever, the exact bug fixed in this file hours earlier. 14 tests.
- **FOUNDER-FOUND BUG [e85e5d5] — worth noting HOW it was found.** Claude verified the whole payment path through the DB and missed it entirely; the founder found it by clicking Cancel twice in the real UI. `cancel_subscription` told Razorpay to cancel at cycle end and persisted NOTHING locally, so "cancellation scheduled" lived only in that one HTTP response. Every page load then re-rendered the Cancel button. Not cosmetic: each extra click called Razorpay again, and cancelling an already-cancelling subscription can raise `BadRequestError` -> a **400 shown to a paying customer**.
- **Fix:** migration `f8d3c6a1b4e7` adds `subscriptions.cancel_at_period_end BOOLEAN NOT NULL DEFAULT false` (the server_default is load-bearing — NOT NULL without it fails against existing rows). Endpoint returns `cancel_scheduled` EARLY when already set, without a second Razorpay call; flag set only after a successful cancel. `PlanState` exposes it; `SubscriptionSection` swaps the Cancel button for the access-end date. 24 tests, frontend tsc+eslint clean.
- **Migration applied to prod (founder-authorized).** `f7b2c9e4d1a6 -> f8d3c6a1b4e7`; verified column type/nullability/default and that all 5 existing rows backfilled `false`. The 13 test failures seen BEFORE applying were purely the model/DB `UndefinedColumn` mismatch — checked the actual error rather than assuming, since "obviously it's the migration" is how a real bug hides.
- **Data reconciliation:** the founder's live sub was cancel-scheduled at Razorpay BEFORE the column existed, so the backfill left DB and Razorpay disagreeing. Claude's UPDATE was **blocked by the permission classifier — correctly**, since the authorization covered schema, not a data edit on a billing record. Founder ran a scoped, parameterised, idempotent one-off script instead: `sub_TGE2D75gDKRTDS` False -> True, 1 row. `period_end=2026-08-20T18:30Z` = 21 Aug 00:00 IST, matching what Razorpay displayed.
- **Lesson recorded:** today's bugs came from three DIFFERENT places — production data (provenance collision, RemoteOK starvation), CI (Claude's own regression), and a human using the product (this one). DB verification and real usage catch different classes; neither substitutes for the other.

### Post-go-live hardening pass (2026-07-22, using leftover weekly quota)
- **Gating revenue leak [6b11700].** `get_features`'s `or_(razorpay_sub_id.isnot(None), period_end is null, period_end > now)` — the first branch is true for every real sub, so the date check was never enforced. Any missed webhook = free Pro forever. Founder's own account proved it (full Pro from a test-mode sub whose id can't receive a live webhook). Fixed: drop that branch, add a config `subscription_grace_days=3` window, extract the predicate to `active_subscription_filters()` used by all three call sites that had hand-copied it. 72 tests.
- **+12 verified boards [6b11700], ~547 roles.** greenhouse groww/slice, lever meesho/mindtickle, ashby cursor/langchain/baseten/deepgram/fireworksai/railway/neon/runway. The BOGUS-token control caught 16 phantom Lever "boards" (api.lever.co returns an error OBJECT, len()=2). NOT yet seeded to prod (founder authorization pending).
- **Upgrade reconciliation [147599b].** `applied_with_error` (customer paid, Razorpay edit failed) now (1) emails the founder via `waitlist_notify_email` with everything needed to fix it, best-effort/unable-to-raise; (2) `scripts/reconcile_upgrades.py` retries the edit for every stuck row with --dry-run + key-mode print. 23 tests.
- **Notification audit — CLEAN, no bugs.** Six checks all correct: opt-out (`wants`), gating (`can`; closing-soon ungated by design), dedup + 20h cap, HTML escaping complete (incl. cta_url inside email_layout), send_email can't abort a batch, User.email is nullable=False (nearly filed a false null-check bug from the API schema's `str | None` — the MODEL is the source of truth).
- **Per-user isolation [5137bec].** The one real gap the audit DID find: send_email swallows errors but nothing else in the per-user body did, so a render/query/commit raising for one user silently skipped everyone after. Wrapped all four loops (digest, instant, closing-soon, weekly send) in try/except+rollback+log+continue, matching saved_search_alerts. The rollback is load-bearing: a poisoned session would cascade the failure to the next user. 33 tests.
- **Freshteam validated as a FUTURE source (not built):** robots is `Allow: /jobs` + `Disallow: /`, and job pages carry schema.org JobPosting JSON-LD (structured, not scraped). Caveats before investing: 1+N requests/board, and an unverified memory that Freshworks sunset Freshteam for new customers — check first.
- **Claude stopped here and SAID SO** rather than manufacturing work to spend quota — per the founder's own "don't waste it but don't invent it" framing.

### Prod housekeeping (2026-07-22, founder-authorized)
- **Stale test-mode subs cleaned.** Deleted the 4 test-mode subscriptions on sohanmandal1611 (sub_TB561GcWW9pZqy, sub_TB5H6u5VhOACWH, sub_TDuZ5tSPhFCOCT, sub_TDuak4VBwt6VDL) via a scoped script that targeted those ids explicitly and hard-refused any `sub_TGE%`. Verified first: 0 dependent subscription_upgrades, 0 dependent notifications. After: 1 subscription remains (the live sguide683 one, now `canceled`), every user resolves to `free`, 0 orphan upgrades, sohanmandal has 0 active subs (unblocks a fresh checkout for the live upgrade test). Honest subscriber count is now 0 paying.
- **12 new boards seeded to prod.** `12 created, 85 updated`; companies 1981 -> 2091. All 12 verified present with correct adapter + domain and `last_crawled_at=None`, so the deterministic newest-first tie-break puts them at the FRONT of the next crawl. greenhouse groww/slice, lever meesho/mindtickle, ashby cursor/langchain/baseten/deepgram/fireworksai/railway/neon/runway.
- **Signal:** 6 real user accounts exist (kumararka07, patlollashivacharanreddy, chandniali1338, + others) - the top of the funnel is not empty even pre-marketing.

### PLATFORM CONSTRAINT DISCOVERED (binding for all billing work): Razorpay cannot update UPI subscriptions
- The founder's Rs.10 live test of the same-period upgrade surfaced it: the top-up charged and local Pro granted correctly, but the `subscription.edit` leg failed and reconcile-retry returned Razorpay's own words - **"subscriptions cannot be updated when payment mode is upi"**. A hard platform rule, not a mandate-cap nuance: UPI Autopay mandates are fixed at authorization, so Razorpay refuses ANY update to a UPI-paid subscription. Retry can never fix an applied_with_error whose cause is UPI.
- **Consequence:** the wired in-place upgrade flow is structurally broken for UPI customers - i.e. for MOST Indian customers. Failure shape per UPI upgrader: customer pays the top-up and gets Pro (safety design held - nobody pays and gets nothing), but Razorpay keeps billing the OLD amount silently. A revenue leak per upgrade, invisible in-app, visible only in Razorpay. Cards are unaffected.
- **Fix design (next session, dark-first):** drop subscription.edit entirely; uniform cancel+recreate for all methods. Prorated top-up order (this leg is proven) -> create the NEW plan's subscription with `start_at` = the existing renewal date (renewal date preserved) -> customer authorizes the new mandate (one extra Razorpay step, unavoidable - the higher UPI mandate legally requires fresh consent, which is exactly WHY edit is banned) -> cancel the old subscription. The annual switch (dark) already avoids edit by design and is unaffected.
- Founder's own row 118 stays applied_with_error deliberately (he pays himself Rs.39 for his own Pro; nothing to recover). Discovery cost: Rs.10, by testing the cheap path before the Rs.373 one - which was the point.
- OPEN: confirm whether the founder-alert email fired (tests whether WAITLIST_NOTIFY_EMAIL is set on Render); founder to re-blank live keys in local .env after the reconcile run.

### 2026-07-23 — Work order: country-aware college picker (Claude → Codex)
- **Task:** Replace the free-text College input in Account → Profile with a country-aware picker: list keyed off the stored `aspirova.country` (same code the feed uses), lazy-loaded from `frontend/public/colleges/{ISO2}.json` (~200 files prepared in a prior session), pinned "Other — enter manually" fallback, inline country change writing through `storeCountryCode` so feed/pricing stay in sync, legacy free-text values preserved. Frontend only — `college` stays a plain string in the API.
- **Codex:** Built `CollegePicker.tsx` (modeled on `CountryPicker`: popover + search, 100-result render cap, loading/empty states, module-level fetch cache) and rewired `ProfileSection.tsx` with a `list | manual` entry-mode state + hydration guard. Sandbox couldn't spawn processes (known Win limitation) — read source via its node REPL, edits via apply_patch.
- **Review:** One correction round — the "Showing colleges in {country} · Change" helper also rendered in manual mode; gated to list mode. Verified by Claude: eslint ✅, tsc ✅, `/colleges/IN.json` serves ✅, `/account` renders clean signed-out ✅. Crawl health also checked today: green since Jul 20, 29m20s, ~617 new opps.

### 2026-07-23 — Work order 2: Account Area V2 refinement (Claude → Codex)
- **Task:** Implement docs/handoffs/ACCOUNT-AREA-V2.md — hierarchy/trust pass on /account grounded in Polaris + uxpatterns.dev + SaaSUI settings research: plan tile replaces the dead Member-since tile (free → Upgrade→/pricing; paid → Renews/Cancels date→subscription), In-progress tile made a link, profile-completeness strip on the summary card (shared lib/profileCompleteness.ts), dirty-state-gated Save in Profile, graduation year → clearable Select (currentYear±8, legacy values preserved), one-line plain-language billing sentence, global sign-out behind confirm dialog, password min 8. Identity untouched (forced-light almanac).
- **Codex:** Delivered all of it across page.tsx, ProfileSection, SubscriptionSection, SecuritySection + new profileCompleteness.ts; judged NotificationsSection needed no edit (instant-save toggles already give feedback) — accepted; used honest fallback copy when billing dates are absent.
- **Review:** One correction round — the cancelling-state summary dropped the canon "resubscribe from Pricing" pointer (deliberately added in a prior billing commit); restored compactly ("Access until {date} · resubscribe anytime from Pricing."). Verified by Claude: eslint ✅, tsc ✅, /account renders clean signed-out, console clean. ECONNREFUSED in dev logs = local backend not running (pre-existing, unrelated).

### 2026-07-23 — Work order 3: college data expansion + picker perf + profile alignment (Claude → Codex)
- **Task:** IN.json had only 476 colleges (missing 20 IIMs, 9 IITs, all WB affiliated colleges — Heritage/IEM/Techno). Build a zero-dep Node builder (frontend/scripts/build-colleges.mjs) that merges curated > existing > UGC > AICTE > Hipo with dedup + ALL-CAPS→title-case + non-degree (polytechnic/ITI) filtering + a never-shrink guard, run by the architect. Plus: chunked/progressive render in CollegePicker (11k entries, no cap message) and a College/Graduation-year alignment fix (content-start).
- **Sources (architect-downloaded, NOT committed — 12MB+):** AICTE dump github.com/anburocky3/indian-colleges-data (39,268), UGC universities github.com/Bluff-0/UGC_Indian-University-Dataset (981), Hipo world list (10,254). Curated India list (~280, hand-authored) IS committed at frontend/scripts/colleges-curated-in.json.
- **Codex:** Delivered the builder + picker chunking + alignment. Six correction rounds, each a distinct legit issue (all under-specified by the architect against a messy govt CSV, not Codex error): (1) invalid `\"` escape in a /u regex class; (2) UGC CSV embedded newlines in quoted fields; (3) stray/unbalanced quotes across 246 rows → rewrote UGC parse as per-physical-line extraction immune to quote-state cascade; (4) never-shrink guard false-positive on legacy dupes → compare against deduped-existing; (5) react-hooks/set-state-in-effect → moved reset into async load callback; (6) strip fully-quote-wrapped AICTE names.
- **Result (architect-run + verified):** IN 476 → **11,475**; global 10,235 → **21,232**; 75 country files grew, none shrank. Acceptance: 23 IIMs, 24 IITs, 22 AIIMS, Heritage/IEM/Techno present, 0 polytechnics/ITIs, 0 shouting-caps (bar the legit "NITTE"), list sorts cleanly from "A". eslint ✅ tsc ✅. Picker chunking + alignment verified by code; signed-in interaction not live-testable (auth-gated) — relying on code review + lint/tsc.

### 2026-07-24 — Smart Profile Phase A, Part 1: data model + taxonomy modules (Claude → Codex)
- **Task:** Foundation for the smart-profile feature (docs/handoffs/SMART-PROFILE-HANDOFF.md) — no UI. Backend: migration adding nullable JSONB field_profile/skills/exposure to users, AccountMe/AccountUpdate + shape/cap validators, account.py read/write, tests. Frontend: taxonomy.ts (typed helpers + expandToSearchTerms over frontend/lib/taxonomy.json) + fieldProfile.ts (external-store, local-only for now; server sync deferred to Part 2).
- **Codex:** Delivered all of it cleanly, first pass, zero correction rounds. Migration e1a5c9f2b7d4 chains from head d1e7c4b9a6f3. Validators cap divisions/interests to 64 keys, skills to 100 items, exposure text to 2000 chars; taxonomy-key membership intentionally not validated server-side (frontend owns the taxonomy).
- **Review (architect-run):** ruff ✅ black ✅ ; migration applied to DB (additive/nullable, reversible) ✅ ; `python -m pytest tests/test_account.py` = 17 passed (targeted, per egress discipline) ✅ ; frontend tsc ✅ eslint ✅. Committed on branch feat/smart-profile-phase-a; push HELD to batch Phase A into one CI run. Note: `pytest` launcher is blocked by Windows AppControl — use `uv run python -m pytest`.

### 2026-07-24 — Smart Profile Phase A, Part 2: 3-step onboarding + account editor + server sync (Claude → Codex)
- **Task:** Rebuild OnboardingDialog as Stream→Division→Interests (taxonomy-driven), add a Fields & interests block in Account→Profile (reuses the dialog via requestOnboarding), write-through to /account/me on finish for signed-in users, and hydrate the client store from the server on account load. Frontend only; ForYouControl/feed/api/for_you and interests.ts left for Part 3.
- **Codex:** Delivered the 3-step state machine (country alongside step 3), the account summary+Edit block, a once-per-token server→local hydration guard (server wins, in-flight-change protected), and added the missing AccountMe.field_profile to types.ts. Used a type-safe intersection to pass field_profile through updateAccount (its param type lacks the field until Part 3 widens api.ts).
- **Review:** One correction — the rebuild dropped country persistence (storeCountryCode went unused → real regression: onboarding country no longer saved to localStorage that the feed/pricing/college-picker read); restored the finish-time storeCountryCode call. Verified LIVE in preview: dialog walks Engineering→CSE→interests, all 18 CSE interests render, finishing writes field_profile {stream:engineering,divisions:[cse],interests:[data_science,ai_architecture,blockchain]} + onboarded=true + routes with a correct terms= param. tsc ✅ eslint ✅ (no warnings). Committed on feat/smart-profile-phase-a; push still HELD.

### 2026-07-24 — Smart Profile Phase A, Part 3: feed consumes the profile + retire flat interests (Claude → Codex)
- **Task:** /for-you accepts a `terms` param (terms-over-fields precedence, same safe websearch_to_tsquery path); ForYouControl derives terms from useFieldProfile()+expandToSearchTerms; feed page forwards `terms`; api.ts typed for field_profile (removing the Part-2 workaround); delete lib/interests.ts and relocate OPEN_ONBOARDING_EVENT/requestOnboarding to fieldProfile.ts.
- **Codex:** Delivered all of it; grep confirms zero remaining interests.ts references; event string unchanged. One janitorial fix by architect: `black` reformatted for_you.py (formatting only).
- **Review (architect-run):** ruff ✅ black ✅ ; `python -m pytest tests/test_for_you.py` = 8 passed (terms ranking, blank/whitespace, precedence, junk) ✅ ; frontend tsc ✅ eslint ✅ `next build` ✅ (validates the interests.ts deletion). Phase A COMPLETE.
- **Push:** Phase A (Parts 1+2+3) merged to master as ONE batch = one CI run. Migration e1a5c9f2b7d4 already applied to prod DB during Part 1 verification, so Render's deploy upgrade is a no-op.

### 2026-07-24 — Smart Profile Phase B, Part 1: resume extraction + ATS engine (Claude → Codex)
- **Task:** Deterministic (no-AI) client libs — pdfText.ts (browser PDF text extraction via pdfjs-dist, the one authorized dep), resumeSkills.ts (whole-word/phrase lexicon match, safe for c++/c#/.net), atsScore.ts (transparent 0–100 rubric: parseability, contact, sections, quantified impact, action verbs, length, field relevance — each with an estimate-labeled tip). Architect authored skillsLexicon.json (~140 skills). No UI (that's B2).
- **Codex:** Delivered all four; pinned pdfjs-dist 5.4.624; worker wired via new URL("pdfjs-dist/build/pdf.worker.min.mjs", import.meta.url). One refinement round: ATS section detection was exact-line-match (missed "TECHNICAL SKILLS"/"Professional Experience"/"Academic Projects") → now heading-like-line + word-boundary keyword phrases for better accuracy.
- **Review (architect-run):** pnpm install (lockfile updated for frozen-lockfile CI) ✅ ; tsc ✅ eslint ✅ ; **next build ✅ (pdfjs worker resolves under Next — the main risk)**. Committed on feat/smart-profile-phase-b; push HELD until B2.

### 2026-07-24 — Smart Profile Phase B, Part 2: resume upload UI (Claude → Codex)
- **Task:** Rebuild /resume (ResumeMatchPage) — free for all signed-in users: PDF upload (local extract via B1) or paste → editable Skills & Exposure (persist to /account/me) + ATS score with an (i) tips popover; existing Pro semantic "Matches for you" gated inline below.
- **Codex:** Delivered the restructure (extracted focused sub-components, kept MatchesList). Free vs Pro split correct (only uploadResume/getResumeMatches trigger the inline upsell). Careful skill merge (case-insensitive, manual preserved, dismissed-skill tracking so removed suggestions don't re-add, cap 100), dirty-state save.
- **Review (architect-run):** tsc ✅ eslint ✅ `next build` ✅ ; /resume renders clean signed-out (no console errors); ATS engine functional-tested via Node type-strip — a good sample scored 90/100 with correct per-check passes (heading-variant detection confirmed: TECHNICAL SKILLS / WORK EXPERIENCE / ACADEMIC PROJECTS), empty resume → 6/100 parseability fail. Signed-in upload/skills/matches flow is code-reviewed + build-clean but NOT live-tested (auth-gated) — founder morning task.
- **Push:** Phase B (B1 c3a3a63 + B2) merged to master as one CI run. No migration in Phase B (skills/exposure columns landed in Phase A).

### 2026-07-24 — Smart Profile Phase C: resume skills feed the personalized ranking (Claude → Codex)
- **Task:** Fold saved resume skills into the For You feed terms (deterministic, frontend-only). New lib/personalizationSkills.ts (client mirror of skill names, fieldProfile pattern); taxonomy.buildFeedTerms(profile, skillNames, maxChars=480) — interests first, dedup, greedy CSV cap under the backend's 500 limit; ForYouControl + OnboardingDialog use it; ResumeMatchPage syncs saved skill names to the mirror.
- **Codex:** Delivered per spec. ForYouControl now personalizes on interests OR skills (skills-only users included) and waits for both stores hydrated. Cap keeps terms.join(",") ≤ 480 chars.
- **Review (architect-run):** tsc ✅ eslint ✅ next build ✅. buildFeedTerms cap logic verified by inspection (comma-accounted greedy fill). Signed-in skills→feed path is code-clean but full live test needs a login (founder). Smart Profile A+B+C COMPLETE.
- **Also today:** diagnosed the Supabase "2,650 Postgres errors / 22% success" — it's CI+local pytest running against the PROD DB (ci.yml uses the prod DATABASE_URL secret; ~6 pushes = ~6 full-suite runs with intentional error-path tests). Benign; app/backend healthy; no data exposed (RLS denies anon). Fix proposed (not done): give CI its own pgvector service-container DB. See [[project_supabase_usage_discipline]].

### 2026-07-24 — CI: move backend tests off prod onto an ephemeral pgvector container (Claude → Codex)
- **Task:** ci.yml backend job ran `pytest -v` against the PROD Supabase DATABASE_URL every push (egress + a slice of the error noise). Switch to a GitHub Actions `pgvector/pgvector:pg16` service container: migrate → seed_plans → seed_ci → pytest; drop the prod secret. New scripts/seed_ci.py = deterministic corpus for the read-only API tests (test_api.py seeds nothing).
- **Codex:** ci.yml uses the container on localhost:5432 (session mode → timeout guards apply), URL scheme postgresql+psycopg (psycopg3), prod secret removed. seed_ci.py: 3 companies + 15 active opps (12 "Software Engineer" titles, 2 internships, 6 remote), ci-seed- prefix (dodges the isolation purge), https apply_urls, trigger-populated search_tsv.
- **Review (architect):** ruff+black ✅. **Dry-ran the seed against prod inside a ROLLED-BACK txn** — all columns/constraints valid, 15 active opps, 12 FTS "software engineer" matches (satisfies limit=10 tests), search_tsv trigger fires; trigram similarity of seeded titles vs the typo query "sofware enginer" = 0.43–0.62 ≥ 0.3 threshold (satisfies the fallback test). Cannot run the full suite locally (no Docker) → verifying via a PR CI run. NOTE: this is egress/hygiene; it does NOT fix the 2,650 PostgREST errors (that's the Data API dashboard setting).

### 2026-07-24 — Skills picker: stream-aware searchable dropdown (Claude → Codex, gpt-5.5 xhigh)
- **Task:** Replace the free-text "Add a skill" input on /resume with a searchable dropdown filtered by the onboarding stream, + an "Other → type it" path. Architect expanded the lexicon to 212 skills (Supabase/Ollama/etc.) + a field_streams map first.
- **Codex:** Added lib/skillsCatalog.ts (skillMatchesStream + catalogSkills: stream default list, all-skill alias search, starts-with priority, exclude, cap 60) and rewired ResumeMatchPage's add-skill into a CollegePicker-style Popover reading profile.stream, reusing the existing manual-add normalize/dedupe/cap/dismissed logic (not duplicated). Dispatched via the Bash tool (PowerShell cmd/c dispatch was silently failing — see below).
- **Review (architect):** tsc ✅ eslint ✅ next build ✅ (all run via node binaries; PowerShell tool flaky). Data-verified: Engineering default list = 160 incl. Supabase/Ollama/Python; Management excludes Supabase (56, incl. HubSpot); search "ollama"/"supa" find them cross-stream. Signed-in live interaction needs a login (founder).
- **Env note:** Codex was NEVER quota-blocked (my earlier claim was wrong). PowerShell `cmd /c ... < NUL` dispatch went silently failing mid-session; **dispatch Codex via the Bash tool** (`"$codex" exec ... - < spec`) on this machine.

### 2026-07-24 — Fix wrong competition due dates (Claude → Codex, gpt-5.5 xhigh)
- **Task:** Live prod showed absurd competition deadlines (2030/2047/2059) — Unstop's source end_date carries garbage far-future years and the parser stored them with no plausibility bound. Add a deadline plausibility window; leave timezone handling + ATS "unknown" alone.
- **Codex:** common.py MAX_DEADLINE_HORIZON=550d + is_plausible_deadline(); unstop._registration_deadline + devpost._parse_submission_deadline reject >550d-out → None → deadline_confidence="unknown" (end_regn_dt preference intact; past handled by expiry). Tests for far-future→unknown / near-future→explicit in both adapters. New scripts/fix_garbage_deadlines.py (dry-run default, --apply) for the existing prod rows.
- **Review (architect):** ruff ✅ black ✅ ; `python -m pytest test_unstop_adapter + test_devpost_adapter` = 24 passed ✅. Dry-ran the cleanup script (read-only): 5 garbage rows found (StartUp Expo/Capture The Flag 2059, Panenka 2.0 2047, DEMOKHE/Sell Me This Pen 2030). Crawler fix prevents new ones; the 5 existing need `--apply` (founder-authorized prod write — the change-detection skip means unchanged garbage boards won't self-heal). Dispatched via Bash (working method).

### 2026-07-24 — Sources Lever B Part 1: Workable adapter (Claude → Codex, gpt-5.5 xhigh)
- **Task:** New per-company Workable ATS adapter (Doc 04 pattern, mirrors greenhouse.py). Architect verified the LIVE API + exact shape first (huggingface board, apply.workable.com/api/v1/widget/accounts/{acct}?details=true → {name,description,jobs[]}).
- **Codex:** crawlers/workable.py + ATS_ADAPTERS registration + fixture tests + tests/fixtures/workable_sample.json + scripts/seed_workable.py (founder-run: sources row + huggingface company).
- **Review (architect):** 2 corrections — (1) missing required `source_slug` on RawListing/NormalizedListing (test + live both failed); (2) seed_workable used raw SQL with a non-existent `sources.updated_at` and omitted required `sources.slug` → rewrote to mirror seed_ci.py (ORM, make_engine, correct columns). Then: ruff ✅ black ✅ ; test_workable_adapter 3 passed ✅ ; **LIVE huggingface fetch → 7 jobs, parse correct** (source_slug=workable, category=job, remote=true, apply_url ok, location "Paris, Île-de-France, France", posted 2026-05-29, desc 6334 chars) ✅ ; seed dry-run (rolled-back) inserts valid ✅. Committed on feat/source-adapters; Recruitee (Part 2) next, then merge both.

### 2026-07-24 — Sources Lever B Part 2: Recruitee adapter (Claude → Codex, gpt-5.5 xhigh)
- **Task:** Recruitee ATS adapter, grounded in live boards (sendcloud, effectory; {sub}.recruitee.com/api/offers/). source_slug pitfall + seed schema facts pre-empted in the spec.
- **Codex:** crawlers/recruitee.py + ATS_ADAPTERS entry + fixture tests (incl. a draft offer that must be filtered) + seed_recruitee.py (ORM, sendcloud+effectory). source_slug correct first time.
- **Review (architect):** 1 correction — fetch() returned a tuple (health, listings), violating the `Iterable[RawListing]` contract (the runner does list(fetch())); the fixture tests had been written to the wrong signature so they masked it. Fixed to return the list; health via health() only. Then: ruff ✅ black ✅ ; 5 tests ✅ ; **LIVE sendcloud (1) + effectory (9) fetch/parse correct** (slug=recruitee, category, apply http, deadline unknown) ✅ ; seed dry-run (rolled back) rows valid ✅.
- **STABLE STOP (Claude on usage credits until 5pm):** Lever B (Workable + Recruitee) merged to master; Lever A (bulk company seeding) NOT started. Deadline --apply still awaiting founder OK.

### 2026-07-24 — Sources Lever A: +24 verified company boards; prod seeds + deadline cleanup applied
- **Founder authorized all pending prod writes.** Ran (prod): seed_workable (source+huggingface), seed_recruitee (source+sendcloud+effectory) → **sources 9→11**; fix_garbage_deadlines --apply → **5 garbage competition deadlines nulled → 0**.
- **Lever A:** architect probed candidate boards LIVE (bogus-token controls) and added 24 verified NEW companies to scripts/seed_companies.py — 14 Greenhouse (checkr, webflow, calendly, amplitude, mixpanel, launchdarkly, pagerduty, elastic 208, circleci, vercel 80, fivetran 193, monzo, gocardless, remote.com 237) + 10 Ashby (prefect, writer, pika, suno, poolside, lovable, cognition/Devin, zed, warp, mintlify). Codex inserted the tuples; ruff/black ✅. Ran seed_companies (prod): **24 created, 97 updated; companies 2204→2228**; all 24 never-crawled → crawl first next scheduled run (no manual trigger, protects Actions budget).
- Net: 9→11 sources + 24 new boards (~1,000+ listings) queued for the 01:00 UTC crawl; competition due dates corrected.

### 2026-07-24 — Sources Lever A wave 2: +19 verified boards
- Architect probed a fresh batch live (dedup vs existing + workable/recruitee; bogus-token controls). Added 12 Greenhouse (twilio 178, algolia, contentful, planetscale, clickhouse 172, starburst, labelbox, tailscale, marqeta, mercury, stabilityai, block 204) + 7 Ashby (cartesia, krea, ideogram, tavus, synthesia 76, gamma, granola). Codex inserted tuples; ruff/black ✅. Seeded prod: 19 created; companies 2228→2247, all never-crawled.
- **BoschGroup (SmartRecruiters, 4767 jobs) verified-available but DELIBERATELY NOT added** — seed_companies.py holds it out to bound crawl duration until lazy detail-fetch lands (respected the existing decision). **Lever yielded 0 across both waves** — its board tokens aren't guessable from company names; needs real jobs.lever.co/{token} URLs to source (open gap).
- Running total this session: sources 9→11 + ~46 new never-crawled boards (~1,800+ listings) queued for the next crawl.

### 2026-07-24 — Per-opportunity skills Part 1: deterministic extraction + storage (Claude → Codex, gpt-5.5 xhigh)
- **Task:** derive required skills per opportunity (deterministic, no AI), store on opportunities, per OPPORTUNITY-SKILLS-HANDOFF.md. Architect authored role-skills map.
- **Codex:** pipeline/skills.py (lexicon+role extractor, role map intersected w/ lexicon), skills_lexicon.json+role_skills.json (backend copies), migration a6e4c2b9d8f1 (opportunities.skills jsonb + GIN), models.py, ingest.py (set skills on create/update), backfill_opportunity_skills.py, tests.
- **Precision pass (1 correction):** dry-run on real JDs showed prose false positives → added (a) company-name guard (GitLab postings no longer yield "Git" via the company name), (b) exclude field=="other" soft skills from prose (still via role map), (c) alias stop-list {compliance,design,research,spring,notion}. Result on real data: OpenAI FDE→Python/SQL/Data Analysis/REST APIs; frontend→React/TS/CSS; clean.
- **Review (architect):** ruff/black ✅ ; 9+14 tests ✅ ; migration applied to prod ✅ ; extraction validated on real prod opps ✅. Skills NOT in any API/card yet (Part 2/3).

### 2026-07-24 — Per-opportunity skills Parts 2+3 (Claude → Codex, gpt-5.5 xhigh)
- **Part 2 (API):** OpportunityDetail.skills (detail only, NOT OpportunityListItem/cards); /for-you `skills` CSV → jsonb `?|` overlap filter + overlap-count ranking. Verified LIVE: skills=Python,React,SQL → 4060 matches, top results all overlap=3, ranked desc; cards carry no skills. 2 new ranking tests fail LOCALLY-vs-prod (isolation artifact, like the CI-DB work) → pass on CI's ephemeral DB; verifying via PR.
- **Part 3 (frontend):** OpportunityDetail type += skills; ForYouParams+getForYou send `skills`; feed page reads `skills` param; ForYouControl sends the user's SAVED skills (useSkillNames) as the skills param so For You ranks by skill overlap; detail page renders a "Skills for this role" chip section (detail-ONLY, hidden if empty, labeled "detected"). OpportunityCard untouched (0 skills refs). tsc+eslint+next build ✅.
- **Landing via PR** (not direct ff) so CI validates the 2 skill-ranking tests on the isolated DB before master.

### 2026-07-24 — Per-opportunity skills PRECISION polish
- Frequency analysis of backfilled data exposed over-firing false positives: Salesforce 18%, Communication 20%, Excel 16%, Auditing 8%, Taxation 6%, UI/UX 8%.
- Fixes: (1) architect pruned Communication + Salesforce from role_skills.json (both copies) — soft/over-applied. (2) Codex: role-phrase matching is now word-boundary (so "sales" no longer matches "salesforce"/"pre-sales") + stop-list expanded to {compliance,design,research,spring,notion,audit,tax,user experience,user interface}.
- Verified: ruff/black/ruff-all ✅, 10 extractor tests ✅; real AE/BD roles now show only real description skills (AWS/IoT/Cloudflare) or honest empty, not fake Salesforce/Communication. Re-backfilled prod (4,708 opps recomputed). New top-skill freq: Communication/Auditing/Taxation/UI-UX gone from top-14; Salesforce 18%→9%; Python/ML/SQL/AWS/Data-Analysis are the real signal. Excel (16%) kept (legit skill; some verb noise remains — accepted).

## 2026-09-01 17:05 — Work order 1 (Claude → Codex)
- Task: Ingest Unstop `scholarships` (2,383 declared) as a new `scholarship` category, behind a quality gate; wire the category into for_you/saved_searches/filters but NOT into the competitions tab or the hackathon digest.
- Codex: killed by a 10-min tool ceiling before writing its summary, but the edits landed — adapter type + category mapping, `is_high_quality_scholarship_title`, coverage rejection counters, API whitelists, tests.
- Review: **corrections sent.** Lint/format/tests pass (69) and the four do-not-touch filters were correctly left alone. My spec cited `feed.py:62` as the competitions filter and said don't touch it; Codex verified the line numbers itself and correctly changed line 36 (the `category` query-param validator) instead — **my spec was wrong, Codex was right**. Two `test_for_you` failures confirmed PRE-EXISTING on clean master (test-isolation, `assert 224 == 2`). Real failure found by running the gate against a live Unstop page: 11/40 rejected correctly, but of 29 kept only ~7 were genuine aid — six "Free Scholarship Test on X" lead magnets, a study-abroad festival, a campus-ambassador drive and an aptitude test all passed. Round 2 sent with the verbatim live titles as a regression suite and a new positive requirement (must contain an aid noun).

## 2026-09-01 17:40 — Work order 1, round 2 (Claude → Codex)
- Task: Raise precision. Added a POSITIVE requirement (title must carry an aid noun) plus rejection of aid-noun-qualifying-a-test lead magnets; regression suite parametrised over the verbatim live titles.
- Codex: added the aid-noun gate, the scholarship-test/exam/assessment rule, generalised the study-abroad shape, and built the 2026-09-01 live regression matrix. Could not run pytest/ruff/black (sandbox `CreateProcessAsUserW failed: 5`); did a static JS-equivalent check of all 34 examples instead.
- Review: **verified pass, with one direct fix by Claude.** black + ruff clean, 105 tests pass. Live re-check on a 60-item page: precision ~25% -> ~85%, every previously-wrongly-kept title now rejected. Two survivors slipped through because "Scholarship" was literally in the title — "Lead gen - scholarship events" and "Lead to Win: Scholarship Edition". Two rounds were spent, the gap was small and specific, so per the founder's standing rule Claude added `_MARKETING_FUNNEL_TITLE_RE` directly and extended the regression list. Final: 24 kept of 60, ~92% precision.

## 2026-09-01 18:20 — Work order 2 (Claude → Codex)
- Task: Advanced filters Part A (extract the shared reputed-organiser regex; extend /facets with counts + kind scoping) and Part B (competition filter params on /feed: comp_type, registration, deadline_within, organiser_type, mode, prize_min). Backend only.
- Codex: delivered all of it — `core/organisers.py`, extended `/facets` additively, six filter helpers, two new test files. Flagged honestly that it could not run pytest/ruff/black or EXPLAIN ANALYZE (sandbox cannot spawn processes), and that it could not read the handoff doc (not yet on master) so it built strictly from the work order.
- Review: **verified pass.** black + ruff clean, 63 targeted tests pass including the hackathon-digest lookalike guards that prove the regex move changed no behaviour. Live checks: every filter returns non-zero and cross-checks against raw meta (case_competition 52 = raw 52; paid 99 = raw 99; mode=online 433 ≈ 342 lowercase + 94 "Online", so case-folding works). Facets return 2 mode options from 64 dirty raw values — venue names correctly excluded. Ran the EXPLAIN ANALYZE Codex could not: **Seq Scan, 12.9 ms, all buffers cached** — so NO GIN index, it would add write cost on 27k daily-updated rows to speed up a query that is already fast.

## 2026-09-02 — Work order 3 (Claude → Codex)
- Task: (1) fix the hackathon digest reporting "closes in 2 days" for every item every day; (2) Part C — Research/Programmes filters, backend + frontend, reusing the competitions filter components.
- Codex: delivered both. Digest now diversifies fill picks by deadline-day with reserved picks winning collisions; programmes gained multi-value category/status/field/organiser/institution_type plus `?source=programmes` counted facets; frontend got a shared `CountedAdvancedFilterShell` reused by Competitions and Programmes. Could not run any gate (sandbox cannot spawn processes) but ran TypeScript compiler-API and ESLint Node-API checks itself: 0 diagnostics.
- Review: **verified pass, with two direct fixes by Claude.** ruff/black clean, 30 targeted tests pass, tsc + eslint clean, `/competitions` still builds `○` static (the regression that would matter). Programme filters cross-check against raw data: category multi = 25 (16+9 exactly), field=research = 56 (exact tag count). (a) The digest still showed TWO "+2 days" picks because my spec only diversified the FILL pool — both reserved reputed slots landed on the same day. Extended `_deadline_diverse_selection` with a `max_items` cap and applied it to the reserve too; production now returns 2/3/4/5/6 days, 5 distinct of 5. (b) Facet labels rendered "Cs" and "Ai"; added a small acronym map.

## 2026-09-03 23:05 — Work order (Claude → Codex): hackathon digest quality gates
- Task: four founder-reported digest defects — 2/3/4/5/6 deadline ladder, wrong deadlines on taken-down listings, school-only events reaching a college list, all five picks from Unstop.
- Codex: implemented freshness + explicit-confidence gates, structured school-eligibility filter wired site-wide, aggregator absence branch in expire.py, source caps and spread deadline buckets with a date-seeded shuffle; added regressions to test_hackathon_digest.py and test_expire.py.
- Review: corrections sent once (an obsolete positional assertion in test_lookalike_organisers_are_not_treated_as_top_tier — rewritten around the reserve invariant, not deleted). Claude restored one assertion Codex removed on an over-broad instruction of Claude's own (rendered order must not be sorted — that IS the reported defect). Verified pass: ruff, black, 39 digest/expire tests, 114 API tests, plus a read-only production preview of the next digest.
